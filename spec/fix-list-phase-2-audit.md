# Phase-2 Audit — Numerical Fix List

Numerical (not architectural) parity divergences surfaced during the v41-port
verification loop. Each item is a genuine arithmetic-order or structural
divergence against ngspice at the bit-exact bar; none are "tolerance" /
"pre-existing" wave-throughs. Items here block the harness gate of the recon
that surfaced them until resolved.

---

## FIX-001 — BJT DC operating-point junction-solve arithmetic-order floor (blocks `bjt#recon/stampAc` harness gate)

- **Surfaced by:** RECONSTRUCTION VERIFIER for `bjt#recon/stampAc` (2026-05-30).
- **digiTS:** `src/components/semiconductors/bjt.ts` — `createSpiceL1BjtElement` `load()` (the BJT junction exp/log operating-point Newton solve that writes the `VBE` / `GPI` / `GMU` / `GM` / `GO` / `GX` state slots). The recon's new `BjtL1Element.stampAc` only *reads* these slots; it does not introduce the divergence.
- **ngspice:** `ref/ngspice/src/spicelib/devices/bjt/bjtload.c` (junction current/conductance evaluation) feeding `bjtacld.c::BJTacLoad`.

### Evidence

Harness gate run on the two task fixtures
(`src/solver/analog/__tests__/ngspice-parity/fixtures/bjt-common-emitter.dts`,
`bjt-bistable-latch.dts`), DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`.

**AC sweep** (`harness_run_ac` — exercises the new `stampAc`):

| fixture | class | cell | ours | ngspice | absDelta | relDelta |
|---|---|---|---|---|---|---|
| common-emitter | matrix (re, im=0 both) | row 2, col 1 @ 1 Hz | -1.3866230645868108e-12 | -1.3866230646249369e-12 | 3.81e-23 | 2.75e-11 |
| bistable-latch | matrix (re, im=0 both) | row 1, col 2 @ 1 Hz | 0.15015689031346008 | 0.15015689031346113 | 1.05e-15 | 7.02e-15 |

The divergence is **purely in the real part** (every imaginary susceptance cell
matched bit-exact — i.e. the recon's `stampElementImag` ωC stamps are clean).
Shape clean: 61/61 AC points present both sides, no frequency deltas.

**Transient** (`harness_run` — does NOT touch `stampAc`, exercises `load()` only):
both fixtures, 107/107 steps converged both engines, stepCount delta 0,
avgIterations 2.047 both. `harness_first_divergence` `earliest` is the **state**
class at step 0 / iter 0:

| fixture | earliest attribute | ours | ngspice | absDelta |
|---|---|---|---|---|
| common-emitter | Q1.VBE | 0.6928875984089286 | 0.6928875984089307 | 2.11e-15 |
| bistable-latch | Q2.VBE | 0.6902044442373514 | 0.6902044442373494 | 2.00e-15 |

The transient matrix cell `(R_B:pos/R_C:pos/V_CC:pos, Q1:B/R_B:neg)` is the SAME
~1.386623e-12 near-zero cell that diverges in the AC run, at the SAME floor —
confirming the AC real-part matrix divergence is the downstream image of the
operating-point `VBE` divergence carried through the state slots, NOT a fault in
the recon's stamp.

### Classification

Genuine arithmetic-order divergence in the BJT junction exp/log Newton solve
(~10× f64 epsilon on `VBE`, ~3e-15 relative). It is present in the transient
path that never calls the recon's `stampAc`, so it is upstream of and
independent of `bjt#recon/stampAc`. It is NOT transcendental-shim ULP noise
(that floor was eliminated 2026-05-13). Root cause to locate: the per-device
`load()` accumulation order / junction evaluation sequence in `bjt.ts` vs
`bjtload.c` (the exp/log argument assembly and the gpi/gmu/gm/go derivation
order), per CLAUDE.md's "look upstream" directive for a bit-identical-matrix
1-ULP class.

### Decision needed (user)

Locate and close the `VBE` operating-point arithmetic-order divergence in the
BJT `load()` junction solve so the `bjt#recon/stampAc` harness gate goes null
across all classes. The recon source itself is isomorphic to the `bjtacld.c`
v26 baseline (verified independently) and its imaginary stamps are bit-exact;
only the inherited real-part floor blocks the gate.

---

## FIX-002 — nodeset/IC exact-`1` pure-voltage constraint not applied at runtime (blocks `maths-sparse#recon/nodesetIcRowZero` harness gate)

- **Surfaced by:** RECONSTRUCTION VERIFIER for `maths-sparse#recon/nodesetIcRowZero` (2026-06-01, via the A2 isolated loop).
- **digiTS:** `src/solver/analog/ckt-load.ts` — `cktLoad` Step 4a nodeset apply + Step 4b IC apply (and the `zeroNoncurRow` helper they call).
- **ngspice:** `ref/ngspice/src/spicelib/analysis/cktload.c:108-158`; `ref/ngspice/src/maths/sparse/spsmp.c:454-471`.
- **Recon source preserved at:** `.wt-failed/maths-sparse.diff` (the worktree was captured + destroyed on the gate fail; not committed; ledger stays PENDING).

### Source isomorphism — PASS (the port is correct; the divergence is at RUNTIME)

Re-derived independently against `ref/ngspice`: `findElement` (`sparse-solver.ts`) ≡ `SMPfindElt(...,0)` incl. the `intCol===-1` guard + read-only invariant (no `_insertionOrder` push); `zeroElement` ≡ `*x=0.0` (real-part only); `size` getter; `setRHS` (`stamp-helpers.ts`) ≡ `CKTrhs[node]=value` assignment; `zeroNoncurRow` ≡ `ZeroNoncurRow` (`col>nodeCount` as the `SP_CURRENT` counterpart); nodeset/IC apply ≡ `cktload.c:108-158`. Comment hygiene clean. **The bug is not in the source.**

### Evidence — the exact-`1` branch is not reached at runtime

`ic-gate.dts` (capacitor + resistor + vsource — isolates the IC mechanism, no BJT confound) is **decisive**. Topology identical (`topology_diff` empty, matrixSize 3=3 — numerical, not structural):

| phase | ngspice | ours |
|---|---|---|
| `dcopInitJct` (`MODETRANOP\|MODEINITJCT`) iter0, C1 IC node | diag=1, RHS=2, V=2.0V, C1.Q=2e-6 (exact pure-voltage pin) | **no captured `dcopInitJct` iteration** (iterationCount 0, outcome `nrFailedRetry`) |
| `tranInit`, C1 node | V=2.0V, Q=2e-6, rhs row=2000 | V≈2.0e12, Q≈2.0e6, rhs row≈2e15 |

ours behaves as if the **1e10 soft-pin path is still active** (1e10·ic·srcFact RHS ÷ near-unity diagonal) — the reconstructed pure-voltage `1·v=value` branch never drives the runtime matrix. `harness_run` passed 0/107. (`nodeset-gate.dts` — BJT bistable — also fails, earliest `state` Q2.VBE step0/iter0, but that confounds FIX-001's BJT floor with the unapplied nodeset; `ic-gate` is the clean signal.)

### Classification

Numerical/runtime: an isomorphic stamping mechanism that does not execute. Likely causes to investigate (harness-first): (a) `zeroNoncurRow` returning `currents=true` for the pure-voltage cap node (a voltage column mis-classified as a branch column → wrong branch taken); (b) the `MODETRANOP|MODEINITJCT` IC-apply pass not running on our side at all for a `.ic` transient-boot (our `dcopInitJct` captured **0** iterations) — an engine init-pass gap upstream of the apply; (c) the `nodesetHandles`/`icHandles` diagonal handle not addressing the same cell `findElement`/`zeroElement` see. **Next probe:** `harness_matrix_diff` on the C1 row at the IC-apply iteration, and confirm whether our transient-init runs a `MODETRANOP|MODEINITJCT` pass for an IC circuit at all.

### Decision needed (user)

Resolve why the reconstructed exact-`1` nodeset/IC constraint does not reach the runtime matrix so the `maths-sparse#recon/nodesetIcRowZero` harness gate (`ic-gate.dts`, `nodeset-gate.dts`) goes null. The source is isomorphic; the gap is in which init-pass runs and which matrix row it writes.

### Re-verification — 2026-06-02 (maths-sparse teardown, escalation [1])

Left PENDING, not committed. SPEC-PRESENCE: PASS (spec/v41-port/reconstruction/maths-sparse-nodeset-ic-rowzero.md exists, 620 lines, RATIFIED 2026-05-30 / REVISED 2026-06-01). SOURCE ISOMORPHISM (a): PASS — re-derived independently vs ref/ngspice. sparse-solver.ts findElement ≡ SMPfindElt(...,0) (spsmp.c:454-471) incl. intCol===-1 guard + read-only invariant (no _insertionOrder push, _spcFindElementInCol createIfMissing=false); zeroElement ≡ *x=0.0 (cktload.c:181, real-part only); size getter; stamp-helpers.ts setRHS ≡ CKTrhs[node]=value assignment; ckt-load.ts zeroNoncurRow ≡ ZeroNoncurRow (cktload.c:167-186) classifying by the authoritative ctx.nodeType(slot) resolver (the CKTnode->type counterpart from _nodeTable, NOT the rejected col>nodeCount proxy); Step 4a/4b nodeset+IC apply ≡ cktload.c:108-158 with absolute zeroElement+stampElement diagonal and setRHS. Comment hygiene clean. FILE-SCOPE: clean. git diff --name-only = ckt-load.ts, sparse-solver.ts, stamp-helpers.ts (named tsFiles) + ckt-context.ts (spec-mandated by the Correction note + Acceptance #3: nodeType resolver + buildNodeTypeTable) + analog-engine.ts (single buildNodeTypeTable(matrixSize,_nodeTable) call at :1592 — structurally forced: nodeType returns the all-voltage default and zeroNoncurRow mis-classifies branch rows without the table populated; minimal-conformance, no new logic; getNodeTable/_nodeTable pre-existed). No over-application. HARNESS GATE (b): FAIL — firstDivergence non-null on BOTH fixtures after server_restart. ic-gate.dts (clean signal, RC+vsource, .ic C1:pos=2V seeded into BOTH engines by the harness): matrix=null (Jacobian matches) but rhs at C1:pos/R1:neg ours ~2e15 vs ngspice 2000 (absΔ~2e15), voltage ours ~2.0e12 vs ngspice 2.0, state C1.Q ours 1.999998e6 vs ngspice 2e-6, shape attemptCount ours 15 vs ngspice 5. Our dcopInitJct (MODETRANOP|MODEINITJCT) captured 0 iterations (ours:null, nrFailedRetry) and fell into dcopGminDynamic where C1:pos=2000 and the IC-node RHS row reads 0 (ngspice writes RHS=2 there); ngspice converges the boot DCOP in dcopInitFloat honoring C1:pos=2, C1.Q=2e-6. nodeset-gate.dts (BJT bistable latch): earliest state Q2.VBE step0/iter0, matrix classification value-only (coord-set identical; device-load conductances differ because the engines sit at different bias points) — NOT a recon defect: the harness ComparisonSession (comparison-session.ts:285-289,633) emits .nodeset ONLY on the ngspice deck and never seeds digiTS ctx.nodesets, so the recon Step 4a loop never iterates and digiTS lands in the opposite latch well; this fixture cannot gate the nodeset path until the harness seeds digiTS nodesets (parallel to how it already seeds ics). CLASSIFICATION/ESCALATION (non-null divergence NOT waved through; banned verdicts avoided): escalated to spec/fix-list-phase-2-audit.md FIX-002 with a 2026-06-02 re-verification appended. The node-type resolver build behaves bit-identically to the prior col>nodeCount build on ic-gate, ELIMINATING cause (a) (voltage/branch mis-classification) and narrowing to cause (b): the MODETRANOP|MODEINITJCT IC-apply pass does not execute on the digiTS side for a .ic transient-boot (our dcopInitJct runs 0 iterations), so the reconstructed exact-1 branch never drives the runtime matrix — the fix is upstream of zeroNoncurRow in the transient-init driver. progress.json contains a STALE prior note (claims spec absent / no edits) that is superseded: spec exists and all 5 files are edited and live in the build. Re-apply requires fixing the transient-init IC-apply pass (cause b) and seeding harness nodesets for nodeset-gate; then re-run the gate.

Net change from the 2026-06-01 entry above: cause (a) (voltage/branch mis-classification in `zeroNoncurRow`) is now ELIMINATED — the new `ctx.nodeType` resolver build is bit-identical to the prior `col>nodeCount` proxy on `ic-gate`. Root cause is narrowed to cause (b): the `MODETRANOP|MODEINITJCT` IC-apply pass does not execute on the digiTS side for a `.ic` transient-boot (our `dcopInitJct` runs 0 iterations), so the reconstructed exact-`1` branch never drives the runtime matrix. The fix is upstream of `zeroNoncurRow` in the transient-init driver. Recon source preserved at `.wt-failed/maths-sparse.diff` (src-only, 319 lines) and `.wt-failed/maths-sparse-full.diff` (338 lines).

### Re-verification — 2026-06-03 (maths-sparse GATE+MERGE+TEARDOWN, serial stage)

Worktree `.wt/maths-sparse` REBASED clean onto the advancing `v41-port` (other units merged since branch); rebase produced ZERO `src/` delta (`git diff v41-port -- src/` empty, kept patch `.wt-failed/maths-sparse.diff` = 0 bytes). This is a progress-only unit whose code already lives on `v41-port` — so the gate here re-measures the CURRENT `v41-port` state of the IC/nodeset path, not new worktree edits. COMPILE GATE PASS: `compile-analog-partition.test.ts` + `compiler.test.ts` = 28/28. HARNESS GATE (after `server_restart` onto the worktree, DLL `Release.x64/ngspice.dll`):

- **`ic-gate.dts` (RC + vsource, `.ic C1:pos=2V` — the clean IC signal) is now FULLY CLEAN.** `harness_run` transient: `firstDivergence` **null across all 8 classes**, 107/107 steps passed, both engines converge every step (avgIter 2.028, maxIter 5, 0 failed). This is a decisive change from the 2026-06-02 entry above, which recorded `ic-gate` grossly failing (RHS ~2e15 vs 2000, V ~2e12 vs 2.0, C1.Q ~2e6 vs 2e-6, our `dcopInitJct` running 0 iterations). **Cause (b) — the `MODETRANOP|MODEINITJCT` IC-apply pass not executing on the digiTS side — is now RESOLVED on `v41-port`:** the reconstructed exact-`1` IC constraint reaches the runtime matrix and the IC transient-boot DCOP converges bit-exact. (Cross-ref the cktic `rhsOld dual-write seed` land, ledger `df59` / task #25.)
- **`nodeset-gate.dts` (BJT bistable latch) still NON-NULL but the divergence has collapsed to sub-ULP and matches the harness-seeding diagnosis, NOT a recon defect.** `harness_run` transient: stepCount **107=107** (exact), convergence identical both sides (avgIter 2.093, maxIter 12, 0 failed). `harness_first_divergence.earliest` = **state** `Q2.VBE` step0/iter0 ours `0.6996637349555478` vs ngspice `0.6996637349555483` absΔ **5.55e-16** (1 ULP); downstream `limiting` `Q2:BE.vAfter` same absΔ; `matrix` diag `(VCC,VCC)` absΔ 2.21e-15; `rhs` `Q1:C` absΔ 6.8e-26; `voltage` `Q1:B` absΔ 8.3e-17. This is the same mechanism FIX-002 already identifies: the harness `ComparisonSession` (comparison-session.ts:285-289,633) emits `.nodeset` ONLY on the ngspice deck and never seeds digiTS `ctx.nodesets`, so the recon Step 4a loop never iterates on the digiTS side. Previously this drove the two engines into OPPOSITE latch wells (gross value-only matrix mismatch); now, with the IC-apply pass fixed and both engines settling to the SAME well, the residual is a 1-ULP arithmetic-order floor on the BJT junction solve (the FIX-001 BJT DC-OP floor showing through on a shared bias point), confounded onto this fixture. **`nodeset-gate.dts` cannot gate the nodeset path until the harness seeds digiTS nodesets in parallel to how it already seeds `.ic`s** (which `ic-gate` confirms now works end-to-end).

GATE VERDICT: gatePass=FALSE (contract requires `firstDivergence` null across ALL classes on ALL fixtures; `nodeset-gate` non-null). Per the gate-fail decision, NOT edited to pass and NOT merged — escalation only. `progress.json` ledger entries for this unit (`#recon/nodesetIcRowZero`, `spfactor.c#h002b`, `spsmp.c#h002`) therefore stay PENDING (NOT flipped to APPLIED). Decision needed (user): seed digiTS `ctx.nodesets` from the harness `ComparisonSession` (mirror the existing `.ic` seeding) so `nodeset-gate.dts` exercises the digiTS Step 4a nodeset loop instead of landing un-seeded; the ic-gate half of this recon is now provably bit-exact and the nodeset half is blocked purely on the harness not feeding digiTS the constraint. Banned closing verdicts avoided; the 1-ULP residual is NOT waved through — it is the FIX-001 BJT floor surfacing on a shared well plus the un-seeded-nodeset confound, both already itemized.

---

## FIX-003 — legacy named-param `square` waveform timestep cadence diverges from ngspice `PULSE` (blocks `vsrc#recon/waveformModel` harness gate)

- **Surfaced by:** RECONSTRUCTION VERIFIER for `vsrc#recon/waveformModel` (2026-06-02, isolated worktree `.wt/vsrc`).
- **digiTS:** `src/components/sources/ac-voltage-source.ts` — `AcVoltageSourceAnalogImpl.acceptStep` legacy `square` SAMETIME branch (the `if (this._waveform === "square")` arm; `_funcTGiven` false on this fixture). This breakpoint-scheduling path is **byte-identical to the pre-recon baseline** (verified by diffing `HEAD:ac-voltage-source.ts` against the worktree — the recon only inserted the `_funcTGiven && _functionType !== null → _acceptNgspice()` short-circuit *ahead* of it; the named-param square arm is untouched). The named-param→`PULSE` deck emitter in `netlist-generator.ts:1164-1192` is likewise unchanged (the recon's new coeff-emit branch is gated on a non-empty `funcType`, absent here).
- **ngspice:** `ref/ngspice/src/spicelib/devices/vsrc/vsrcacct.c:48-139` (`VSRCaccept` PULSE breakpoint scheduler) for the emitted `PULSE(...)` deck.

### Source isomorphism of the recon — PASS

The reconstructed coefficient model is bit-exact against the ngspice baseline, re-derived independently against `ref/ngspice`: `evaluateNgspiceWaveform` (PULSE/SINE/EXP/SFFM/AM/PWL arms) ≡ `vsrcload.c:96-345` incl. the `FREQ*time*2.0*M_PI+phase` operand order (`vsrcload.c:193`), the `order>N && coeffs[N]!=0` default guards (CKTstep/CKTfinalTime), the AM `phases`-twice quirk (`vsrcload.c:294-295`); `_acceptNgspice` ≡ `vsrcacct.c:48-201` PULSE+PWL incl. the `simTime >= _breakTime` gate (`vsrcacct.c:94/162`), the wait-to-next-phase-boundary ladder, and the `_breakTime -= minBreak` back-off (`vsrcacct.c:136/197`); `applyCoeffs` ≡ `copy_coeffs` (`vsrcpar.c:17-29`); `_applyRepeat` ≡ `VSRC_R` (`vsrcpar.c:124-161`); `setup()` `_breakTime=-1.0` seed ≡ `vsrcset.c:34`; the `WaveformStepContext`/`cktStep`/`cktFinalTime` thread ≡ `ckt->CKTstep`/`ckt->CKTfinalTime`; current-source mirror faithful. FILE-SCOPE clean: `git diff --name-only` = the 5 named tsFiles + `ckt-context.ts` (structurally forced — `LoadCtxImpl` implements the `LoadContext` interface that gains `cktStep`/`cktFinalTime`; minimal field declaration + builder initialization, no new logic). Comment hygiene clean. **The bug is not in the recon source.**

### Evidence — the gate

Server restarted onto the `.wt/vsrc` build. DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`. Fixtures `vsrc-ac-sine-rload.dts`, `vsrc-ac-square-rload.dts`.

- **vsrc-ac-sine-rload** — transient `firstDivergence` **null across all 8 classes**; AC sweep `acFirstDivergence` **null** (61/61 points, shape clean). CLEAN.
- **vsrc-ac-square-rload** — AC sweep `acFirstDivergence` **null** (61/61 points). Transient: `firstDivergence` non-null. stepCount ours **107** vs ngspice **229** (`withinTol:false`); both engines converge every step (0 failed). `harness_first_divergence.earliest` = **rhs** at step0/iter0, `V1:branch` ours **5** vs ngspice **-4.9** (absΔ 9.9); `voltage` `R1:pos/V1:pos` same 5 vs -4.9. `harness_get_step index 0`: the DC-OP is correct on both sides (`endBranchNorm:5` both), but the paired `tranNR` compares two engines at **different sim times** — our step 0 ends at t=200ns while ngspice's paired step ends at t≈134ns (ngspice took a 67ns first dt, ours 200ns). The square source value differs only because the two timelines are misaligned: at ngspice's finer grid the source is near a falling/rising edge (-4.9), at our coarser grid it sits on the HIGH plateau (+5).

### Classification

Numerical/structural breakpoint-cadence: the legacy named-param `square` `acceptStep` SAMETIME scheme registers a different breakpoint set than ngspice's `VSRCaccept` PULSE scheduler does for the emitted `PULSE(V1 V2 TD TR TF PW PER)` deck, so the two engines walk different timestep grids (107 vs 229 steps) and the paired-step comparison diverges. The recon left this path byte-identical to baseline, so the divergence is independent of the coefficient-model rebuild — but the contract gate (`firstDivergence` null on ALL classes on ALL fixtures) is a hard requirement and cannot be waved through. The path forward the recon itself enables: the `square` fixture should drive the source through the new coefficient model (`funcType=PULSE` + `coeffs`) so BOTH our `_acceptNgspice` (≡ `vsrcacct.c` PULSE, which IS isomorphic) and ngspice's `VSRCaccept` schedule from the identical coefficient set — eliminating the SAMETIME-vs-PULSE cadence mismatch. Alternatively the legacy `square` SAMETIME `acceptStep` must be reconciled bit-for-bit with `vsrcacct.c:48-139`.

### Decision needed (user)

Either (a) re-author `vsrc-ac-square-rload.dts` to use the SPICE coefficient path (`funcType="PULSE"`, `coeffs="-5 5 <td> <tr> <tf> <pw> <per>"`) so the gate exercises the reconstructed `_acceptNgspice` PULSE scheduler against ngspice's `VSRCaccept` from one shared coefficient set (the recon's stated Part G precondition for a bit-exact waveform gate), or (b) treat the legacy named-param `square` SAMETIME `acceptStep` as a separate numerical fix and reconcile it to `vsrcacct.c:48-139`. The recon's coefficient-model source is isomorphic and the sine fixture is fully clean; the square fixture's divergence is in the untouched legacy breakpoint scheduler that the named-param fixture routes through.

### Teardown confirmation — 2026-06-02 (vsrc teardown, escalation [1])

`vsrc#recon/waveformModel` left PENDING (no commit, `progress.json` untouched). SPEC-PRESENCE PASS (`spec/v41-port/reconstruction/vsrc-waveformModel.md` exists, RATIFIED 2026-05-30 / REVISED 2026-05-31). EMPTY-DIFF PASS. FILE-SCOPE PASS: `git diff --name-only` = `ac-voltage-source.ts`, `ac-current-source.ts`, `load-context.ts`, `analog-engine.ts`, `netlist-generator.ts` (5 named tsFiles) + `ckt-context.ts` (structurally forced — `LoadCtxImpl` implements the `LoadContext` interface the recon extends with `cktStep`/`cktFinalTime` per the Part A precondition correction; the impl class must declare+initialize those fields or it won't type-check; minimal conformance — field decl + builder copy of `params.outputStep`/`params.tStop` plus the same re-read in `configure()` for hot-loadability, no new logic). SOURCE ISOMORPHISM PASS (re-derived independently vs `ref/ngspice`): `evaluateNgspiceWaveform` PULSE/SINE/EXP/SFFM/AM/PWL arms bit-exact vs `vsrcload.c:96-345` incl. `FREQ*time*2.0*M_PI+phase` operand order (`vsrcload.c:193`), `order>N && coeffs[N]!=0` `CKTstep`/`CKTfinalTime` default guards, AM `phases`-twice quirk (`vsrcload.c:294-295`), PWL repeat/interp (`vsrcload.c:300-345`); `_acceptNgspice` PULSE+PWL bit-exact vs `vsrcacct.c:48-201` incl. `simTime>=_breakTime` gate (`vsrcacct.c:94/162`), wait-ladder, `_breakTime-=minBreak` back-off (`vsrcacct.c:136/197`), no-back-off on the no-repeat-past-end PWL arm (`vsrcacct.c:181`); `applyCoeffs` ≡ `copy_coeffs` (`vsrcpar.c:17-29`); `_applyRepeat` ≡ `VSRC_R` (`vsrcpar.c:124-161`); `setup()` `_breakTime=-1.0` ≡ `vsrcset.c:34`; `FunctionType` enum PULSE=1..TRRANDOM=8, EXTERNAL=9 ≡ `vsrcdefs.h:131-145` (PORT absent); TRNOISE/TRRANDOM correctly throw blocked-on `maths-misc#recon/randnumb` (not present in worktree); current-source mirror faithful; Part G generator coeff-emit branch present. Comment hygiene clean. HARNESS GATE FAIL after `server_restart`: `vsrc-ac-sine-rload.dts` transient `firstDivergence` NULL all 8 classes + AC `acFirstDivergence` NULL (61/61 pts) = CLEAN; `vsrc-ac-square-rload.dts` transient `firstDivergence` NON-NULL: stepCount ours 107 vs ngspice 229 (`withinTol:false`, both converge 0-fail), earliest=rhs step0/iter0 `V1:branch` ours 5 vs ngspice -4.9 (absΔ 9.9), voltage `R1:pos` same; `harness_get_step` idx0 DC-OP correct both sides (`endBranchNorm:5` both); divergence is a timestep-cadence mismatch (our step0 ends t=200ns, ngspice paired step t≈134ns) so the paired tranNR samples the square at different sim times (our coarse grid HIGH=+5, ngspice fine grid edge=-4.9); AC on square NULL. Root cause confirmed unchanged from the entry above: the legacy named-param square SAMETIME `acceptStep` (`_funcTGiven`-false arm, which this fixture routes through having no funcType/coeffs) registers a different breakpoint set than ngspice `VSRCaccept` PULSE for the emitted PULSE deck; this path is BYTE-IDENTICAL to the pre-recon baseline (diffing `HEAD:ac-voltage-source.ts` vs worktree confirms the recon only inserted the coeff-path short-circuit ahead of it; the square SAMETIME arm and the `netlist-generator.ts` square→PULSE emitter are untouched), so the divergence is independent of the coefficient-model rebuild and is pre-existing. Per contract, non-null `firstDivergence` may NOT be waved through — escalation stands. Recon source preserved at `.wt-failed/vsrc.diff` (src-only) and `.wt-failed/vsrc-full.diff` (full). Decision needed (user): unchanged — either (a) re-author `vsrc-ac-square-rload.dts` onto the SPICE coeff path so both engines schedule from one shared coefficient set via the isomorphic `_acceptNgspice` PULSE scheduler, or (b) reconcile the legacy square SAMETIME `acceptStep` bit-for-bit to `vsrcacct.c:48-139`. Key files: `src/components/sources/ac-voltage-source.ts` (acceptStep square arm ~1496-1545; `_acceptNgspice` ~1355-1456; `_evaluate` ~1233-1251), `src/solver/analog/__tests__/harness/netlist-generator.ts` (square→PULSE emit 1164-1192).

### Update 2026-06-02 — re-expression landed; SAMETIME diagnosis RESOLVED, root cause refined to the transient firsttime delta

Criterion #11 is now built and `vsrc#recon/waveformModel` is APPLIED (inline land). `square`/`triangle`/`sawtooth` route through `_deriveEnumCoeffs` → `applyCoeffs(PULSE, coeffs)`, which sets `_funcTGiven=true` + `_functionType=PULSE` — VERIFIED by reading the routing: both `_evaluate` (`ac-voltage-source.ts:1271`) and `acceptStep` (`:1508`) gate on `_funcTGiven && _functionType !== null`, so square now both evaluates AND schedules breakpoints via the isomorphic `_acceptNgspice` (≡ `vsrcacct.c` PULSE). The legacy SAMETIME arm (`:1525`) is bypassed for square. **The original SAMETIME-breakpoint root cause is therefore RESOLVED** — the breakpoint sets match ngspice; the sine fixture gates null across all classes.

**Refined remaining root cause (RECLASSIFIED — transient timestep controller, not the vsrc waveform model):** the residual square divergence is the FIRSTTIME delta (the first step taken before any breakpoint). digiTS `timestep.ts:218-221` uses an explicit `params.firstStep`; ngspice `dctran.c:118` computes `MIN(CKTfinalTime/100, CKTstep)/10` then applies the at-breakpoint cuts (`dctran.c:552-560`, `/=10` firsttime). They disagree → digiTS first dt ≈ 2e-7 vs ngspice ≈ 1e-14, so the paired tranNR samples the *correctly-evaluated* PULSE at misaligned sim times (HIGH plateau +5 vs rising edge −4.9). The 1e-12 rise edge is sub-`CKTminBreak`, skipped by BOTH sides (`cktsetbk.c:55-61`), so this is NOT a breakpoint-set mismatch. It affects ANY transient with a sharp transition inside the divergent first step; the bootstrap device fixtures have no such edge (hence they gate null). **This no longer blocks `vsrc#recon/waveformModel` (APPLIED) — it is a standalone timestep-controller parity item.** Fix locus: reconcile `timestep.ts` firsttime/`firstStep` to `dctran.c:118`+`552-560`, then re-validate parity across ALL transient fixtures (the controller touches every transient). Unit-test fallout: `ac-voltage-source.test.ts` square/triangle + `hotload_square` assert the pre-#11 +5 start — update to the ngspice-PULSE behavior.

---

## FIX-004 — AcCurrentSource AC RHS stamp sign/orientation flip vs ngspice `ISRCacLoad` (blocks `isrc#recon/parallelMultiplier` harness gate)

- **Surfaced by:** RECONSTRUCTION VERIFIER for `isrc#recon/parallelMultiplier` (2026-06-02, isolated worktree `.wt/isrc`).
- **digiTS:** `src/components/sources/ac-current-source.ts` — `AcCurrentSourceAnalogImpl.stampAc` (the four `rhsRe[nodePos]+=`, `rhsRe[nodeNeg]-=`, `rhsIm[nodePos]+=`, `rhsIm[nodeNeg]-=` stamps at lines 439-442). This AC stamp is **byte-identical to the pre-recon baseline when `m` is unset**: the recon only inserted the `this._M *` factor, and the gate fixture `isrc-ac-rload.dts` netlists no `m`, so `_M = 1` and the stamped values equal the pre-recon `± acReal` / `± acImag`. The sign/orientation divergence is therefore independent of (not introduced by) the `m` multiplier recon.
- **ngspice:** `ref/ngspice/src/spicelib/devices/isrc/isrcacld.c:41-50` (`ISRCacLoad`): `CKTrhs[posNode]+=m*acReal; CKTrhs[negNode]-=m*acReal; CKTirhs[posNode]+=m*acImag; CKTirhs[negNode]-=m*acImag`.

### Source isomorphism of the recon — PASS

Re-derived independently against `ref/ngspice`: the `m` parallel-multiplier rebuild is bit-exact against the v26 baseline on both elements. `isrcload.c:45` `m = here->ISRCmValue` → constructor `_M`/`_p.m` read with the `isrctemp.c:62-63` `!ISRCmGiven ⇒ 1` default realized via the paramDefs `default:1`; `isrcload.c:387-388` `*CKTrhs[pos]+=m*value; *CKTrhs[neg]-=m*value` → `stampRHS(ctx.rhs,nodePos,_M*I); stampRHS(ctx.rhs,nodeNeg,-_M*I)` (AC `ac-current-source.ts:412-413`, DC `dc-current-source.ts:208-209`, with `I` already carrying the `isrcload.c:382-383` srcFact ramp — operand order `m*(value·srcFact)` matched); `isrcload.c:392` `ISRCcurrent=m*value` → `getPinCurrents` `*this._M`/`*this._p.m`; `isrcacld.c:41-50` `m*acReal`/`m*acImag` → `stampAc` `_M*acReal`/`_M*acImag`. WHERE-match confirmed: `m` is applied at the stamp sites, not folded at param time (matches `isrcpar.c:48-51` which stores `m` raw, unlike `vccspar.c:27-28`). Hot-loadable: AC `setParam` mirrors `_M = _p.m` (`ac-current-source.ts:379`); DC `setParam` writes `_p[key]`. Comment hygiene clean (citations present-tense, no era tags). FILE-SCOPE clean: `git diff --name-only` = exactly the 2 named tsFiles (`ac-current-source.ts`, `dc-current-source.ts`); no over-application. **The bug is not in the recon source.**

### Evidence — the gate

Server restarted onto the `.wt/isrc` build. DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`. Fixtures `isrc-dc-rload.dts`, `isrc-ac-rload.dts`.

- **isrc-dc-rload** — `harness_run` (DC-OP + transient): 107/107 steps converged both engines, stepCount delta 0, avgIterations 2.028 both. `harness_first_divergence` **null across all 8 classes** (`earliest` null). CLEAN.
- **isrc-ac-rload** — `harness_run_ac`: shape clean (61/61 points both sides, no freq deltas). `harness_first_divergence` `acFirstDivergence` **NON-NULL**, a pure `±` sign flip at point 0 (1 Hz):

| class | cell | ours | ngspice | absDelta | relDelta |
|---|---|---|---|---|---|
| solution | row 1 | re:-1000, im:0 | re:+1000, im:0 | 2000 | 2 |
| rhs | row 0 | re:-1, im:0 | re:+1, im:0 | 2 | 2 |

The relDelta is exactly `2` (i.e. `+x` vs `-x`) — a clean sign inversion, not arithmetic-order noise. Topology: I1 pins `[gnd(neg), node1(pos)]`, R1 = 1 kΩ to node1. ngspice `ISRCacLoad` stamps `+m·acReal` into the **posNode** row (acReal = acMag·cos(0) = 1) producing the +1000 V node-1 solution; digiTS's `stampAc` produces the **negated** node-1 solution (-1000 V). The flip is in the mapping/sign of `pinNodes.get("pos")`/`get("neg")` relative to ngspice's `ISRCposNode`/`ISRCnegNode` for the auto-generated AC deck, isolated to the AC small-signal RHS path (the transient/DC path, which uses the same `pos`/`neg` map but is exercised by `load()`/`stampRHS`, is fully clean — so the divergence is specific to `stampAc`, not the pin map itself).

### Classification

Numerical/structural sign divergence in the AcCurrentSource AC small-signal RHS stamp (`stampAc`). The transient/DC `load()` path through the identical `pinNodes` map gates clean, so this is not a global pin-orientation fault; it is specific to the AC excitation stamp's sign convention vs `isrcacld.c:43-50`. The recon left `stampAc` byte-identical to baseline (modulo the `m=1`-inert `_M*` factor), so the divergence is independent of the `m`-multiplier rebuild and is pre-existing — but the contract gate (`firstDivergence`/`acFirstDivergence` null on ALL classes on ALL fixtures) is a hard requirement and may NOT be waved through ("pre-existing"/"tolerance" are banned closing verdicts).

### Decision needed (user)

Locate and close the `stampAc` AC RHS sign divergence vs `isrcacld.c:43-50` so the `isrc#recon/parallelMultiplier` AC gate goes null. The two candidate root causes (harness-first to confirm): (a) the AcCurrentSource `stampAc` applies the `+`/`-` to the wrong terminal relative to ngspice's `ISRCposNode`/`ISRCnegNode` for the generated deck (a `pos`/`neg`-swap specific to the AC path), or (b) the AC harness/deck emission assigns the current-source terminals in the opposite order to the transient path. The recon's `m`-multiplier source is isomorphic and the DC/transient fixture is fully clean; the AC fixture's sign flip is in the AC stamp the recon multiplied (by an inert `m=1`) but did not author. `isrc#recon/parallelMultiplier` left PENDING (no commit; `progress.json` untouched). `isrc#recon/coeffWaveforms` is independently a MISMATCH (entirely unimplemented — see verifier report; its shared-engine dependency `vsrc#recon/waveformModel` has not landed either).

### Teardown confirmation — 2026-06-02 (isrc teardown, escalation [2])

`isrc#recon/parallelMultiplier` left PENDING (no commit, `progress.json` untouched). SPEC-PRESENCE PASS (`spec/v41-port/reconstruction/isrc-parallelMultiplier.md` exists, RATIFIED 2026-05-30). EMPTY-DIFF PASS (both tsFiles landed). FILE-SCOPE PASS (`git diff --name-only` = exactly `ac-current-source.ts` + `dc-current-source.ts`; no over-application). SOURCE ISOMORPHISM PASS, re-derived independently vs `ref/ngspice`: `isrcload.c:45` `m=ISRCmValue` -> constructor `_M`/`_p.m` with `isrctemp.c:62-63` `!ISRCmGiven=>1` default via paramDefs `default:1`; `isrcload.c:387-388` `m*value` RHS -> `stampRHS(_M*I)`/`(-_M*I)` (ac:412-413, dc:208-209), `I` already srcFact-ramped per `isrcload.c:382-383` so operand order `m*(value·srcFact)` matches; `isrcload.c:392` `ISRCcurrent=m*value` -> `getPinCurrents *_M`/`*_p.m`; `isrcacld.c:41-50` `m*acReal`/`m*acImag` -> `stampAc *_M` (ac:439-442); WHERE-match confirmed (`m` at stamp, not folded at param time, matching `isrcpar.c:48-51` raw store vs `vccspar.c:27-28`); hot-loadable (ac setParam mirrors `_M=_p.m` line 379, dc setParam writes `_p[key]`); comment hygiene clean. HARNESS GATE FAIL after `server_restart`. `isrc-dc-rload.dts`: DC-OP+transient 107/107 converged both, firstDivergence NULL all 8 classes = CLEAN. `isrc-ac-rload.dts`: `harness_run_ac` shape clean 61/61 pts, but `acFirstDivergence` NON-NULL — pure ± sign flip at point0/1Hz: solution row1 ours re:-1000 vs ngspice re:+1000 (relDelta 2), rhs row0 ours re:-1 vs ngspice re:+1 (relDelta 2). relDelta exactly 2 = clean sign inversion, NOT arithmetic-order noise. Root: `AcCurrentSource.stampAc` applies +/- to opposite terminal vs ngspice `ISRCposNode`/`ISRCnegNode` for the auto-gen AC deck (`isrcacld.c:43-50`). The transient/DC `load()` path through the SAME pinNodes map gates clean, so it is specific to the AC stamp sign convention, not a global pin-orientation fault. With `m` unset on this fixture `_M=1` so `stampAc` is byte-identical to the pre-recon baseline (recon only inserted the inert `_M*` factor) — divergence is independent of / not introduced by the `m`-multiplier recon, but per contract non-null `acFirstDivergence` may NOT be waved through (banned: pre-existing/tolerance). Decision needed (user): unchanged — locate and close the `stampAc` AC RHS sign divergence vs `isrcacld.c:43-50` so the AC gate goes null. Recon source preserved at `.wt-failed/isrc.diff` (src-only, 183 lines).

### GATE re-run — 2026-06-02 (isrc serial GATE+MERGE+TEARDOWN, post-rebase onto advancing v41-port)

`wt/isrc` REBASED onto the advancing `v41-port` (3 commits replayed, NO conflicts — `rebaseClean=true`; other units had merged since this worktree branched). COMPILE GATE PASS: `compile-analog-partition.test.ts` + `compiler.test.ts` = 28 passed / 0 failed post-rebase, so the worktree compiles cleanly on top of the advanced base. HARNESS GATE re-run after `server_restart` onto the rebased `.wt/isrc` build (DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`): `isrc-dc-rload.dts` DC-OP+transient 107/107 converged both engines, `firstDivergence` NULL all 8 classes = CLEAN; `isrc-ac-rload.dts` `harness_run_ac` shape clean 61/61 pts but `acFirstDivergence` NON-NULL, reproducing the FIX-004 signature BIT-FOR-BIT after the rebase: `rhs` point0/1Hz row0 ours `re:-1,im:0` vs ngspice `re:+1,im:0` (absΔ 2, relΔ 2); `solution` row1 ours `re:1000,im:0` vs ngspice `re:-1000,im:0` (absΔ 2000, relΔ 2); `matrix`/`shape` null. relΔ exactly 2 = clean `±` sign inversion, not arithmetic-order noise. The rebase onto the advancing base did NOT change the divergence — confirming it is the same untouched `stampAc` AC RHS sign/orientation flip vs `isrcacld.c:43-50`, not anything introduced by the units that merged ahead. `gatePass=false`, so MERGE skipped (`merged=false`); `wt/isrc` left unmerged. MAIN `src/` confirmed clean (no leaked edit; `mainSrcClean=true`). Kept work re-preserved at `.wt-failed/isrc.diff` (`git diff v41-port -- src/`, 22492 bytes) for resume-from-patch. Decision needed (user): unchanged — locate and close the `stampAc` AC RHS sign divergence vs `isrcacld.c:43-50` so the `isrc` AC gate goes null; until then `isrc` cannot ff-merge to `v41-port`.

### RESOLVED — 2026-06-02 (harness generator node-order fix; root cause (b) confirmed)

Confirmed as candidate **(b)** from the Decision-needed above: the harness netlist-generator emitted the AcCurrentSource deck with n+/n- SWAPPED relative to DcCurrentSource. `netlist-generator.ts` DcCurrentSource (line 618) emits `n+ = pin 0, n- = pin 1`; AcCurrentSource (lines 626-627) emitted `n+ = pin 1, n- = pin 0` (it had copied the voltage-source node order). digiTS's `AcCurrentSourceAnalogImpl` stamps `+` at the "pos" pin in BOTH `load()` (581-582) and `stampAc` (609-612), and the DC current-source deck (pin 0 = n+) gates clean — so the AC deck's swapped order was the sole cause of the ± flip. **digiTS's `stampAc` is correct and matches `isrcacld.c:43-50`; the bug was in the test-deck generator, not the engine or the recon source.** Fix: AcCurrentSource now uses `n+ = pin 0, n- = pin 1` (the DcCurrentSource order). Re-gated after `server_restart`: `isrc-ac-rload.dts` `harness_run_ac` → `acFirstDivergence` NULL across all classes (`earliestPointIndex` null). FIX-004 CLOSED. `isrc#recon/parallelMultiplier`'s AC gate now passes — the recon (isomorphic, DC clean) merges once built.

---

## FIX-007 — transient timestep controller cuts dt later than ngspice on RC discharge (blocks `analysis` recon harness gate)

- **Surfaced by:** GATE+MERGE+TEARDOWN for unit `analysis` (2026-06-03, isolated worktree `.wt/analysis`, serial stage post-rebase onto the advancing `v41-port` — 0 conflicts, `rebaseClean=true`; COMPILE GATE 28/28 pass).
- **digiTS:** `src/solver/analog/timestep.ts` — the transient timestep / LTE controller that selects the next `delta`. On the gate fixture it holds `dt=50us` (the `maxStep` cap, `stopTime/100`) across the discharge, where ngspice's LTE controller cuts `dt` to ~21us at ~2.98ms.
- **ngspice:** `ref/ngspice/src/spicelib/analysis/dctran.c` (the `CKTtrunc` / `DEVtrunc` next-`delta` selection and the `delta = MIN(delta, ...)` / LTE-acceptance loop), feeding the per-device `CAPtrunc` truncation-error step for `C1`.

### Evidence — the gate

Server restarted onto the `.wt/analysis` build. DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`. Gate fixtures `resistive-divider.dts`, `rc-transient.dts`, `inductor-rl-step.dts`, `rc-lowpass-ac.dts`.

- **resistive-divider.dts** — `harness_run` (DC-OP + transient): 107/107 steps converged both engines, stepCount delta 0. `harness_first_divergence` **null across all 8 classes** (`earliest` null). CLEAN.
- **inductor-rl-step.dts** — `harness_run`: 107/107 converged both, stepCount delta 0. `harness_first_divergence` **null across all 8 classes**. CLEAN.
- **rc-lowpass-ac.dts** — `harness_run_ac` (1 Hz–1 MHz, dec/10): shape clean (61/61 points both sides, no freq deltas). `harness_first_divergence` `acFirstDivergence` **null** (solution/shape/matrix/rhs all null). CLEAN.
- **rc-transient.dts** — `harness_run` (stopTime 5ms): both engines converge every step (0 failed), but stepCount ours **173** vs ngspice **219** (`withinTol:false`, relDelta 21%); `harness_first_divergence` **NON-NULL**. `earliest` = **integration** at step132/iter0: `delta` ours **5.0e-5** vs ngspice **1.6113e-5** (absΔ 3.389e-5). Downstream at the SAME (132,0): `matrix` diag `(R1:pos/V1:pos,R1:pos/V1:pos)` ours 0.041 vs ngspice 0.1251 (the companion `ag0≈1/dt` differs because `dt` differs); `rhs` `C1:pos/R1:neg` ours 0.02881 vs ngspice 0.08880; `voltage` `C1:pos/R1:neg` ours 0.70273 vs ngspice 0.71772 (absΔ 0.01498); `state` `C1.Q` ours 7.0273e-7 vs ngspice 7.1772e-7.

`harness_get_step`: through step **131** the timelines are BIT-IDENTICAL (`dt`=50us both, `stepStartTime`/`stepEndTime` absΔ 0, `divergenceNorm` 0). At step **132** they split: `ourStepIndex 132 ↔ ngspiceStepIndex 155` — ngspice has already taken 23 extra steps via finer timestepping. Our `dt` stays 50us (the `maxStep` cap); ngspice's `dt` is 20.97us (relΔ 58%). The matrix/rhs/voltage/state cells differ purely as the downstream image of the differing `delta` (companion conductance `ag0=1/dt`), NOT a bit-identical-matrix load/accumulation-order divergence — the Jacobian differs only because the integration `delta` chosen diverged.

### Classification

Numerical transient timestep-controller (LTE / truncation-error) parity divergence: at ~2.98ms on the RC discharge ngspice's `CKTtrunc`/`CAPtrunc` step-control reduces `dt` below the `maxStep` cap while digiTS holds the cap, so the two engines walk different timestep grids (173 vs 219 steps) from step 132 onward and the paired-step comparison diverges. This is the same standalone timestep-controller domain already flagged in FIX-003's 2026-06-02 update ("reconcile `timestep.ts` firsttime/`firstStep` to `dctran.c:118`+`552-560`, then re-validate parity across ALL transient fixtures — the controller touches every transient") and overlaps the pending FIX-005 ("square late-run edge drift at stopTimes >~2ms"); here the divergence is the mid-run LTE-driven dt-cut selection rather than the firsttime delta. Both onset ~2.98ms+. The `analysis` recon left the controller's truncation-error path as-is; the contract gate (`firstDivergence` null on ALL classes on ALL fixtures) is a hard requirement and may NOT be waved through ("tolerance"/"pre-existing" are banned closing verdicts), so `analysis` cannot ff-merge to `v41-port` until this is closed.

### Decision needed (user)

Reconcile `src/solver/analog/timestep.ts`'s next-`delta` / LTE truncation-error selection bit-for-bit to `dctran.c`'s `CKTtrunc`/`DEVtrunc` loop (and `CAPtrunc` for the capacitor) so `rc-transient.dts` walks the identical 219-step grid as ngspice and the `analysis` harness gate goes null across all classes on all four fixtures. The other three gate fixtures (resistive-divider, inductor-rl-step, rc-lowpass-ac) are fully clean; only the RC-discharge transient exercises the divergent LTE dt-cut. This is the same controller item FIX-003/FIX-005 point at; resolving it should be cross-validated against ALL transient fixtures since the controller touches every transient. `analysis` unit left UNMERGED (no commit; `progress.json` untouched). Recon source preserved at `.wt-failed/analysis.diff` (`git diff v41-port -- src/`).

## FIX-008 — square-wave transient timestep grid diverges from ngspice past ~3ms, source sampled on wrong half-cycle (blocks `vsrc#recon/waveformModel` harness gate)

- **Surfaced by:** GATE+MERGE+TEARDOWN for unit `vsrc` (2026-06-03, isolated worktree `.wt/vsrc`, serial stage post-rebase onto the advancing `v41-port` — 0 conflicts, `rebaseClean=true`; COMPILE GATE 28/28 pass). The `vsrc` worktree carried NO src changes vs `v41-port` (`committedAnything=false`, iso/progress-only unit; `git diff v41-port -- src/` empty), so this divergence is in the inherited transient timestep controller, not in any vsrc recon code.
- **digiTS:** `src/solver/analog/timestep.ts` — the transient timestep / LTE controller selecting the next `delta`. On the square gate fixture it holds `dt=50us` (the `maxStep` cap, `stopTime/100`) where ngspice cuts `dt` to ~14us around the square edge near ~3ms.
- **ngspice:** `ref/ngspice/src/spicelib/analysis/dctran.c` (the `CKTtrunc` / `DEVtrunc` next-`delta` selection and the breakpoint-aware `delta = MIN(delta, ...)` loop that snaps `dt` to the next `PULSE`/`square` edge time).

### Evidence — the gate

Server restarted onto the `.wt/vsrc` build. DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`. Gate fixtures `vsrc-ac-square-rload.dts`, `vsrc-ac-sine-rload.dts` (both `V1` AC source + `R1` load to gnd; matrixSize 1, single node `R1:pos/V1:pos`). Both fixtures already tracked in the worktree (vsrc is not a from-scratch class; no authoring needed).

- **vsrc-ac-sine-rload.dts** — `harness_run` (DC-OP + transient, stopTime 5ms): 107/107 steps converged both engines, stepCount delta 0 (`withinTol:true`), 107/107 passed. `harness_first_divergence` **null across all 8 classes** (`earliest` null). `harness_run_ac` (1 Hz–1 MHz, dec/10): shape clean (61/61 points both sides, no freq deltas); `acFirstDivergence` **null** (solution/shape/matrix/rhs all null). CLEAN on both transient and AC.
- **vsrc-ac-square-rload.dts** — `harness_run_ac` (1 Hz–1 MHz, dec/10): shape clean (61/61 both, no freq deltas); `acFirstDivergence` **null**. AC CLEAN. BUT `harness_run` (stopTime 5ms): both engines converge every step (0 failed), stepCount ours **203** vs ngspice **434** (`withinTol:false`, relDelta 53%); `harness_first_divergence` **NON-NULL**. `earliest` = **integration** at step132/iter0: `delta` ours **5.0e-5** vs ngspice **1.4101e-5** (absΔ 3.590e-5). Downstream at the SAME (132,0): `voltage` `R1:pos/V1:pos` ours **-5** vs ngspice **+5** (absΔ **10**) and `rhs` `V1:branch` ours **-5** vs ngspice **+5** (absΔ 10) — the source is evaluated on the OPPOSITE half-cycle of the square wave because our coarser `dt` straddled the edge instead of landing on it. `matrix`/`state`/`limiting`/`convergence`/`shape` all null.

The matrix is null (single-node resistive load, the Jacobian cell is `dt`-independent here), so this is NOT a bit-identical-matrix load/accumulation-order divergence — the divergence is purely the integration `delta` chosen at the square edge, which then samples the source excitation on the wrong half-cycle.

### Classification

Numerical transient timestep-controller (breakpoint / LTE) parity divergence: near the square-wave edge at ~3ms (step 132 of the ours-trace) ngspice's `dctran.c` step control cuts `dt` to ~14us to land on the edge breakpoint while digiTS holds the `maxStep` cap (50us), so the two engines walk different timestep grids (203 vs 434 steps) from step 132 onward and the source is read on opposite half-cycles (ours -5V vs ngspice +5V). This is the same standalone timestep-controller domain already flagged in FIX-003 ("legacy named-param `square` waveform timestep cadence diverges from ngspice `PULSE`"), the pending FIX-005 ("square late-run edge drift at stopTimes >~2ms"), and FIX-007 (RC-discharge mid-run LTE dt-cut). All onset ~2.98–3.0ms+. The `vsrc` unit carried no controller code (empty src diff), so it cannot fix this; the contract gate (`firstDivergence` null on ALL classes on ALL fixtures) is a hard requirement and may NOT be waved through ("tolerance"/"pre-existing" are banned closing verdicts), so `vsrc` cannot ff-merge to `v41-port` until this is closed.

### Decision needed (user)

Reconcile `src/solver/analog/timestep.ts`'s next-`delta` / breakpoint snapping bit-for-bit to `dctran.c`'s `CKTtrunc`/`DEVtrunc` loop plus the `PULSE`/`square` edge-breakpoint enforcement, so `vsrc-ac-square-rload.dts` walks the identical 434-step grid as ngspice (landing exactly on each square edge) and the `vsrc` harness gate goes null across all classes on the square fixture. The sine fixture is fully clean (transient + AC) and the square fixture's AC is clean; only the square transient exercises the divergent edge dt-selection. This is the same controller item FIX-003/FIX-005/FIX-007 point at; resolving it should be cross-validated against ALL transient fixtures since the controller touches every transient. `vsrc` unit left UNMERGED (no commit reached `v41-port`; `progress.json` `vsrc#recon/waveformModel` NOT set APPLIED). The worktree src diff vs `v41-port` was empty so `.wt-failed/vsrc.diff` is 0 bytes (no recon work to resume — this is purely an inherited-controller gate failure).

## FIX-009 — diode-bridge step0/iter0 divergence: VSRC branch-coupling coord-set-differs + capacitor state-seed mismatch (blocks `dio` device-class harness gate)

- **Surfaced by:** GATE+MERGE+TEARDOWN for unit `dio` (2026-06-03, isolated worktree `.wt/dio`, serial stage post-rebase onto the advancing `v41-port` — 13 commits replayed, 0 conflicts, `rebaseClean=true`; COMPILE GATE 28/28 pass). `dio` carries real recon src work (`.wt-failed/dio.diff` = 103682 bytes, `git diff v41-port -- src/`).
- **digiTS:** (a) VSRC analog stamp/setup — the branch-coupling MNA cells for V1 (the four `pos↔branch` / `neg↔branch` allocations). At the V1:neg node our coord-set holds `(D4:K/D2:A/V1:neg, V1:branch)` (the `negNode×branch` / `VSRCnegIbrPtr` cell) but is MISSING the transpose `(V1:branch, D4:K/D2:A/V1:neg)` (`branch×negNode` / `VSRCibrNegPtr`), while ngspice has the transpose and (at the harness reference iter) not the forward — i.e. the two coord-sets are each other's transpose at this one coupling. (b) the capacitor (`C_filter`) transient initial state seed in `src/components/passives/capacitor*` — `C_filter.Q` state0 ours **-5.66e-42** vs ngspice **-1.46e-32** at step0/iter0 (state2 slot ours -1.46e-32 ≈ ngspice's state0, suggesting a one-vintage-shifted / differently-rounded Q seed at tranInit).
- **ngspice:** `ref/ngspice/src/spicelib/devices/vsrc/vsrcset.c:88-91` (`VSRCsetup` non-RF branch): `TSTALLOC(VSRCposIbrPtr, VSRCposNode, VSRCbranch); TSTALLOC(VSRCnegIbrPtr, VSRCnegNode, VSRCbranch); TSTALLOC(VSRCibrNegPtr, VSRCbranch, VSRCnegNode); TSTALLOC(VSRCibrPosPtr, VSRCbranch, VSRCposNode)` — ngspice allocates ALL FOUR cells (both the `node×branch` pair AND the `branch×node` pair). The capacitor seed: `ref/ngspice/src/spicelib/devices/cap/captran`-path Q integration at `MODEINITTRAN`.

### Evidence — the gate

Server restarted onto the `.wt/dio` build. DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`. Gate fixtures `diode-resistor.dts`, `diode-bridge.dts` (both already tracked in the worktree — `dio` reuses the existing ngspice-parity fixtures, no authoring needed).

- **diode-resistor.dts** — `harness_run` (DC-OP + transient): 107/107 steps converged both engines, stepCount delta 0 (`withinTol:true`), 107/107 passed. `harness_first_divergence` **null across all 8 classes** (`earliest` null). CLEAN.
- **diode-bridge.dts** — `harness_run` (transient): both engines converge every step (107/107, 0 failed, stepCount delta 0, avgIterations 2.084 both), BUT `harness_first_divergence` **NON-NULL** at **step0/iter0** (phase `tranInit`). Classes:
  - `earliest` = **state**: `C_filter.Q` ours **-5.657066563332259e-42** vs ngspice **-1.4646757140241192e-32** (absΔ 1.46e-32).
  - **matrix** (`harness_matrix_diff`, classification **`coord-set-differs`**, 12 cells each side, `valueMismatches` EMPTY): `oursOnly` = `(row2,col3)=(D4:K/D2:A/V1:neg, V1:branch)` ours 0 / ngspice null; `ngspiceOnly` = `(row3,col2)=(V1:branch, D4:K/D2:A/V1:neg)` ours null / ngspice 0. Transpose pair at the V1:neg↔branch coupling.
  - **voltage** `C_filter:pos/D2:K/D1:K` ours 5.31e-30 vs ngspice -1.41e-28 (absΔ 1.46e-28); **rhs** same node ours -5.31e-25 vs ngspice -1.52e-23 (absΔ 1.46e-23); **limiting** `D4:AK.vAfter` ours 1.8849555921554255e-7 vs ngspice 1.8849555921538315e-7 (absΔ 1.59e-19). `integration`/`convergence`/`shape` null.

`harness_topology_diff`: elementDiffs/orderingDiffs/unmappedNgspiceNodes/structuralFindings ALL EMPTY; ourMatrixSize 4 = ngspiceMatrixSize 4; 7 elements / 3 nodes both sides. `harness_get_attempt` (step0, `tranInit`, slice on V1, iter0): the dense 3×3 node+branch slice matrix is **BIT-IDENTICAL** both sides (`[0,100000.001,-1.387e-12, 0,0,-1.387e-12, 0,1,0]`), as are `ag`/method/order/delta(1e-9)/initMode(`MODETRAN|MODEINITTRAN`). The rhs differs (ours `[4.36e-31,-4.42e-31,-4.42e-31]` vs ngspice `[-1.46e-23,0,0]`) and ngspice's `residualInfinityNorm` is 1.46e-31 (ours 0); both at the machine-zero floor, seeded by the divergent `C_filter.Q`.

### Classification

Two coupled step0/iter0 divergences, NOT a timestep-controller item (distinct from FIX-005/007/008 which all onset ~3ms; this is at t=0):

1. **VSRC branch-coupling coord-set-differs (structural / setup TSTALLOC).** Per CLAUDE.md "Sparse Solver is Settled" the divergence over a bit-identical dense matrix is upstream in per-device `setup()` TSTALLOC sequence. `vsrcset.c:88-91` allocates BOTH `VSRCnegIbrPtr (negNode,branch)` AND `VSRCibrNegPtr (branch,negNode)`; our VSRC setup appears to allocate only one orientation of the neg↔branch pair (the harness CSC element-pool dump shows ours with `(negNode,branch)` and ngspice with `(branch,negNode)` at the reference iter). Because the dense matrix is bit-identical and both stamped cells are 0 here, this surfaces as a coord-set divergence rather than a value mismatch — but it is structurally visible and a hard contract failure. Verify our VSRC `setup()` TSTALLOC list against `vsrcset.c:88-91` (all four `node×branch` + `branch×node` cells must be allocated, matching the exact pair order).
2. **Capacitor transient Q-seed mismatch (numerical / arithmetic-order at tranInit).** `C_filter.Q` ours -5.66e-42 vs ngspice -1.46e-32 at state0/step0/iter0; our state2 slot already holds -1.46e-32 (≈ ngspice's state0), indicating the cap's initial-charge seed is written into a different state vintage or with a different rounding at `MODEINITTRAN` than ngspice's cap. This propagates into rhs (1.46e-23) and the node voltage (1.46e-28). Per MEMORY (post-libm-shim) a ~1e-32-vs-1e-42 charge delta is NOT transcendental noise — it is a genuine seed/vintage divergence.

The diode device itself (the `dio` recon target) gates clean on `diode-resistor.dts` (single diode + R, fully null) and the diode state slots match on the bridge; the bridge failure is in the shared VSRC stamp coord-set and the capacitor seed, not in the diode model. The contract gate (`firstDivergence` null on ALL classes on ALL fixtures) is a hard requirement and may NOT be waved through ("tolerance"/"pre-existing" are banned closing verdicts), so `dio` cannot ff-merge to `v41-port` until both are closed.

### Decision needed (user)

(1) Reconcile the VSRC `setup()` TSTALLOC sequence bit-for-bit to `vsrcset.c:88-91` so the neg↔branch coupling allocates BOTH `(negNode,branch)` and `(branch,negNode)` cells (the harness `coord-set-differs` transpose-pair goes away and `matrix` first-divergence goes null). (2) Reconcile the capacitor transient initial Q-seed (state vintage + rounding) at `MODEINITTRAN` to ngspice's cap so `C_filter.Q` matches bit-exact at step0/iter0 (the `state`/`rhs`/`voltage` first-divergences go null). `diode-resistor.dts` is fully clean; only `diode-bridge.dts` (4 diodes + filter cap + VSRC) exercises both divergences. `dio` unit left UNMERGED (no commit reached `v41-port`; `progress.json` for the 37 `dio` items NOT set APPLIED). Recon source preserved at `.wt-failed/dio.diff` (`git diff v41-port -- src/`, 103682 bytes) for resume-from-patch.

## FIX-010 — MOS1 transient NR sub-solve attempt-structure divergence: extra ngspice tranNR/re-predictor sub-attempts over a bit-identical solve (blocks `mos1` device-class harness gate)

- **Surfaced by:** GATE+MERGE+TEARDOWN for unit `mos1` (2026-06-03, isolated worktree `.wt/mos1`, serial stage post-rebase onto the advancing `v41-port` — rebase replayed clean, `rebaseClean=true`; COMPILE GATE 28/28 pass on `compile-analog-partition.test.ts` + `compiler.test.ts`). The `mos1` worktree `git diff v41-port -- src/` is **empty** (0 bytes) — the MOS1 class source is already on `v41-port`; this is a pure harness-gate failure surfaced on the gate fixture, not a recon-source defect.
- **digiTS:** `src/solver/analog/` transient Newton driver / LTE-retry state machine — the per-step sequence of NR sub-solve attempts and the disposition each is tagged with (`tranSolve` / `lteRejectedRetry`). On `mosfet-inverter.dts` step 6 our engine walks 4 attempts (predictor→`tranNR(lteRejectedRetry,4it)`→predictor→`tranNR(accepted,5it)`) while ngspice walks 4 attempts where the FIRST tranNR is tagged `dcopSubSolveConverged` and the pairing shows TWO ngspice sub-attempts (its tranNR idx1 + re-predictor idx2) that our trace does not emit as separate paired attempts. The numerics are identical; the attempt *bookkeeping/disposition* differs.
- **ngspice:** `ref/ngspice/src/spicelib/analysis/dctran.c` — the transient inner loop's `NIiter` sub-solve / `CKTtrunc` LTE-reject-and-retry path that decides when a tranNR sub-solve is "converged but LTE-rejected" (ngspice's `dcopSubSolveConverged` + retry) vs digiTS's single `lteRejectedRetry` tag, and how many predictor/NR sub-attempts are emitted per accepted step. MOS1 limiter functions confirmed correct: `mos1load.c:368` (`DEVfetlim` vgs), `:380` (`DEVfetlim` vgd), `:374,:385` (`DEVlimvds` vds), `:393,:399` (`DEVpnjlim` vbs/vbd) — the harness GS `limitType` label `pnjlim` on the ngspice side is an instrumentation-label artifact, not an applied-limiter divergence (`firstDivergence.limiting` is null).

### Evidence — the gate

Server restarted onto the `.wt/mos1` build via `.mcp-active-tree`. DLL `ref/ngspice/visualc/sharedspice/Release.x64/ngspice.dll`. Gate fixture `mosfet-inverter.dts` (`src/solver/analog/__tests__/ngspice-parity/fixtures/`, already tracked — `mos1` reuses the existing parity fixture, no authoring needed): NMOS `M1` common-source, `R_D` drain load, `V_IN` gate drive, `V_DD` supply, DC path to gnd; 3 nodes / 4 elements.

- **mosfet-inverter.dts** — `harness_run` (DC-OP + transient, stopTime 10µs): both engines converge every step (122/122, 0 failed, stepCount delta 0 `withinTol:true`, avgIterations 2.197 both, maxIterations 11 both, worstStep 6 both), 135/135 compared passed. BUT `harness_first_divergence` **NON-NULL**: `earliest` = **convergence** at **step6/iter0**, attribute `M1`, ours `"converged"` vs ngspice `"failed"` (absΔ 1). `voltage`/`rhs`/`matrix`/`state`/`integration`/`limiting`/`shape` ALL **null**.
- `harness_get_step` step6: stepStartTime/endTime/dt bit-identical (delta 0). `ours` attempts: [0 `tranPredictor`/handoff/1it, 1 `tranNR tranSolve`/**`lteRejectedRetry`**/4it, 2 `tranPredictor`/handoff/1it, 3 `tranNR tranSolve`/accepted/5it]. `ngspice` attempts: [0 same, 1 `tranNR`/**`dcopSubSolveConverged`**/4it, 2 `tranPredictor`/handoff/1it, 3 `tranNR tranSolve`/accepted/5it]. All four `endNodeNorm`/`endBranchNorm` bit-identical. `pairing` shows ngspice idx1 (`tranNR`) and idx2 (`tranPredictor`) as **unpaired** (`ourIndex:null`, divergenceNorm -1) and our idx2/idx3 unpaired against ngspice — the two engines emit a different sub-attempt count/structure for the same accepted step.
- `harness_get_attempt` step6 `tranNR` phaseAttemptIndex0 iter0 (slice on M1): **every numerical field is bit-identical** — nodeVoltages, nodeVoltagesBefore, branchValues, all four elementStates vintages (state0/1/2/3), `rhs` `[8.462e-4, -8.633e-4]`, `residual` `[-8.462e-4, -5.063e-3]`, `residualInfinityNorm` 5.063226357071298e-3 both, `ag`, `delta` 3.2e-10, `matrix`, `initMode` `MODETRAN|MODEINITFLOAT`, `divergenceNorm` **0**. Both report `globalConverged:false`, `elemConverged:false`, `noncon:1` at iter0 (ours `convergenceFailedElements:[]` / ngspice `ngspiceConvergenceFailedDevices:["m1"]`). The limiting events produce identical voltages (GS not limited 3.2→3.2; DS limited -3.105→-0.5; BD limited 3.105→0.124 both sides).

### Classification

Numerical/structural transient-NR sub-solve **attempt-structure** divergence at step6/iter0, NOT a value divergence and NOT a load/accumulation-order divergence (matrix/rhs/voltage/state all null, dense slice bit-identical, `divergenceNorm` 0 — per CLAUDE.md "Sparse Solver is Settled" there is nothing for the solver/stamp path to fix here). The `convergence`-class first-divergence is the harness comparing the *attempt-level converged disposition*: our transient driver tags the LTE-rejected tranNR sub-solve `lteRejectedRetry` and collapses the retry into 4 paired attempts, while ngspice's `dctran.c` inner loop tags the equivalent sub-solve `dcopSubSolveConverged` and emits two extra sub-attempts (tranNR idx1 + a re-predictor idx2) that our trace does not surface as separate paired attempts. Because every shared numeric quantity is bit-exact and both engines accept the identical final solve in 5 iterations, this is a state-machine / attempt-emission parity gap in the transient Newton+LTE-retry controller, not a MOS1 model arithmetic bug. The contract gate (`firstDivergence` null on ALL classes on ALL fixtures) is a hard requirement and may NOT be waved through ("tolerance"/"close enough"/"pre-existing" are banned closing verdicts), so `mos1` cannot ff-merge to `v41-port` until this is closed and the 43 queued `mos1` ledger items stay NOT-APPLIED.

### Decision needed (user)

Reconcile the transient Newton driver's LTE-reject-and-retry sub-solve sequencing in `src/solver/analog/` bit-for-bit to `dctran.c`'s inner `NIiter`/`CKTtrunc` loop so that on `mosfet-inverter.dts` step 6 our engine emits the same sub-attempt structure and per-attempt converged disposition as ngspice (the `convergence`-class first-divergence goes null). This is a shared transient-controller item (adjacent to the timestep-controller family FIX-003/005/007/008 but distinct: those are `delta`/breakpoint selection at ~3ms; this is sub-attempt *emission + disposition tagging* at step6/t≈0.5ns) and should be cross-validated against all transient fixtures since the controller touches every transient solve. `mos1` unit left UNMERGED (no commit reached `v41-port`; `progress.json` for the 43 `mos1` items NOT set APPLIED — they stay `PENDING`). The worktree src diff vs `v41-port` is empty so `.wt-failed/mos1.diff` is 0 bytes (no recon work to resume — this is a harness-gate failure on already-merged MOS1 class source against a shared transient-controller gap).

## FIX-011 — JFET2 (PS-model JFET) device family registered in the deck-pin-order table but absent from the audit's `DECK_EMITTING_FAMILIES`, breaking the compile gate (blocks `jfet2` device-class harness gate)

- **Surfaced by:** GATE+MERGE+TEARDOWN for unit `jfet2` (2026-06-03, isolated worktree `.wt/jfet2`, serial stage post-rebase onto the advancing `v41-port` — 2 commits replayed, 0 conflicts, `rebaseClean=true`). `jfet2` is a from-scratch device class (`isomorphic=true`, `committedAnything=true`); it carries real recon src work (`.wt-failed/jfet2.diff` = 71581 bytes, `git diff v41-port -- src/`). The HARNESS gate never ran on `src/solver/analog/__tests__/ngspice-parity/fixtures/jfet2-gate.dts` because the **COMPILE GATE failed first**, so `firstDivergence` is `null` (no harness divergence was produced — the failure is upstream at registry build time).
- **digiTS:** `src/solver/analog/ngspice-load-order-audit.ts:10-34` — the `DECK_EMITTING_FAMILIES` set. The jfet2 implementer registered `JFET2N`/`JFET2P` in all three load-order tables in `src/solver/analog/ngspice-load-order.ts`: `TYPE_ID_TO_NGSPICE_LOAD_ORDER` (`:139-140`, ord 31 = `dev.c:185 get_jfet2_info`), `TYPE_ID_TO_DEVICE_FAMILY` (`:188-189`, family `"JFET2"`), and `TYPE_ID_TO_DECK_PIN_LABEL_ORDER` (`:250-252`, pin order `["D","G","S"]`, comment `// Z name D G S model (PS JFET2)`). JFET2 emits a `Z` card and IS therefore deck-emitting, so registering the deck-pin row is correct — but `"JFET2"` was NOT added to `DECK_EMITTING_FAMILIES` (which lists `"JFET"` but not `"JFET2"`). The audit (`ngspice-load-order-audit.ts:64-68`) then flags `JFET2N`/`JFET2P` as "in `TYPE_ID_TO_DECK_PIN_LABEL_ORDER` but family `JFET2` is not deck-emitting" and throws, which fires inside `createDefaultRegistry` (`src/components/register-all.ts:584`) — so EVERY analog compile path that builds the default registry throws. Compile gate result: `compile-analog-partition.test.ts` + `compiler.test.ts` = **27 passed / 1 failed** on the worktree, vs **28/0** on the `v41-port` base (this is NOT pre-existing — it is introduced by the jfet2 registration work).
- **ngspice:** `ref/ngspice/src/spicelib/devices/jfet2/` (the PS-model JFET2 device — the `Z`-card device class, sibling to `jfet/` JFET1). The deck card is `Z<name> <D> <G> <S> <model>` (matching the registered `["D","G","S"]` pin-label order and ord 31 from `dev.c:185 get_jfet2_info`).

### Evidence — the compile gate

```
FAIL src/solver/analog/__tests__/compile-analog-partition.test.ts > compileAnalogPartition
     > setup() assigns _stateBase >= 0 to pool-backed elements after engine warm-start
Error: ngspice-load-order audit failed:
  - typeId "JFET2N" is in TYPE_ID_TO_DECK_PIN_LABEL_ORDER but family "JFET2" is not deck-emitting
  - typeId "JFET2P" is in TYPE_ID_TO_DECK_PIN_LABEL_ORDER but family "JFET2" is not deck-emitting
Fix the affected keys in src/solver/analog/ngspice-load-order.ts.
  ❯ Module.auditNgspiceLoadOrderTables src/solver/analog/ngspice-load-order-audit.ts:83:11
  ❯ Module.createDefaultRegistry src/components/register-all.ts:584:3
  ❯ Module.buildFixture src/solver/analog/__tests__/fixtures/build-fixture.ts:73:20
```

### Classification

Structural/registration consistency defect introduced by the from-scratch `jfet2` class: the device's three load-order tables and the audit's `DECK_EMITTING_FAMILIES` set disagree on whether the `JFET2` family is deck-emitting. JFET2 demonstrably IS deck-emitting (it emits a single `Z` card, the deck-pin row is correct), so the audit's family set is the side that is missing the entry. This is a hard compile-gate failure (the default registry throws at build time, before any harness run), so per the contract step-2 path the worktree is non-compiling post-rebase and is NOT merged. The fix is a one-line addition of `"JFET2"` to `DECK_EMITTING_FAMILIES` in `ngspice-load-order-audit.ts:10-34` — but per the gate-fail decision the gate stage does NOT edit to pass; it escalates. (The audit error's `.test()` name — "setup() assigns _stateBase >= 0…" — is incidental: the throw happens during registry construction inside that test's `buildFixture`, not in `_stateBase` logic; the `_stateBase`/state-pool path itself is not implicated.)

### Decision needed (user)

Add `"JFET2"` to `DECK_EMITTING_FAMILIES` in `src/solver/analog/ngspice-load-order-audit.ts` (the family emits a `Z` card and already carries a correct deck-pin-order row), so `createDefaultRegistry` stops throwing and the compile gate returns 28/28 on the `jfet2` worktree. Then re-run the `jfet2` GATE+MERGE+TEARDOWN: the compile gate must pass before the harness gate on `jfet2-gate.dts` can run and the 12 `jfet2` ledger items can be evaluated for APPLIED. `jfet2` unit left UNMERGED (no commit reached `v41-port`; `progress.json` for the 12 `jfet2` items NOT set APPLIED — they stay `PENDING`/un-recorded). Recon source preserved at `.wt-failed/jfet2.diff` (`git diff v41-port -- src/`, 71581 bytes) for resume-from-patch. NOTE: this is the second from-scratch class (after the queued `asrc`/`mes`) whose registration must add its family to `DECK_EMITTING_FAMILIES` when the device emits a primitive card — worth a from-scratch-class checklist item so the next such build does not re-trip this audit.

## FIX-012 — MOS3 half-ported `NOBYPASS` bypass path in `mosfet3.ts` `load()`: bypass GATE present but the `goto bypass` SKIP missing, so the moseq3 core re-runs at snapped voltages and Y-stamps/RHS diverge bit-exactly from ngspice whenever `ctx.bypass` is enabled (blocks `mos3#recon/wholeClass` harness gate)

- **Surfaced by:** GATE+MERGE+TEARDOWN for unit `mos3` (2026-06-03, isolated worktree `.wt/mos3`, serial stage). `mos3` is a from-scratch device class with `isomorphic=false` / `committedAnything=false` — the recon work was never committed (the worktree carried uncommitted/untracked recon src: `mosfet3.ts`, two `__tests__/mosfet3*.test.ts`, two `mos3-{amp,ac}.dts` fixtures, plus tracked edits to `register-all.ts` and `mosfet.ts`). Because the work was uncommitted, the `git rebase v41-port` could not start ("cannot rebase: You have unstaged changes"), so `rebaseClean=false` and the unit is NOT merged. `firstDivergence` is therefore not produced by a harness run at this gate (the MISMATCH below is the recon-review verdict passed into the gate, a bit-exact load()-level divergence, not a re-run harness divergence). Recon source preserved at `.wt-failed/mos3.diff` (staged `git diff v41-port -- src/` capturing the untracked `mosfet3.ts` and fixtures, 132633 bytes) for resume-from-patch.
- **digiTS:** `src/components/semiconductors/mosfet3.ts` `load()`. The bypass GATE is present and correct (mirrors `ref/ngspice/src/spicelib/devices/mos3/mos3load.c:282-336`): it evaluates the reltol/voltTol/cdhat criterion, snaps vbs/vbd/vgs/vds back to state0, rebuilds bypassCapgs/gd/gb, and sets `bypassed=true`. But the SKIP that the bypass exists to perform is missing. The bulk-diode block (lines 960-979), the full moseq3 drain-current core (988-1293) and `cd = opMode*cdrain - cbd` (1298) all run UNCONDITIONALLY, recomputing cbs/cbd/gbs/gbd/cdrain/_gm/_gds/_gmbs from the snapped voltages rather than reloading the stored converged values. Only the bulk-cap (1304) and Meyer (1406/1452) blocks are correctly gated on `!bypassed`.
- **ngspice:** `ref/ngspice/src/spicelib/devices/mos3/mos3load.c`. `goto bypass` (mos3load.c:334) jumps to the `bypass:` label (mos3load.c:1158), skipping the next1 bulk diodes (414-433), mode determination, the entire moseq3 drain-current core (446-871) and the `cd` assignment (882); on bypass ngspice reuses the saved conductances and reconstructs `cdrain = MOS3mode*(MOS3cd+MOS3cbd)` (line 322).

### Evidence — the recon-review verdict (MISMATCH)

Verdict: MISMATCH on `mos3#recon/wholeClass`. Because the bypass criterion is a tolerance band (not exact equality), a fresh moseq3 evaluation at the snapped state0 voltages does not reproduce the previously-converged cdrain/gm/gds/gmbs, so the Y-matrix stamps and RHS diverge bit-exactly from ngspice whenever `ctx.bypass` is enabled and a device bypasses. This violates acceptance criterion #7 (`load()` ports MOS3load bit-exact) and the Part E item-2 bypass requirement ("if kept, port the reltol gates line-for-line" — the gate was kept but the goto-bypass skip was not). The canonical correct shape is the MOS1 sibling at `mosfet.ts:1320-1340`: `if (bypassed) { gmNR=this._gm; gdsNR=this._gds; gmbsNR=this._gmbs; gbd=this._gbd; gbs=this._gbs; cbd=this._cbd; cbs=this._cbs; cd=this._cd; cdrain = opMode*(cd+cbd); } else { <bulk diodes>; <moseq3>; }`. The FIX is fully contained in `src/components/semiconductors/mosfet3.ts` (recon's own file) — wrap the bulk-diode (960-979), moseq3 (988-1293) and cd assignment (1298) in an `else` of `if (bypassed) { reload saved scalars + cdrain=opMode*(cd+cbd) }`, mirroring `mosfet.ts:1320`. Per the recon-review note this is NOT an out-of-scope escalation (no foreign file needed) and is NOT closeable as pre-existing.

All other MOS3 surfaces verified isomorphic by the recon review: `MOSFET3_SCHEMA` 17 slots in `mos3defs.h:277-302` order; param defs/defaults match `mos3set.c`/`mos3temp.c` (kappa=.2, mjsw=.33, tox=1e-7, is=1e-14, kp=2e-5, u0=600, +CONSTCtoK on temp/tnom, no setters for xd/alpha/uexp/neff); TSTALLOC 22-cell sequence line-for-line `mos3set.c:234-255`; prime-node gates `mos3set.c:188-226`; computeTemperature model-preprocessing (`mos3temp.c:39-114`) + per-instance (118-340) line-for-line incl ratio4=ratio*sqrt(ratio) and the f2d/f3d/f4d/f2s/f3s/f4s coefficients; the moseq3 core itself (XJ fshort, ETA vbix, THETA onfg/fgate, VMAX vdsat/fdrain, KAPPA line510/520/700/900 goto structure) bit-exact in ngspice operand order; bulk caps + niIntegrate; Meyer via `devQmeyer` with mode>0/<0 arg swap; RHS load (1202-1224); 22-cell Y stamp (1236-1263); stampAc full MOS3acLoad incl real conductances per OQ#46; getLteTimestep on QGS/QGD/QGB only (`mos3trun.c:21-23`). The two tracked edits are structurally-forced minimal: `export function devQmeyer` (`mosfet.ts:581`, required precondition) and `register-all.ts` registering Mosfet3NDefinition/Mosfet3PDefinition. The two untracked `.dts` fixtures (`mos3-amp.dts`, `mos3-ac.dts`) are spec-required (Ratification item 2). Pin layout 4-terminal D/G/S/B and NMOS3/PMOS3 polarity factories per Ratification 3/4. Re-derived independently against `ref/ngspice/src/spicelib/devices/mos3/`.

### Classification

Numerical bug: bit-exact Y-stamp/RHS divergence from ngspice on any step where `ctx.bypass` is enabled and a MOS3 device bypasses, caused by re-running the moseq3 drain-current core at snapped state0 voltages instead of reloading the converged conductances and reconstructing `cdrain = opMode*(cd+cbd)` per `mos3load.c:322`. The defect is contained entirely in the recon's own file `src/components/semiconductors/mosfet3.ts`; the fix is the `if (bypassed) { reload } else { <bulk diodes>; <moseq3>; <cd assign> }` restructure mirroring the MOS1 sibling at `mosfet.ts:1320-1340`. Not closeable as "pre-existing" or "tolerance" (banned closing verdicts) — the bypass band guarantees the recomputed conductances do not reproduce the converged values bit-for-bit.

### Decision needed (user)

Apply the bypass-skip restructure in `src/components/semiconductors/mosfet3.ts` `load()`: wrap the bulk-diode block (960-979), the moseq3 core (988-1293) and the `cd = opMode*cdrain - cbd` assignment (1298) in the `else` branch of `if (bypassed) { gmNR=this._gm; gdsNR=this._gds; gmbsNR=this._gmbs; gbd=this._gbd; gbs=this._gbs; cbd=this._cbd; cbs=this._cbs; cd=this._cd; cdrain = opMode*(cd+cbd); }`, mirroring `mosfet.ts:1320-1340` (MOS1) and `mos3load.c:322` (`cdrain = MOS3mode*(MOS3cd+MOS3cbd)`). Then re-run the `mos3` GATE+MERGE+TEARDOWN from the preserved patch: the recon work was uncommitted so the rebase never started (`rebaseClean=false`), the unit is UNMERGED, no `mos3` ledger items set APPLIED — they stay `PENDING`. Recon source preserved at `.wt-failed/mos3.diff` (132633 bytes, includes untracked `mosfet3.ts` + `mos3-{amp,ac}.dts` fixtures) for resume-from-patch; on resume the implementer must (a) commit the recon work, (b) apply this bypass-skip restructure, then (c) the gate can rebase onto `v41-port` and run the harness gate on the `mos3` fixtures.

## FIX-013 — `maths-sparse/spsmp.c#h002` element-lookup unseen-row short-circuit removal reorders per-device accumulation into the shared `(VCC,VCC)` diagonal: bit-divergent Jacobian diagonal + state at step 0 iter 0 on the BJT-bearing `nodeset-gate.dts` fixture (blocks `maths-sparse` harness gate)

- **Surfaced by:** GATE+MERGE+TEARDOWN for unit `maths-sparse` (2026-06-03, isolated worktree `.wt/maths-sparse`, serial stage; `isomorphic=true` / `committedAnything=true`). The unit rebased cleanly onto `v41-port` (`rebaseClean=true`) and passed the compile gate (`src/solver/analog/__tests__/compile-analog-partition.test.ts` + `compiler.test.ts`, 28/28). The harness/self-compare gate FAILED on `nodeset-gate.dts` (`gatePass=false`), so the unit is NOT merged, no `maths-sparse` ledger items set APPLIED (`maths-sparse/spsmp.c#h002` stays `PENDING`). Per the gate-fail Decision (escalate, never edit to pass) the divergence was NOT chased into other files. Kept work preserved at `.wt-failed/maths-sparse.diff` (1170 bytes, `git diff v41-port -- src/`) for resume-from-patch.
- **The harvested hunk (`spsmp.c#h002`, hunkHash `39f4e6f103e2be01`, ref `harvested:268f4a59`):** in `src/solver/analog/sparse-solver.ts` (~line 710, the read-only `createIfMissing=false` element-lookup), it removes the symmetric `intRow === -1` early-out so an unseen ext row is left to the column-chain walk (`_spcFindElementInCol` returns no match) rather than short-circuiting to `-1`. Intent: match `ref/ngspice/src/spicelib/sparse/spsmp.c:464-469` where only the absent-column case (line 464-466) short-circuits and the unseen row falls out of the column walk (468-469). The change is contained to the lookup short-circuit; the matrix ordering / Markowitz / pivot core is untouched (and is settled per CLAUDE.md).
- **digiTS divergence (harness, post-hunk):** `nodeset-gate.dts` (two cross-coupled BJTs Q1/Q2, RB1/RB2/RC1/RC2, VCC) — `harness_first_divergence` is NON-null. `earliest` (causal tie-break) = `state` class `Q2.VBE` at stepIndex 0 / iterationIndex 0, ours `0.6996637349555478` vs ngspice `0.6996637349555483`, absDelta `5.551115123125783e-16`. Same (step,iter): `matrix` diagonal cell `(RC2:pos/RC1:pos/VCC:pos, RC2:pos/RC1:pos/VCC:pos)` ours `0.01521466984188619` vs ngspice `0.0152146698418884`, absDelta `2.2100377083944522e-15`; `voltage` `Q1:B/RB1:neg` absDelta `8.33e-17`; `rhs` `Q1:C/RB2:pos/RC1:neg` absDelta `6.79e-26`; `limiting` `Q2:BE.vAfter` = the same VBE. The companion `ic-gate.dts` (V1/R1/C1, no semiconductors) is FULLY clean: `harness_first_divergence` null in all eight classes, 107/107 steps pass.

### Classification

Numerical bug, NOT the sparse solver. The matrix and RHS are NOT bit-identical at iter 0 (`(VCC,VCC)` diagonal differs by 2.21e-15, rhs by 6.8e-26), so per CLAUDE.md "Sparse Solver is Settled" this is not a `spOrderAndFactor`/`spFactor`/`spSolve` divergence and not the per-bucket setup walk. The signature — a bit-divergent shared diagonal `(VCC,VCC)` at the very first iteration with the causal tie-break landing on a BJT state (`Q2.VBE`), present only on the BJT-bearing fixture and absent on the linear RC fixture — is **per-device `load()` accumulation order on a shared diagonal**: the element-lookup short-circuit removal changes which element-pool slot an unseen-row lookup resolves to (or introduces column-walk pool churn), reordering the floating-point `+=` accumulation of the multiple device stamps that land on the shared VCC-node diagonal cell. The divergence is structurally upstream of the solve, exposed by the matrix-assembly lookup change, exactly the upstream class CLAUDE.md directs investigation toward (per-device `load()` accumulation order on a shared diagonal). Not closeable as "tolerance" / "1-ULP noise" (banned closing verdicts) — post the ucrt-libm shim every remaining NUMERICAL parity failure is genuine arithmetic-order divergence, and the delta is a multi-ULP diagonal disagreement, not transcendental noise.

### Decision needed (user)

The `spsmp.c#h002` element-lookup change is bit-exact-correct against `spsmp.c:464-469` in isolation (only the absent-column case should short-circuit). The divergence it surfaces is in the per-device `load()` accumulation order into the shared `(VCC,VCC)` diagonal — investigate the BJT `load()` stamp sequence into the collector/VCC-node diagonal vs `ref/ngspice/src/spicelib/devices/bjt/bjtload.c` accumulation order, and the resistor stamps (RC1/RC2) that co-accumulate into that same diagonal, against their `resload.c` order — the `+=` order on the shared cell must match ngspice's. Either (a) the BJT/resistor `load()` accumulation order is the real defect this hunk merely exposed (fix the stamp order, then `maths-sparse` re-gates clean and `spsmp.c#h002` lands APPLIED), or (b) the hunk's lookup change perturbs the element-pool slot resolution for the VCC diagonal in a way that reorders the existing-correct stamps (in which case the lookup change must preserve the pool-slot ordering of already-allocated cells). Re-run the `maths-sparse` GATE from `.wt-failed/maths-sparse.diff` after the accumulation-order fix; the unit is currently UNMERGED with `spsmp.c#h002` at `PENDING`.

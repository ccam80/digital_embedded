# Root-cause inventory

225 unique findings across 6 categories.

| Category | Findings |
|---|---:|
| `COMPONENT` | 126 |
| `ANALOG-SOLVER` | 41 |
| `HARNESS/PARITY` | 41 |
| `OTHER` | 11 |
| `NAN` | 5 |
| `DIGITAL` | 1 |

## `COMPONENT` (126)

| Test | Root cause (file:line) | Diagnosis | Fix hint | Conf |
|---|---|---|---|---|
| clock_below_vIH_does_not_trigger_conversion | `src/components/active/adc-driver.ts:235` | CLK=1.5V is between vIL=0.8 and vIH=2.0 so the DigitalInputThresholder outputs the 0.5V indeterminate level on clk_result. The driver edge test risingEdge = vClock*(1-prevClock) = 0.5*(1-0) = 0.5 > 0 fires an edge, so in | The driver must treat clk_result as logic-high only when vClock > 0.5 (above the indeterminate midpoint produced by DigitalInputThresholder which emits 0.0/0.5/1.0). Replace the raw vClock*(1-prevClock) edge test at line | high |
| hotload_vIH_changes_clock_sensitivity | `src/components/active/adc-driver.ts:290` | After hotloading vIH=4.0 the CLK thresholder outputs the 0.5V indeterminate level for a 3.3V clock (0.8<3.3<4.0). The instant-mode EOC-clear branch tests vClock < 0.5 which is false at exactly 0.5, so EOC never clears an | clk_result carries logic levels 0.0/0.5/1.0 from DigitalInputThresholder. The driver must classify clk high only when vClock > 0.5 (above indeterminate midpoint) for edge detection at line 235-237 and clear EOC when vClo | high |
| hotload_transresistance_changes_vout | `src/components/active/ccvs.ts:180` | CCVSAnalogElement.setParam only handles senseSourceLabel and silently ignores transresistance. The transresistance is baked into the compiled expression (transresistance * I(sense)) inside the factory at lines 388-391, a | Add a transresistance branch to setParam (line 180-184) that, when the expression is the default I(sense) linear shortcut, reparses value * I(sense) and updates the elements compiled expression and derivative (the _compi | high |
| dcop_oc_off_output_near_voh | `src/components/active/comparator-driver.ts:193` | ComparatorDriver stamps a RAIL-LEVEL Thevenin target vTarget = (1-w)*vOH + w*vOL onto the internal ctrl_out net, but the downstream DigitalOutputPinLoaded.outPin expects ctrl in NORMALIZED [0,1] V (digital-output-pin-loa | The driver must stamp the NORMALIZED logic level onto ctrl_out, not the rail-level vTarget. With latch=0 inactive the logic level is 1 (-> outPin drives vOH) and latch=1 active is 0 (-> vOL), so stamp (1 - wForStamp) as  | high |
| hotload_hysteresis_flips_latch_when_margin_inside_dead_band | `src/components/active/comparator-driver.ts:193` | before and after are both 0.157V (after expected greater). The output is saturated/railed because the rail-level vTarget on ctrl_out is double rail-mapped by the outPin VCVS, so flipping the latch via hysteresis does not | Same root cause as dcop_oc_off: stamp the normalized [0,1] logic level (1 - wForStamp) onto ctrl_out at line 193 instead of the rail-level vTarget; once cmp:out tracks the logic level a hysteresis-induced latch flip will | high |
| hotload_responseTime_changes_weight_integration | `src/components/active/comparator-driver.ts:181` | weightBefore and weightAfter are both 1. The buildFixture warm-start runs a DC-family solve and the DC branch sets wNew = latchNew = 1 directly (line 180-181), jamming OUTPUT_WEIGHT to the steady-state target during warm | The DC weight collapse pre-saturates OUTPUT_WEIGHT so the transient integration test starts at the target. Either the DC branch should not persist the steady-state weight into the slot used by the subsequent transient in | medium |
| hotload_responseTime_faster_integration_lowers_output_sooner | `src/components/active/comparator-driver.ts:193` | Both fixtures (slow and fast responseTime) report the identical output 0.157V because the rail-level vTarget stamped onto ctrl_out is double-mapped by the outPin VCVS, saturating cmp:out at a railed divider value that is | Same root cause as dcop_oc_off: stamp the normalized [0,1] logic level (1 - wForStamp) onto ctrl_out at line 193 rather than the rail-level vTarget, so the weight integration actually moves cmp:out and responseTime affec | high |
| hotload_vos_shifts_threshold_above_input_flips_latch | `src/components/active/comparator-driver.ts:193` | before and after are both 0.157V (after expected greater). cmp:out is saturated/railed because the rail-level vTarget on ctrl_out is double rail-mapped by the outPin VCVS, so raising vos to flip the latch off does not mo | Same root cause as dcop_oc_off: stamp the normalized [0,1] logic level (1 - wForStamp) onto ctrl_out at line 193 instead of the rail-level vTarget; with cmp:out tracking the logic level a vos-induced latch flip will rais | high |
| stamps vOH at out when in+ is above in- (not sinking: pull-up holds line high) | `src/components/active/comparator-driver.ts:165` | Open-collector comparator latch polarity is inverted: latch=1 (active/sinking, drives vOL/LOW) is set when vPlus >= vTh i.e. when in+ is ABOVE in-, but a comparator drives output HIGH when V+ > V-. So in+ above in- wrong | Invert the latch transition conditions at lines 165-166: latch should go to 1 (sinking) when vPlus < vTl and release to 0 when vPlus >= vTh, so V+>V- yields not-sinking (HIGH) and V+<V- yields sinking (LOW). | high |
| stamps vOL at out when in+ is below in- (asserted: sinking pulls line low) | `src/components/active/comparator-driver.ts:166` | Same inverted open-collector latch polarity: with in+ below in- the code keeps latch=0 (not sinking) so it drives ctrl_out toward vOH instead of sinking. The 24.9V readback is the outPin DigitalOutputPinLoaded fighting t | Invert latch transition at lines 165-166 so latch becomes 1 (sinking, drives vOL) when vPlus < vTl; that makes in+ below in- pull the open-collector output low through the external pull-up. | high |
| dcop_pp_off_output_near_voh | `src/components/active/comparator-pushpull-driver.ts:170` | ComparatorPushPullDriver stamps a RAIL-LEVEL vTarget = (1-w)*vOH + w*vOL onto ctrl_out, but the downstream DigitalOutputPinLoaded.outPin expects a NORMALIZED [0,1] V logic level and re-applies the rail mapping via its eD | Stamp the normalized logic level (1 - wForStamp) onto ctrl_out at line 170 instead of the rail-level vTarget, matching the DigitalOutputPinLoaded normalized-ctrl contract so the single rail mapping happens only in the ou | high |
| stamps vOH at out when in+ is above in- (push-pull drives HIGH) | `src/components/active/comparator-pushpull-driver.ts:146` | Push-pull comparator latch polarity is inverted (mirrors comparator-driver.ts): latch=1 (drives vOL/LOW) is set when vPlus >= vTh i.e. in+ above in-, so the push-pull stage drives the output LOW (got 0) when a comparator | Invert the latch transition conditions at lines 146-147 so latch=1 (drive vOL) engages when vPlus < vTl and releases when vPlus >= vTh; V+>V- then yields w->0 and vTarget->vOH (HIGH). | high |
| stamps vOL at out when in+ is below in- (push-pull drives LOW) | `src/components/active/comparator-pushpull-driver.ts:147` | Same inverted push-pull latch polarity: in+ below in- keeps latch=0 so the stage drives ctrl_out toward vOH; the outPin DigitalOutputPinLoaded then drives out to 24.9V (out of rail) fighting the wrongly-driven ctrl_out,  | Invert latch transition at lines 146-147 so latch=1 (vOL) engages when vPlus < vTl; in+ below in- then drives the push-pull output LOW as expected. | high |
| hotload_rOut_sags_output_under_load | `src/components/active/dac-driver.ts:196` | DACDriver stamps an ideal VSRC branch (V_OUT - V_GND = target exactly, lines 196-200) with no series output resistance, and buildDacNetlist (dac.ts) never wires the rOut param into any element. So the output is a stiff v | Stamp a Thevenin output (target voltage behind rOut) in DACDriver.load instead of an ideal VSRC branch, and wire the rOut param into the driver so output impedance creates the divider; make rOut hot-loadable via setParam | high |
| hotload_rOut_changes_output_divider | `src/components/active/opamp.ts:298` | OpampElement.setParam writes only this._p[key], but load() computes the RES conductance G = 1/this._rOut using the separate constructor-cached field this._rOut (opamp.ts:194,258) which setParam never updates. Hot-loading | In setParam, when key===rOut update this._rOut (or make load/G read this._p.rOut directly so the live value drives the conductance). The cached _rOut field and _p must not diverge. | high |
| hotload_gmMax_clamp_changes_vout_in_linear_region | `src/components/active/ota.ts:226` | The test expects clamping gmMax to change the converged V_out, but gmMax is a Jacobian-only quantity. In load() the Norton offset iNR = iOutNow - gmEff*vDiff (ota.ts:226) plus the gmEff stamp contribution gmEff*vDiff can | This is a test-contract error not a source bug: gmMax only bounds the linearization slope and cancels at the fixed point. Either assert on convergence iteration behavior or remove the not.toBeCloseTo expectation; do not  | high |
| dcop_rail_saturation_clamps_to_vrail_pos | `src/components/active/real-opamp.ts:541` | Rail-saturated unity follower (Vin=16) never converges (dcOperatingPoint returns converged=false, 3124 iters, all nodes 0). The load() mode switch at line 541 replaces the entire gain-stage Jacobian coupling with a const | Saturation must be enforced as a smooth/limited continuation of the same Jacobian (keep the gain-stage coupling and add a soft clamp / push the rail limit through railLim only) instead of a hard branch that drops the -aE | high |
| dcop_unity_follower_tracks_vin | `src/components/active/real-opamp.ts:170` | Unity follower Vout reads 2.00098 not ~2.00000. buildUnityFollower (test line 45) passes no vos so the model default vos=1e-3 (param default at real-opamp.ts:170) applies. The closed-loop output is (Vin+vos)*aol/(1+aol)  | Either the param default vos at real-opamp.ts:170 (1e-3) is non-zero while the test fixture assumes 0, or the gain-stage vos term at line 555 should not be applied at this magnitude; reconcile the default vos with the un | high |
| hotload_aol_changes_inverting_gain_error | `src/components/active/real-opamp.ts:553` | Inverting amp aol=100 DCOP before reads -0.900 but test expects -0.0908. The source gain stage stamps a Norton VCVS with -aol*G_out / +aol*G_out coupling (lines 553-554) and diagonal G_out (533), which realizes the close | The gain-stage Norton formulation (diagonal G_out at 533 plus -aol*G_out coupling at 553) yields closed-loop aol/(NG+aol) instead of aol/(1+aol*NG). To match the standard finite-aol op-amp the gain element should be a hi | medium |
| hotload_aol_shifts_finite_gain_error | `src/components/active/real-opamp.ts:170` | Same default-vos issue: the before assertion expects Vout close to 2 within 0.0005 but reads 2.00098. buildUnityFollower passes no vos so default vos=1e-3 (real-opamp.ts:170) shifts the unity-follower output by +1mV via  | Reconcile default vos (real-opamp.ts:170) with the test fixtures finite-gain-error budget; the +1mV default offset is the source of the failing before assertion. | high |
| hotload_vos_shifts_unity_follower_output | `src/components/active/real-opamp.ts:555` | Test sets vos=0.5 then steps 5 times (transient) and expects unity-follower Vout to shift by ~0.5V; actual shift is only 0.155V. In transient (geq_int>0) the gain-stage normal branch uses aEff=aol/(1+g) (line 459) which  | In the transient gain-stage the vos offset must propagate through the same loop gain as the steady-state DC path; the line-555 RHS uses aEff (bandwidth-reduced) for vos rather than the integrator-mediated full offset, so | medium |
| hotload_vSatPos_shifts_rail_clamp | `src/components/active/real-opamp.ts:541` | Same root cause: Vin=16 rail-saturated unity follower is stepped 5 times then OUT_SAT_FLAG is asserted to be 1 but reads 0. The discontinuous saturated/unsaturated stamp branch at line 541 (gain coupling dropped on satur | Make the saturated operating point a stable fixed point (see line 541-542 fix); flag latching then follows. | high |
| init_rail_sat_state_out_sat_flag_set | `src/components/active/real-opamp.ts:541` | Same root cause as dcop_rail_saturation: with Vin=16 the warm-start/DCOP for the rail-saturated unity follower does not settle into the saturated region, so OUT_SAT_FLAG (written at line 565 from outputSaturated) stays 0 | Fix the non-convergent saturation branch (line 541-542) so the saturated operating point is a stable NR fixed point at vOut=vRailPos; then OUT_SAT_FLAG written at 565 will latch to 1. | high |
| limiting_railLim_engaged_under_overdrive | `src/components/active/real-opamp.ts:541` | Same root cause: Vin=16 overdrive DCOP does not converge into the rail region; output node settles around 1.6V (not >=13.499). The non-convergent saturated/unsaturated stamp switch at line 541 means railLim never produce | Same fix as line 541-542: keep the gain-stage Jacobian coupling under saturation so NR converges to vOut=vRailPos; railLim event recording at 488-502 then fires correctly. | high |
| preset_OPA2134_shifts_dc_output_by_vos_delta | `src/components/active/real-opamp.ts:553` | Preset-vs-default DC output delta at Vin=4 reads 0.000536 but test expects 0.0005 within 5e-6. The preset OPA2134 changes both vos (0 -> 500e-6) AND aol (100000 -> 1e6). The +36uV excess over the pure vos delta is the fi | The 36uV is genuine finite-gain-error physics from the gain stamp (533/553); either the test must budget the aol-induced gain-error difference (it only accounted for vos), or the gain-stage transfer differs from the assu | medium |
| dcop_inv_low_output_near_voh | `src/components/active/schmitt-trigger.ts:63` | Inverting netlist rOut Resistor maps {R: rOut} which does not bind (Resistor key is resistance); resistor defaults to 1000 ohm. DC-OP st:out = 2.9999997 vs expected 3.28358; close-to assert fails by 0.2836. | Change rOut sub-element param map from {R: rOut} to {resistance: rOut} on SCHMITT_INVERTING_NETLIST line 63. | high |
| dcop_noninv_high_output_near_voh | `src/components/active/schmitt-trigger.ts:83` | rOut Resistor maps {R: rOut} which does not bind (Resistor key is resistance); resistor defaults to 1000 ohm. DC-OP st:out = 3.3*10000/11000.001 = 2.9999997 vs expected 3.3*10000/10050 = 3.28358; close-to assert fails by | Change rOut sub-element param map from {R: rOut} to {resistance: rOut} in SCHMITT_NON_INVERTING_NETLIST line 83. | high |
| hotload_vTL_shifts_falling_threshold | `src/components/active/schmitt-trigger.ts:83` | Same rOut mapping bug: {R: rOut} does not bind to the Resistor (key is resistance), resistor defaults to 1000 ohm so the before-state st:out is 2.9999997 not >3.0; the >3.0 precondition assert at line 213 fails before th | Change rOut sub-element param map from {R: rOut} to {resistance: rOut} in SCHMITT_NON_INVERTING_NETLIST line 83. | high |
| init_inv_low_input_output_high | `src/components/active/schmitt-trigger.ts:63` | Inverting netlist rOut Resistor maps {R: rOut} but the Resistor param key is resistance; mapping never binds so resistor defaults to 1000 ohm. With latch low input the inverted output drives high but st:out = 2.9999997 ( | Change rOut sub-element param map from {R: rOut} to {resistance: rOut} on SCHMITT_INVERTING_NETLIST line 63. | high |
| init_noninv_high_input_latch_one | `src/components/active/schmitt-trigger.ts:83` | rOut Resistor sub-element maps params {R: rOut} but the Resistor param key is resistance not R; the mapping never binds so the resistor defaults to 1000 ohm. st:out becomes 3.3*10000/11000.001=2.9999997 instead of 3.3*10 | Change the rOut sub-element param map from {R: rOut} to {resistance: rOut} in SCHMITT_NON_INVERTING_NETLIST (and SCHMITT_INVERTING_NETLIST line 63); also fix the cIn/cOut maps from C to capacitance. | high |
| hotload_vOH_changes_output_high_voltage | `src/components/active/timer-555-latch-driver.ts:129` | Same root cause: after lowering vOH to 4.0 OUT reads 12.124V (super-rail) so abs(after-4) > abs(after-5) fails. The latch driver drives ctrl_out to a rail voltage (q?vOH:vOL) that outPin then re-multiplies by gain (vOH-v | latchDrv must drive ctrl_out as a normalized [0,1] logic level (q?1:0); outPin then maps 1 to vOH. Then OUT high tracks vOH directly. | high |
| hotload_vOH_changes_output_voltage_when_high | `src/components/active/timer-555-latch-driver.ts:129` | latchDrv drives ctrl_out (net 12) to a RAIL level q?vOH:vOL but that net feeds outPin DigitalOutputPinLoaded whose ctrl contract requires a NORMALIZED [0,1] logic level. outPin then computes nDriveV = vOL + ctrl*(vOH-vOL | latchDrv should stamp ctrl_out to a normalized logic level q?1:0 (line 129 ctrlTarget = q?1:0 with unit drive) so outPin maps it to the rail; remove the rail-level vOH/vOL drive from the latch driver and let outPin own t | high |
| hotload_vOL_changes_output_voltage_when_low | `src/components/active/timer-555-latch-driver.ts:129` | Same defect as vOH: latch driver feeds rail-level ctrl_out into outPin whose ctrl input must be normalized [0,1]. In the low state q=0 the latch stamps ctrl_out toward vOL but the comparator-fight / outPin re-mapping lea | Normalize ctrl_out to q?1:0 so outPin produces vOL when ctrl=0; let outPin own vOL via its vLowRail source rather than the latch driver double-driving the rail. | high |
| hotload_vDrop_changes_dis_base_voltage | `src/components/active/timer-555.ts:56` | Verified via circuit_dc_op: t:DIS stays pinned at ~5V (VCC) regardless of disBase. disBase correctly tracks vDrop at build time (fresh build vDrop=3 gives node disBase=3.0V, vDrop=1.5 gives 1.5V) yet t:DIS is 5.0V in bot | The discharge BJT must saturate and pull DIS toward GND when the base is driven to vDrop. Investigate the bjtDis NpnBJT spice-model area/IS defaults and the disBase clamp interaction (latch-driver.ts:123-126 G_base=1 Nor | medium |
| hotload_transconductance_changes_output_voltage | `src/components/active/vccs.ts:162` | VCCSAnalogElement.setParam is an empty no-op (params underscored, no body), so setComponentProperty(transconductance, 0.02) is silently dropped and V(out+) stays at -1V instead of -2V. The element drives current from a p | Implement setParam so key transconductance (and gain/m) rebuilds the linear expression / effectiveGm = transconductance*m used in stampOutput, mirroring how other controlled sources hot-reload; update the cached expressi | high |
| clock_stays_high_no_trigger | `src/components/flipflops/monoflop.ts:163` | prevClock state slot initializes to 0. When C is set high before any step, the first sampleMonoflop sees clock=1 and prevClock=0 and the rising-edge test (clock!==0 && prevClock===0) fires a spurious trigger, so Q=1 inst | Seed prevClock from the actual initial clock level so a pre-existing high does not register as a rising edge (e.g. initialize prevClock=clock on the first sample, or have the engine prime prevClock from initial inputs be | high |
| dataBits_8_truncates_packed_d_low_byte_dataBits_24_preserves_full | `src/components/graphics/graphic-card.ts:348` | executeGraphicCard packs the D output (addr<<0 \| str<<16 \| clk<<17 \| ld<<18 \| bank<<19 = 983045 = 0xF0005) into the output slot with no mask to the dataBits bus width. dataBits=8 must truncate to the low byte (0xFF - | Mask the packed output to the data bus width: read dataBits via layout.getProperty(index, dataBits) and AND the result with ((1<<dataBits)-1) (handle dataBits>=32 as full width) before writing state[wt[outputIdx]]. | high |
| hotload_Frequency_changes_breakpoint_schedule | `src/components/io/clock.ts:306` | AnalogClockElementImpl.setParam is an empty no-op so setComponentProperty(Frequency, 4000) is dropped; _halfPeriod stays at 0.5ms (1kHz). At t=0.2ms the element still reports the high half (3.3V) instead of the 4kHz low  | Make _halfPeriod mutable and implement setParam so key Frequency recomputes _halfPeriod = 1/(2*value) (and vdd updates _vdd). nextBreakpoint/instantaneousValue read _halfPeriod so the new schedule takes effect immediatel | high |
| hotload_vdd_changes_out_voltage | `src/components/io/clock.ts:306` | AnalogClockElementImpl.setParam is an empty no-op so setComponentProperty(vdd, 5) is dropped and V(out) stays at 3.3V. Compounding it, _vdd and _halfPeriod are declared readonly (clock.ts:260-261) so they cannot be mutat | Make _vdd (and _halfPeriod) mutable and implement setParam: key vdd updates _vdd, key Frequency recomputes _halfPeriod = 1/(2*value). The instantaneousValue/breakpoint math already reads these fields so the change become | high |
| jump_then_increment_continues_from_loaded_value | `src/components/memory/program-counter.ts:240` | Fixture builds the ProgramCounter at bitWidth:1 (test line 77) so mask=1; incrementing the loaded value 1 gives ((1+1)>>>0)&1 = 0, not 2. The increment+mask at line 240 is correct for a 1-bit counter; value 2 is unrepres | Fixture/test bug: build the PC at bitWidth>=2 so Q=2 is representable. Source line 240 is correct ngspice-style modular increment. | high |
| hotload_rBlown_changes_blown_output_voltage | `src/components/passives/analog-fuse.ts:323` | createAnalogFuseElement (and buildAnalogFuseElement constructor) never reads the blown property to seed _intact, which is hardcoded true at line 103. Building with blown:true still uses _rCold (0.01), so the fuse is cond | Seed _intact from props.blown: add a blown param to AnalogFuseElement constructor (initializing _intact = !blown) and pass props.getOrDefault blown false through buildAnalogFuseElement and createAnalogFuseElement so a fu | high |
| dcop_dc_current_through_cap_equals_v_over_esr_plus_rleak | `src/components/passives/capacitor.ts:641` | Test reads getElementPinCurrents on the cap:cBody Capacitor leaf expecting the DC leakage current V/(esr+rLeak)=2e-7A, but the Capacitor leaf returns CCAP (companion current) which is 0 at the DC operating point per capl | Test targets wrong leaf: probe cap:rLeak (or cap:rEsr) for DC series current, not cap:cBody. Source is correct; if a total-component current is wanted, read it off the wrapper or a resistive leaf. | high |
| hotload_capacitance_changes_first_step_current | `src/components/passives/capacitor.ts:491` | Capacitor.setParam(capacitance) updates only _nominalC; the stamped capacitance this.C is recomputed solely in computeTemperature() (capacitor.ts:340/432), which a bare coordinator.step() after setComponentProperty never | In capacitor.ts setParam, after setting _nominalC recompute this.C = _nominalC * _SCALE (or have the engine re-run computeTemperature on any param change) so capacitance is truly hot-loadable. | high |
| hotload_leakageCurrent_changes_dc_steady_state_current | `src/components/passives/capacitor.ts:641` | Same root cause as the dc-current test: iBefore is read via getElementPinCurrents on cap:cBody, which returns CCAP=0 at the DC operating point (capacitor.ts:641, capload.c:80-81). The toBeCloseTo(2e-7,8) against the cBod | Read DC current off cap:rLeak not cap:cBody. Also make rLeak resistance a function-form re-derivable binding so leakageCurrent/voltageRating hot-load updates R_leak (polarized-cap.ts:200). | high |
| hotload_M_scales_dc_steady_state_current | `src/components/passives/capacitor.ts:641` | iBefore and iAfter are both read via getElementPinCurrents on cap:cBody, which returns CCAP=0 at DC (capacitor.ts:641, capload.c:80-81). The ratio Math.abs(iAfter)/Math.abs(iBefore) becomes 0/0 = Infinity, failing toBeCl | Read DC current off cap:rLeak (the conduction path that M scales at DC), not the open-circuited cap:cBody body. | high |
| hotload_voltageRating_changes_dc_steady_state_current | `src/components/passives/capacitor.ts:641` | iBefore read via getElementPinCurrents on cap:cBody returns CCAP=0 at DC operating point (capacitor.ts:641, capload.c:80-81); the toBeCloseTo(2e-7,8) fails. The DC current is in cap:rLeak. Additional latent bug: voltageR | Probe cap:rLeak for DC current. Make rLeak resistance re-derivable on voltageRating/leakageCurrent change (polarized-cap.ts:200). | high |
| hotload_frequency_changes_transient_response | `src/components/passives/crystal.ts:195` | QuartzCrystal is a kind:netlist composite. buildCrystalNetlist derives Ls and Rs from frequency/qualityFactor/motionalCapacitance ONCE at compile time and emits frozen leaf primitives (rS/lS/cS/c0). setComponentProperty( | Add a setParam path on the composite that recomputes Ls/Rs (crystalMotionalInductance/crystalSeriesResistance) and delegates resistance/inductance/capacitance into the rS/lS/cS/c0 leaves on hot-load. | high |
| hotload_motionalCapacitance_changes_transient_response | `src/components/passives/crystal.ts:195` | Same composite-freeze bug: motionalCapacitance feeds both Ls (lS leaf) and Cs (cS leaf) at buildCrystalNetlist compile time. kind:netlist composite has no runtime recompute, so hot-load never reaches the leaves; before== | Composite setParam(motionalCapacitance) must recompute Ls and delegate inductance into lS and capacitance into cS leaves. | high |
| hotload_qualityFactor_changes_transient_response | `src/components/passives/crystal.ts:195` | Same composite-freeze bug: qualityFactor only feeds Rs at buildCrystalNetlist compile time. kind:netlist composite has no runtime setParam recompute, so hot-loading qualityFactor never reaches the rS leaf resistance; tra | Composite must recompute Rs=crystalSeriesResistance(f,Ls,Q) and delegate into the rS leaf on setParam(qualityFactor). | high |
| hotload_shuntCapacitance_changes_transient_response | `src/components/passives/crystal.ts:195` | Same composite-freeze bug: shuntCapacitance is C0 (c0 leaf) set at buildCrystalNetlist compile time. kind:netlist composite has no runtime recompute path, so hot-load never reaches the c0 leaf capacitance; before==after. | Composite setParam(shuntCapacitance) must delegate capacitance into the c0 leaf. | high |
| hotload_IC_seeds_uic_initial_branch_current | `src/components/passives/inductor.ts:685` | setParam(IC) only stores this._IC=value. The IC flux seed s0[PHI]=L/m*IC fires solely in loadFluxInit under MODEUIC+MODEINITTRAN (line 780-783), which is the first transient step already consumed by buildFixture warm-sta | In setParam(IC), when stateBase/pool are live, seed s0[PHI]=s1[PHI]=(_effectiveL/_M)*value (mirror the memristor initialState setParam that writes both s0 and s1). | high |
| dcop_resistive_divider_at_initial_state | `src/components/passives/memristor.ts:226` | After buildFixture warm-start (one transient step) the last-accepted W lives in s1 because rotateStateVectors at the top of the next step recycles states[0]. The second dcOperatingPoint() then re-enters DC mode where loa | In DC mode the memristor must read the last-accepted W from s1 (like the inductor), not s0. Reading s0 only holds the seed on the FIRST load before any rotation; after a warm-start rotation the valid value is in s1. Reso | medium |
| init_w_seeded_to_initialState_after_warm_start | `src/components/passives/memristor.ts:259` | buildFixture warm-start runs DCOP then ONE transient step. That transient step executes the W integration at load() line 259 (s0[W]=wNew with dw*dt nonzero), drifting W from the seeded 0.5 to 0.5000000834. The test reads | Either skip the W integration on the first transient step (MODEINITTRAN) so the seeded initialState survives one step, or have the test assert s1[W] (last-accepted seed) rather than s0[W] post-warm-start. The seed-surviv | medium |
| init_w_slot_seeded_from_initial_state_edge | `src/components/passives/memristor.ts:259` | Same root cause as the mid case. buildFixture (build-fixture.ts:137) runs DCOP plus one transient step; in that step memristor load() non-DC branch writes integrated wNew into state0[W] at line 259, overwriting the seede | Read fix.pool.state1[mem._stateBase+SLOT_W] for the pristine last-accepted seed, or assert state0[W] equals seed plus one integration step. Line 259 (ngspice CKTstate0 idiom) is correct; the state0-holds-verbatim-seed pr | high |
| init_w_slot_seeded_from_initial_state_mid | `src/components/passives/memristor.ts:259` | buildFixture warm-start runs DCOP plus one transient step (build-fixture.ts:137 then engine first step). In that transient step memristor load() takes the non-DC branch and at line 259 writes the integrated wNew into sta | Assert the seed against fix.pool.state1[mem._stateBase+SLOT_W] (last-accepted, pre-first-step) instead of state0[W], or relax to verify state0[W] is the seed plus exactly one integration step. The source line 259 is corr | high |
| hotload_esr_changes_initial_step_current | `src/components/passives/polarized-cap.ts:641` | Test reads getElementPinCurrents on cap:cBody (the capacitor body) expecting it to track V/esr. esr does hot-load correctly (Resistor.setParam calls updateConduct at resistor.ts:595), but the cBody companion current (CCA | Read the ESR-limited current off cap:rEsr (the resistor in the series path), not the cap:cBody companion current, and read it at the first transient step before charging dominates. | medium |
| hotload_CJO_changes_dynamic_response | `src/components/semiconductors/__tests__/schottky.test.ts:156` | The test reads node D1:A which equals the ideal source node v1:pos (clamped to 0.5V), so before and after are both exactly 0.5 regardless of CJO. Even under transient excitation the anode node is pinned by the ideal volt | Test-design bug: read a non-source-clamped node (cathode D1:K or the internal prime) to observe the CJO-dependent transient. The diode cap path (diode.ts computeJunctionCapacitance/Charge) is correct. | high |
| hotload_RS_changes_anode_voltage | `src/components/semiconductors/__tests__/schottky.test.ts:138` | The test reads node D1:A which is wired directly to the ideal voltage source v1:pos (buildSchottkyForward connection v1:pos -> d1:A). That node is clamped to V1 voltage (0.5V) by the ideal source regardless of the diode  | Test-design bug: assert on the diode internal voltage drop (V(D1:A)-V(D1:K)) or the cathode node, not the source-clamped anode node. Diode source RS path is correct. | high |
| computeTemperature_per_instance_override_wins_over_ambient | `src/components/semiconductors/diode.ts:1388` | vfAfter (0.1081312) differs from vfOverride (0.1081302) by 1.0076e-6, just over the 5e-7 (6-digit) tolerance. The per-instance TEMP=450 override IS held correctly: setParam(TEMP) sets _tempGiven=true (diode.ts:1385) and  | Borderline NR-tolerance gap, not a temperature-override bug. Either re-run a full DCOP before reading vfAfter, or the single step settles within reltol*Vf+vAbsTol (1e-6 here) - which exceeds the 6-digit assertion. Overri | medium |
| hotload_BV_changes_reverse_breakdown_vd | `src/components/semiconductors/diode.ts:1379` | BV hot-load does not propagate into the diode breakdown behavior. MCP evidence: building the same reverse-bias circuit with BV=5 at compile time gives anode=4.958V (breakdown engages); building with default BV=Inf then c | Make the diode setParam(BV,...) path re-establish the breakdown regime: ensure the finite-BV branch flags/seed used at setup (and the warm-start junction voltage) are refreshed so the next solve re-enters the breakdown a | medium |
| hotload_RS_changes_anode_voltage_forward | `src/components/semiconductors/diode.ts:839` | The RS series-resistance internal prime node is allocated only when RS != 0 and only at setup() time (diode.ts:839-844). With varactor default RS=0 no internal node exists so _posPrimeNode == posNode. Hot-loading RS=50 v | Always allocate the RS internal prime node at setup() regardless of RS value (or re-run topology setup on RS hot-load), so a nonzero RS loaded later actually inserts the series drop. Match diosetup.c node creation but ma | high |
| hotload_RS_changes_vd | `src/components/semiconductors/diode.ts:839` | The diode series-resistance internal prime node is allocated in setup() ONLY when params.RS != 0 (line 839-843: RS==0 sets _posPrimeNode = posNode = anode, no internal node). The fixture builds the diode with default RS= | Series-resistance RS is a topology-affecting param (needs an internal prime node). Hot-loading RS from 0 to nonzero cannot be honored by setParam alone; either always allocate the internal prime node and gspr stamps rega | high |
| limiting_pnjlim_fires_on_clamp_diode_under_reverse_bias | `src/components/semiconductors/diode.ts:1258` | events.length>=1 passes (line 573); the failing assertion is line 574 .some(e.junction===VD \|\| e.junction.startsWith(V)). The diode _recordLimit hardcodes junction:AK (diode.ts:1258), an anode-cathode label that neithe | Make _recordLimit emit the junction name VD (matching the DIOvoltage state-slot name at diode.ts:68 and ngspice diodefs.h:196) instead of the literal AK, so limiting events are identified by the same junction label the r | high |
| hotload_GAMMA_changes_vd | `src/components/semiconductors/mosfet.ts:1375` | Body tied to source (vbs=0, setup line 909) makes the GAMMA body-effect contribution cancel: von=tVbi*polarity+GAMMA*sarg where tVbi (mos1temp.c:170-176, computed line 427) contains -polarity*GAMMA*sqrt(PHI); at vbs=0 an | Source matches ngspice. The test must apply a nonzero source-body bias (Vbs!=0) to expose GAMMA; with body=source GAMMA legitimately has no OP effect. No source line change is correct. | high |
| hotload_PHI_changes_vd | `src/components/semiconductors/mosfet.ts:1375` | For the spice-l1 fixture GAMMA defaults to 0 and the body is tied to source (vbs=0, setup line 909). With GAMMA=0 the von threshold computation von=tVbi*polarity+GAMMA*sarg (mos1load.c:507) has no PHI dependence at nomin | Source is ngspice-correct (mos1load.c von formula). The test needs a fixture with nonzero GAMMA and a nonzero Vbs so PHI feeds the body-effect term; otherwise PHI has no OP effect. No source line change is correct. | high |
| init_seeded_op_point_slots | `src/components/semiconductors/triode-analog-element.ts:311` | Cached VPK slot (247.22459) differs from the final converged node difference vP-vK (247.22386) by 0.73 mV vs the 5e-10 tolerance. load() writes s0[SLOT_VPK]=vpkNew where vpkNew=vP-vK reads ctx.rhsOld (line 263) - the PRE | This is correct SPICE-parity caching - the slot is meant to lag by one iteration. The test expectation at line 98 (cached VPK == final node difference to 5e-10) is wrong; loosen it to the NR convergence tolerance or comp | medium |
| computeTemperature_ambient_propagates_to_vf | `src/components/semiconductors/zener.ts:230` | Same root cause as hotload_TEMP_changes_vf_forward but via the engine-driven computeTemperature path (setCircuitTemp(400) -> computeTemperature -> _computeZenerTp at zener.ts:525-529). _computeZenerTp (zener.ts:230-248)  | Same fix as the hotload variant: derive tIS in _computeZenerTp per diotemp.c:175-185 and use it in all current/conductance branches in load(). One change fixes both zener temperature tests. | high |
| hotload_TEMP_changes_vf_forward | `src/components/semiconductors/zener.ts:230` | _computeZenerTp (zener.ts:230-248) recomputes the temperature-scaled thermal voltages vt nVt nbvVt and tBV but never derives a temperature-scaled saturation current (ngspice DIOtSatCur diotemp.c:175-185 = IS * exp((T/TNO | Add a tIS (DIOtSatCur) derivation to _computeZenerTp mirroring diotemp.c:175-185 (scale IS by exp((T/TNOM-1)*EG/vt)*(T/TNOM)^(XTI/N)) and use tIS instead of params.IS everywhere current/conductance is computed in load()  | high |
| hotload_temperature_changes_resistance_in_fixed_mode | `src/components/sensors/ntc-thermistor.ts:298` | getPinCurrents (ntc-thermistor.ts:292-304) unconditionally reads tOld from the TEMPERATURE pool slot states[1][SLOT_TEMPERATURE] (line 298-300) instead of mirroring load()s non-self-heating branch which uses the live _tA | In getPinCurrents branch on _selfHeating like load() does: when !_selfHeating use this._tAmbient for tOld (and computeRFromT) so a hot-loaded temperature is reflected; only read the TEMPERATURE slot in self-heating mode. | high |
| hotload_iHold_above_steady_current_extinguishes_gap | `src/components/sensors/spark-gap.ts:226` | CORRECTION (supersedes prior medium record): direct probe confirms load() recomputes the latch and writes s0[CONDUCTING] every step but state1[CONDUCTING] never updates from its boot value. The discrete CONDUCTING latch  | Make the CONDUCTING slot written to s0 in load() reach state1 each accepted step (boot DCOP seeds s1 so init tests pass; the transient per-step commit does not). Ensure rotation/seed carries this latch slot so conducting | high |
| hotload_iHold_raise_above_steady_current_extinguishes_gap | `src/components/sensors/spark-gap.ts:226` | Reproduced with a direct probe: after hot-load load() correctly recomputes the latch and writes s0[CONDUCTING] every step (probe shows s0=1 firing on every step) but state1[CONDUCTING] stays frozen at the boot value 0 ac | Ensure the CONDUCTING slot written to s0 in load() is committed to state1 each accepted step the same way the boot DCOP seed reaches s1 (the init test relies on _seedFromDcop copying s0->s1). Investigate whether a predic | high |
| hotload_vBreakdown_drop_below_vsrc_fires_blocking_gap | `src/components/sensors/spark-gap.ts:226` | Reproduced with a direct probe (Vsrc=500 vBreakdown lowered 1000->200): setParam reaches the SparkGapElement (_vBreakdown becomes 200) and load() fires the gap writing s0[CONDUCTING]=1 on EVERY step but state1[CONDUCTING | Same fix: make the CONDUCTING slot written to s0 in load() reach state1 on each accepted step. The boot DCOP path seeds s1 (init tests pass) but the per-step transient commit does not carry the latch; ensure rotation/see | high |
| hotload_vBreakdown_lower_below_source_fires_gap | `src/components/sensors/spark-gap.ts:226` | CORRECTION (supersedes prior medium record): direct probe of the equivalent fire scenario confirms setParam reaches the element (_vBreakdown updates) and load() writes s0[CONDUCTING]=1 firing the gap every step but state | Make the CONDUCTING slot written to s0 reach state1 each accepted step. Boot DCOP seeding works (init tests pass); the transient commit does not. Ensure rotation/seed propagates this latch slot so the next-step conductin | high |
| am_mode_diverges_from_pure_sine_after_warmstart | `src/components/sources/ac-voltage-source.ts:498` | The AM evaluation at ac-voltage-source.ts:498 (dcOffset + (1+depth*sin(2pi*modFreq*t))*amplitude*sin(arg)) is mathematically correct and matches the documented contract. buildFixture takes exactly ONE warm-start step (bu | This is a test-expectation issue: either step the fixture to a non-trivial t (e.g. a few modulation periods, t ~ 1/modFreq) before reading, or relax the AM divergence assertion to the achievable magnitude (~1e-7) / asser | medium |
| dcop_gate_low_drives_v_s_near_vs | `src/components/switching/behavioral-fet-driver.ts:135` | PFET gate-ON (vg=0) DCOP V(S) is the Roff value (~1e-6V) not 1000/1001V because the discontinuous vGS=V(G)-V(S) PFET threshold oscillates against the source-follower and the channel does not converge ON. | Classify the PFET gate level on V(G) vs a fixed reference (or add continuation smoothing); same driver fix as NFET gate-on. | high |
| dcop_gate_on_drives_v_s_near_vs | `src/components/switching/behavioral-fet-driver.ts:135` | BehavioralFETDriver.load classifies on a hard discontinuous threshold on vGS=V(G)-V(S); in a source-follower V(S) is the load output so turning on raises V(S) which drops vGS below Vth flipping the driver off again - the | Classify the gate logic level on V(G) against a fixed logic reference (or add hysteresis/continuation smoothing) instead of the source-relative vGS so the source-follower feedback does not create a bistable discontinuity | high |
| hotload_Ron_drops_v_s_under_load | `src/components/switching/behavioral-fet-driver.ts:135` | PFET baseline gate-ON V(S) is wrong (~1e-6V Roff value not 1000/1001V) before the Ron hot-load because the discontinuous vGS=V(G)-V(S) PFET threshold oscillates and the source-follower channel never converges ON. | Classify the PFET gate level on V(G) vs a fixed reference (or add continuation smoothing); the Ron hot-load assertion is downstream of this seed value. | high |
| hotload_Vth_above_gate_drive_isolates_channel | `src/components/switching/behavioral-fet-driver.ts:135` | NFET baseline gate-ON V(S) is wrong (~5e-7V not 1000/1001V) before the Vth hot-load because the discontinuous vGS=V(G)-V(S) driver threshold oscillates and the source-follower channel never converges ON. | Classify gate level on V(G) vs a fixed reference (or add continuation smoothing) so the ON baseline converges; same root cause as the other gate-on NFET cases. | high |
| hotload_Vth_inverts_channel_state | `src/components/switching/behavioral-fet-driver.ts:135` | PFET baseline gate-ON V(S) is wrong (~1e-6V not 1000/1001V) before the Vth hot-load because the discontinuous vGS=V(G)-V(S) PFET threshold oscillates and the source-follower channel never converges ON. | Classify the PFET gate level on V(G) vs a fixed reference (or add continuation smoothing); same root cause as the other gate-on PFET cases. | high |
| init_post_warm_start_node_voltage_pass_through_seed | `src/components/switching/behavioral-fet-driver.ts:135` | PFET gate-ON (vg=0) channel never converges ON - the same discontinuous vGS=V(G)-V(S) threshold (PFET arm vGS<-Vth) oscillates against the source-follower so V(rload:pos) stays at the Roff value (~1e-6V) not 1000/1001V. | Classify the PFET gate level on V(G) vs a fixed reference (or add continuation smoothing) instead of source-relative vGS; same driver fix covers both polarities. | high |
| dcop_paired_dt_energised | `src/components/switching/current-controlled-switch.ts:311` | The DC-OP node voltage at rLoadC:pos is 0.9998 (ours) vs 0.9999 (ngspice); harness_first_divergence localizes to the dcopInitFloat contact-path conductance being 100 (ours, one contact closed) vs 200 (ngspice, both conta | This is an architectural divergence to escalate, not a numerical bug: the normallyClosed inversion in current-controlled-switch.ts:311 has no ngspice CSW counterpart and the ON keyword at netlist-generator.ts:905 is not  | high |
| dcop_paired_ac_sinusoid | `src/solver/analog/analog-engine.ts:1091` | harness_run on the tapped-transformer ac-sinusoid fixture: our engine produces only 1 step then errors Our engine did not advance at step 0 (simTime=0 lastDt=0) with 49 NR iterations vs ngspice 1009 steps avg 2.26 iters. | Trace why _params.firstStep (resolveSimulationParams) is 0 for these transformer transient runs so the first-step dt feeds a nonzero ag0; the inductor/mutual stamp code is ngspice-faithful and is not the bug. Engine firs | medium |
| dcop_paired_ac_step_down | `src/solver/analog/analog-engine.ts:1082` | Harness on transformer-canon-ac-step-down.dts shows our engine runs only 1 step then stalls (lastDt=0, did not advance) while ngspice runs 1009 steps. Our DCOP limps through 11 gminDynamic + 11 srcSweep attempts ALL acce | Make the transient first-step seeding (currentDt=firstStep and _seedFromDcop) robust to the transformer coupled-inductor DCOP path so MODEINITTRAN and a nonzero first dt are established; root the upstream DCOP convergenc | medium |
| lte_rollback_q_slot_rotation_invariant | `src/solver/analog/analog-engine.ts:681` | Test enables the convergence log via coordinator.setConvergenceLogEnabled(true) (coordinator.ts:482-491) AFTER buildFixture warm-started with logging off, then runs 200 coordinator.step() calls; getConvergenceLog() retur | Confirm via convergence log whether the engine errors on this fixture; if it errors, ensure the error-exit path records a StepRecord so the log is non-empty, or relax the fixture stiffness. If it does not error, audit wh | low |
| lte_rollback_state_invariant_after_rejection | `src/solver/analog/analog-engine.ts:625` | On LTE rejection the engine (analog-engine.ts:625-636) only rewinds simTime and sets dt=newDt; it deliberately does NOT restore the state pool (per the ngspice dctran.c:935-954 comment at line 626-629). So after a reject | Either the rejection path must restore state pool s0 from s1 before the retry to honor the asserted invariant, or the test invariant is wrong (ngspice intentionally does not restore state on LTE reject). Escalate: this i | medium |
| Loaded: setComponentProperty(dipl, cIn, 1e-7) slows RC settling — binding is observable in transient | `src/solver/analog/behavioral-drivers/bridge-input-driver.ts:124` | setParam(cIn) updates only this._spec.cIn but never the child AnalogCapacitorElement which holds its own capacitance baked at construction (line 62) and is what load() actually stamps (line 100-102). So hot-loading cIn=1 | In setParam case cIn forward the value to the child cap: this._capChild?.setParam(capacitance, value) (and reconcile when _capChild is null but value becomes >0). The spec field alone is not what gets stamped. | high |
| dcop_blocking_current_below_one_milliamp | `src/solver/analog/compiler.ts:517` | Same root cause: composite diode sub-elements get ISR/NR model defaults marked given so the recombination current arm (diode.ts:1031) is active. In the blocking regime this adds spurious conduction (observed 18.5mA, expe | Stop marking model-registry default params given for composite sub-elements at compiler.ts:516-518. | high |
| dcop_blocking_vs_triggered_anode_current_ordering | `src/solver/analog/compiler.ts:517` | Same root cause. iOn (7.74e-12) is not greater than iOff (8.0e-12) because the triggered SCR fails to conduct - both regimes sit near leakage. The spurious colPrime node per sub-BJT (RCO default falsely marked given duri | Fix the leaf-default givenness at compiler.ts:515-518 so rcoGiven is false for SCR sub-BJTs; restoring correct MNA topology lets the triggered regime conduct more than blocking. | high |
| dcop_breakover_conducts_above_threshold | `src/solver/analog/compiler.ts:517` | Same root cause: composite diode sub-elements wrongly mark ISR/NR (and other) model defaults as given so the recombination arm overflows to Infinity at the INITJCT critical voltage (diode.ts:1031, exp(vd/(NR*vt)) with vd | Stop marking model-registry default params given for composite sub-elements at compiler.ts:516-518; only explicit overrides are given. | high |
| dcop_breakover_symmetric_under_polarity_flip | `src/solver/analog/compiler.ts:517` | Same root cause: composite diode sub-elements mark model-default ISR/NR as given so the recombination arm overflows at INITJCT vd=tVcrit (diode.ts:1031) producing NaN and non-convergence. The converged assertion at line  | Stop marking model-registry default params given for composite sub-elements at compiler.ts:516-518. | high |
| dcop_gated_on_conducts | `src/solver/analog/compiler.ts:517` | Triac never latches: harness shows ours V_MT2=4.99999999924 (full source, I=7.6e-12 A) vs ngspice latched 0.909 V. Cause is the same default-seeding-givenness bug: every BJT sub-element gets rcoGiven=true (RCO default 0. | Fix compiler.ts:517 to seed leaf model defaults without marking them given so rcoGiven is false for the triac BJTs; the Kull epi node disappears and the latch can regenerate. No change needed in triac.ts or bjt.ts setup  | high |
| dcop_paired_active | `src/solver/analog/compiler.ts:517` | Same composite default-given leak. compiler.ts:517 setModelParam copies the BJT spice default RCO=0.01 and marks it given, so rcoGiven (bjt.ts:1549) is true and the BJT allocates the Kull colPrime internal node (bjt.ts:1 | Stop marking leaf-default model params as given in composite expansion so rcoGiven matches the ngspice-emitted deck and the BJT does not split the quasi-sat node. | high |
| dcop_paired_active | `src/solver/analog/compiler.ts:517` | cv.withinTol is false at step-end node compare: harness DCOP shows our engine fails to converge while ngspice converges, first divergence at step0 iter0 worstLabel tx:bjtPhoto:collector absDelta 17.5V. Root cause is the  | Set leaf-entry default params not-given (replaceModelParams markGiven:false); only subEl.params overrides given. Fix compiler.ts:517 and the twin at compiler.ts:295. | high |
| dcop_paired_blocking | `src/solver/analog/compiler.ts:517` | Composite sub-element PropertyBag is seeded from leaf model defaults via subProps.setModelParam(k,v), which marks every default-valued param as GIVEN (properties.ts:164 adds to _mparamsGiven). The BJT sub-elements theref | Seed leaf-entry default params without marking givenness - add a setModelParamDefault path (or seed _mparams without _mparamsGiven) so only params explicitly listed in subEl.params/subcktParams count as given. Then rcoGi | high |
| dcop_paired_blocking | `src/solver/analog/compiler.ts:517` | Same root cause as the SCR limiting test. Composite expansion at compiler.ts:515-518 marks BJT default RCO=0.01 as given, so each sub-BJT allocates an extra Kull QS colPrime node (rcoGiven gate bjt.ts:1549/1753). Harness | Do not mark leaf-entry default params as given during composite expansion. Only explicit subEl.params overrides should set givenness. Fix the defaults loop at compiler.ts:515-518. | high |
| dcop_paired_blocking | `src/solver/analog/compiler.ts:517` | Same root cause confirmed via harness on diac-canon-blocking.dts: our engine produces NaN node voltages and fails to converge (2300 iters, 0 converged) while ngspice converges in 5. Composite diode sub-elements mark ISR/ | Stop marking model-registry default params given for composite sub-elements at compiler.ts:516-518. | high |
| dcop_paired_breakover | `src/solver/analog/compiler.ts:517` | Same root cause as dcop_paired_blocking: composite diode sub-elements mark model-default ISR/NR as given, activating the recombination arm (diode.ts:1031) which overflows at INITJCT vd=tVcrit producing NaN and non-conver | Stop marking model-registry default params given for composite sub-elements at compiler.ts:516-518. | high |
| dcop_paired_gated_on | `src/solver/analog/compiler.ts:517` | Same root cause as dcop_paired_blocking: composite sub-element bag seeds leaf defaults via setModelParam which marks them given, so BJT rcoGiven becomes true and each of the 4 BJTs allocates a spurious Kull quasi-saturat | Seed leaf default params without setting givenness so rcoGiven (and rcGiven etc) evaluate false for params the triac netlist does not pass. Same fix as compiler.ts:517 default-seeding givenness bug. | high |
| dcop_paired_low | `src/solver/analog/compiler.ts:517` | Identical to dcop_paired_active on the low-drive fixture. The BJT spice default RCO is copied as given by compiler.ts:517 setModelParam, turning on rcoGiven (bjt.ts:1549) and the Kull colPrime node split (bjt.ts:1756). T | Same fix: do not mark leaf-default model params as given during composite expansion. | high |
| dcop_paired_low | `src/solver/analog/compiler.ts:517` | Same root cause on the low-drive .dts fixture: cv.withinTol false because our DCOP diverges from ngspice. The optocoupler NpnBJT leaf gets its default RCO=0.01 marked given by composite expansion (compiler.ts:517 setMode | Use replaceModelParams markGiven:false for leaf-entry defaults so defaulted RCO is not given; keep only explicit subEl.params overrides given. Apply to compiler.ts:517 and compiler.ts:295. | high |
| dcop_paired_reverse_blocking | `src/solver/analog/compiler.ts:517` | Same root cause as the other dcop_paired triac tests: composite expansion seeds leaf BJT defaults via setModelParam marking them given, so rcoGiven=true and each BJT allocates a spurious Kull quasi-saturation epi node+br | Seed leaf default params without marking givenness in expandCompositeInstance so unspecified gate params (RCO, RC, etc) default to ungiven and no QS node is allocated. | high |
| dcop_paired_triggered | `src/solver/analog/compiler.ts:517` | Same root cause. Triggered fixture harness: ours matrixSize 11 vs ngspice 9 (+2 eqs from spurious per-BJT colPrime collector node), all 107 step-ends fail withinTol. BJT junction conductances missing (matrix cells 1e-12/ | Stop marking leaf-entry defaults as given at compiler.ts:515-518; only subEl.params overrides should mark givenness so rcoGiven stays false for SCR sub-BJTs. | high |
| dcop_photocurrent_couples_through_phototransistor_collector | `src/solver/analog/compiler.ts:517` | Composite expansion copies every leaf-model DEFAULT param into subProps via setModelParam, which marks it as user-given. For the phototransistor BJT spice model this marks RCO (default 0.01) as given, so bjt.ts:1549 rcoG | Do not mark leaf-default model params as given during composite expansion. Use a non-given default-seeding path so isModelParamGiven only reports params explicitly set in the netlist or by the user, matching standalone p | high |
| dcop_photocurrent_couples_through_phototransistor_collector | `src/solver/analog/compiler.ts:517` | dc.converged is false: same root cause. Composite expansion marks the BJT leaf default RCO=0.01 as given (setModelParam at compiler.ts:517 always adds to _mparamsGiven), so rcoGiven=true at bjt.ts:1549 and the Kull QS br | Copy leaf model-entry defaults with markGiven:false (replaceModelParams) so defaulted RCO is not given; keep explicit subEl.params overrides marked given. Mirror fix at compiler.ts:295. | high |
| dcop_triggered_anode_voltage_drops_when_gate_biased | `src/solver/analog/compiler.ts:517` | Same root cause. Own-engine DCOP: scr:A measured 9.9999999992 (full supply) instead of <8, i.e. the SCR never latches into conduction. The spurious per-BJT colPrime node (from RCO default falsely marked given at composit | Fix givenness marking of leaf-entry defaults at compiler.ts:515-518 so SCR sub-BJTs do not allocate the Kull QS colPrime node; the latch will then conduct and pull scr:A down. | high |
| hotload_AREA_changes_blocking_anode_voltage | `src/solver/analog/compiler.ts:517` | after===before exactly (diff 0). AREA propagates to all 4 BJTs via wrapper bindings and bjt.ts recomputes tp (AREA folds into sat currents), but in deep blocking V_MT2=4.999999991 V the AREA-scaled leakage change is belo | Fix compiler.ts:517 givenness seeding and assert AREA sensitivity in a regime where leakage is observable (partial gate bias or smaller series R). setParam routing is correct. | medium |
| hotload_BF_changes_blocking_anode_voltage | `src/solver/analog/compiler.ts:517` | after===before exactly (diff 0). setParam DOES propagate: triac netlist params are string-mapped so compiler.ts:608-619 builds wrapper bindings BF->[Q1,Q3] and wrapper.setParam forwards to the BJTs which recompute tp (bj | Fix compiler.ts:517 default-seeding givenness so the BJTs are correct GP devices; additionally the test must assert in a sensitized regime (gate partially biased or a small load so leakage is observable) because a deep-b | medium |
| hotload_BR_changes_blocking_anode_voltage | `src/solver/analog/compiler.ts:517` | after===before exactly (diff 0). BR propagates to the PNP sub-elements Q2/Q4 via wrapper bindings (BR->[Q2,Q4]) and bjt.ts recomputes tp, but in deep blocking the BR-modulated leakage change on V_MT2=4.999999991 V is bel | Fix compiler.ts:517 givenness seeding; sensitize the test regime so the BR-dependent leakage is observable. setParam routing is correct. | medium |
| hotload_ctr_changes_collector_voltage | `src/solver/analog/compiler.ts:517` | Number.isFinite(after) is false after stepping with raised CTR: the underlying DCOP/step never converges because the phototransistor BJT runs the phantom Kull QS branch. Composite expansion marks leaf default RCO=0.01 as | Copy leaf model-entry defaults with markGiven:false (replaceModelParams) leaving RCO not-given; mark only explicit subEl.params overrides given. Fix compiler.ts:517 and compiler.ts:295. | high |
| hotload_IS_changes_blocking_anode_voltage | `src/solver/analog/compiler.ts:517` | after===before exactly (diff 0). IS propagates to all 4 BJTs via wrapper bindings and bjt.ts recomputes tp, but the test sets IS=1e-12 which on the deep-blocking V_MT2=4.999999991 V node moves only sub-pA leakage; the re | Fix compiler.ts:517 givenness seeding; sensitize the test so an IS change is observable at the node. setParam plumbing is correct. | medium |
| hotload_Is_raises_led_current_lowers_anode_voltage | `src/solver/analog/compiler.ts:517` | before and after anode voltages are identical (difference 0) so toBeCloseTo passes and the not.toBeCloseTo assertion fails. The optocoupler subsystem does not converge: compiler.ts:517 copies the BJT spice default RCO=0. | Fix the composite default-given leak so DCOP converges; the Is change then shifts the anode voltage. | high |
| hotload_n_changes_led_anode_voltage | `src/solver/analog/compiler.ts:517` | before equals after (difference 0) so not.toBeCloseTo fails. Same root cause: compiler.ts:517 copies the BJT spice default RCO=0.01 as given, enabling rcoGiven (bjt.ts:1549) and the Kull colPrime node split (bjt.ts:1756) | Fix the composite default-given leak so DCOP converges; the n change then shifts the anode voltage. | high |
| hotload_TEMP_changes_blocking_anode_voltage | `src/solver/analog/compiler.ts:517` | after===before exactly (diff 0). TEMP propagates through the wrapper bindings (TEMP->all 4 BJTs) and bjt.ts:2862 recomputes tp at the new temperature, but the deep-blocking V_MT2=4.999999991 V changes only via sub-pA rev | Fix compiler.ts:517 givenness seeding; and sensitize the test regime so the leakage shift is observable. setParam plumbing itself is correct. | medium |
| init_active_anode_and_collector_finite | `src/solver/analog/compiler.ts:517` | Composite expansion copies the leaf model-entry DEFAULT params (subProps.setModelParam) which marks every default key as given; the optocoupler phototransistor NpnBJT thus reports isModelParamGiven(RCO)=true (bjt.ts:1549 | Write leaf-entry default params with not-given semantics (replaceModelParams markGiven:false) so registry defaults are not flagged given; only the explicit subEl.params overrides should be marked given. Same bug at compi | high |
| init_blocking_anode_voltage_tracks_source | `src/solver/analog/compiler.ts:517` | Same root cause as init_blocking_node_voltage_near_zero: composite sub-element diode defaults ISR/NR are wrongly marked given so the recombination arm (diode.ts:1031) is active and overflows at vd=tVcrit. The 19.80 vs 20 | Do not mark model-registry default params given for composite sub-elements at compiler.ts:516-518; only explicit subEl.params overrides are given. | high |
| limiting_events_recorded_during_dcop | `src/solver/analog/compiler.ts:517` | The test asserts dc.converged is true (line 315) before checking limiting events. DCOP never converges because compiler.ts:517 copies the BJT spice default RCO=0.01 as given, turning on rcoGiven (bjt.ts:1549) and the Kul | Fix the composite default-given leak so the BJT converges; limiting events are then captured normally. | high |
| limiting_events_recorded_during_dcop | `src/solver/analog/compiler.ts:517` | dc.converged is false: same root cause. The optocoupler phototransistor NpnBJT leaf default RCO=0.01 is wrongly flagged given by composite expansion (compiler.ts:517 setModelParam marks every default key given), so rcoGi | Write leaf-entry default params not-given (replaceModelParams markGiven:false); only explicit subEl.params overrides marked given. Fix compiler.ts:517 plus the twin in the node-allocation pass at compiler.ts:295. | high |
| limiting_pnjlim_fires_on_q1_or_q2_junction_during_dcop | `src/solver/analog/compiler.ts:517` | Composite expansion writes BJT leaf-entry DEFAULTS (including RCO=0.01) via subProps.setModelParam which marks every key GIVEN. The BJT setup gate rcoGiven=isModelParamGiven(RCO) (bjt.ts:1549) then becomes erroneously tr | Write leaf-entry defaults WITHOUT marking givenness (registry-default semantics per properties.ts:168-169). Only subEl.params overrides at compiler.ts:520-547 should mark given. Use replaceModelParams without markGiven f | high |
| dcop_paired_reverse_bias | `src/solver/analog/ngspice-load-order.ts:0` | harness_first_divergence earliest = matrix cell (nCap,nCap) at step0/iter0, classification value-only, single cell, absDelta=1.78e-15 (1 ULP). The nCap diagonal accumulates three += stamps: rEsr.G + rLeak.G + cBody.geq.  | Align the per-leaf load() walk order for the PolarizedCap composite so rEsr/rLeak/cBody stamp the nCap diagonal in the same sequence ngspice loads them (RESload before CAPload); verify expandCompositeInstance/ngspiceLoad | medium |
| lte_rollback_state_invariant | `src/solver/analog/timestep.ts:616` | The test asserts an LTE rejection MUST occur for the square-wave RL (L=1e-3,R=1,500Hz,5ms,free-running maxStep), then reads it. No step in the convergence log has lteRejected===true so the lookup returns undefined and to | Test-precondition gap, not a source bug: shouldReject and cktTerr match ngspice. To exercise the rollback path the fixture needs sharper flux gradients or smaller minTimeStep so worstRatio exceeds 1.111, or the test must | medium |
| hotload_rOut_changes_output_divider | `src/solver/coordinator.ts:716` | Two stacked bugs: (1) schmitt rOut mapping {R: rOut} never binds (Resistor key is resistance) so baseline rOut is the 1000 ohm default giving before=2.9999997; (2) setComponentProperty(st, rOut, 5000) routes to a single  | Fix the schmitt rOut param key to resistance (schmitt-trigger.ts:83) and fan composite hot-load out to the matching sub-element via a one-to-many elementToCircuitElement map and per-sub-element param keys. | high |
| hotload_vIH_shifts_threshold_so_3p3v_reads_low | `src/solver/coordinator.ts:915` | setComponentProperty routes a parent hot-load to only ONE flattened analog sub-element. _resolveElementIndex (coordinator.ts:712-719) builds a Map keyed by CircuitElement, so for a composite all sub-elements collapse to  | Make setComponentProperty fan out the param to ALL analog sub-elements whose CircuitElement is the target (iterate elementToCircuitElement collecting every idx for the element, call setParam on each), so composite-nested | high |
| hotload_vOH_changes_output_high_level | `src/solver/coordinator.ts:716` | setComponentProperty(st, vOH, 5.0) on the composite resolves to a single reverse-mapped sub-element (last wins = Capacitor, not drv) so the driver vOH is never updated; output barely moves (3.0294 vs expected 4.975). The | Fan composite hot-load out to all sub-elements and route vOH to the driver; build a one-to-many elementToCircuitElement map. Also fix schmitt rOut mapping at schmitt-trigger.ts:83. | high |
| hotload_vOL_changes_output_low_level | `src/solver/coordinator.ts:716` | setComponentProperty(st, vOL, 0.4) on the composite resolves to a single reverse-mapped sub-element (last wins = Capacitor, not drv), so the driver vOL is never updated; output stays near 0 (0.00456) instead of 0.398. | Fan composite hot-load out to all sub-elements so vOL reaches the driver; build a one-to-many elementToCircuitElement map. Also fix schmitt rOut mapping at schmitt-trigger.ts:83. | high |
| hotload_vTH_shifts_rising_threshold | `src/solver/coordinator.ts:716` | setComponentProperty(st, vTH, 1.0) on a composite resolves via the elementToCircuitElement reverse map which collapses all 4 sub-elements (drv, cIn, rOut, cOut) to one index (last wins = a Capacitor), so el.setParam(vTH) | Composite param hot-load must fan out to every compiled sub-element of the CircuitElement (build a one-to-many map) and route plain model-param keys to the driver sub-element, not just the last-iterated one; also fix sch | high |
| blown_property_seeds_state_to_permanently_off | `src/solver/digital/compiler.ts:647` | Step 8d compile-time state seeding only handles el.typeId===Function; there is no branch that seeds the FGPFET blown property into state[stBase+1] (read at fgpfet.ts:209), so executeFGPFET always reads blown=0 and a blow | Add an FGPFET (and FGNFET) branch in the Step 8d seeding loop (compiler.ts:645-665) that writes initialStateSlots[stBase+1] = blown property ? 1 : 0, mirroring the Function truth-table seeding. | high |

## `ANALOG-SOLVER` (41)

| Test | Root cause (file:line) | Diagnosis | Fix hint | Conf |
|---|---|---|---|---|
| RC circuit runs transient steps stably with capacitor near Vs | _(unresolved)_ | Test loops while simTime<1e-3 (one RC time constant for R=1k C=1uF) then asserts a node exceeds 4.4V; at t=1ms an ideal RC cap reaches only 5*(1-e^-1)=3.16V, below 4.4V, so the threshold is reachable only if the final ac | Run harness on an equivalent RC .dts (R=1k C=1uF stopTime=5e-3) and compare cap-node voltage trajectory vs ngspice via harness_get_attempt to decide engine-bug vs test-threshold; the 4.4V threshold at 1ms is physically i | low |
| hotload_loaded_structural_property_seeds_pin_subelements | `src/components/digital-pins/digital-input-pin-loaded.ts:13` | Test expects the loaded sel pin to sag below 4.7V via rIn approx 100k in series with a 10k source resistor, but DigitalInputPinLoaded default rIn is 1e6 (line 13 params, also 37 and 42), so vSel = 5*1e6/1.01e6 = 4.9505 ( | Reconcile rIn default: either the source default should be 100k or the test must assert the 1MOhm sag (approx 4.9505); the loaded vs unloaded contrast also fails because unloaded has no rIn so both read near 5V. User dis | high |
| loaded_sel_pin_sags_through_external_series_resistor | `src/components/digital-pins/digital-input-pin-loaded.ts:13` | Test asserts vSel=4.5454 = 5*100k/(10k+100k) assuming the loaded pin presents rIn approx 100k, but DigitalInputPinLoaded default rIn is 1e6, so vSel = 5*1e6/(10k+1e6) = 4.9505 (the observed value). Same rIn-default misma | Reconcile the rIn default (line 13 / 37 / 42 are 1e6) with the tests assumed 100k contract; either change the source default or update the closed-form expectation to the 1MOhm sag. | high |
| hotload_rOut_changes_active_output_divider | `src/components/digital-pins/digital-output-pin-loaded.ts:99` | Decoder rOut=1000 hotload asserts 4.5454 (pure divider) but step() is transient and the outPin cOut companion conductance C/dt shifts the node (got 4.5547). | Re-settle to DCOP after the hotload or include the cOut transient companion in the expected value. | medium |
| hotload_rOut_changes_output_divider_voltage | `src/components/digital-pins/digital-output-pin-loaded.ts:99` | Hotload rOut=1000 asserts 5*RLOAD/(rOut+RLOAD)=4.5454 but step() is transient; the outPin cOut companion conductance C/dt shifts the node (got 4.5547 high vs 4.5454). With larger rOut the same cOut shunt produces a diffe | Re-settle to DCOP after setComponentProperty before reading, or account for the cOut transient companion; cOut element here is in-circuit during step(). | medium |
| hotload_rOut_changes_selected_output_divider | `src/components/digital-pins/digital-output-pin-loaded.ts:99` | Demux rOut=1000 hotload asserts 4.5454 (pure divider) but step() is transient and the outPin cOut companion conductance C/dt shifts the node (got 4.5547). | Re-settle to DCOP after the hotload or include the cOut transient companion in the expected value. | medium |
| hotload_vOH_changes_active_output_voltage | `src/components/digital-pins/digital-output-pin-loaded.ts:99` | Same mechanism: decoder outPin asserts resistive divider 6*RLOAD/(rOut+RLOAD) but step() is transient and the outPin cOut companion conductance C/dt shunts the output node (got 5.9346 vs 5.9405). | Re-settle to DCOP after the hotload before reading, or include the cOut transient companion in the expected value. | medium |
| hotload_vOH_changes_output_high_voltage | `src/components/digital-pins/digital-output-pin-loaded.ts:99` | Hotload test asserts the pure resistive divider vOH*RLOAD/(rOut+RLOAD) but coordinator.step() runs a TRANSIENT step; the outPin cOut capacitor (1e-12) contributes a companion conductance C/dt in parallel with rLoad, pull | Either re-settle to DCOP after setComponentProperty (zero out cOut companion) before reading, or have the test compare against the transient-loaded divider including cOut companion; the source-side cOut companion is in-c | medium |
| hotload_vOH_changes_selected_output_voltage | `src/components/digital-pins/digital-output-pin-loaded.ts:99` | Same mechanism as the mux vOH hotload: demux outPin asserts resistive divider 6*RLOAD/(rOut+RLOAD) but step() is transient and the outPin cOut companion conductance C/dt shunts the output node (got 5.9346 vs 5.9405). | Re-settle to DCOP after the hotload before reading, or fold the cOut transient companion into the expected value; cOut element at this line is the in-circuit shunt during step(). | medium |
| ideal_pin_sees_full_source_voltage | `src/components/gates/and.ts:196` | The ideal loading override (digitalPinLoadingOverrides loading=ideal on gate:In_1) is never honored. buildAndGateNetlist at and.ts:196 picks DigitalInputPinLoaded vs Unloaded purely from the static model param loaded (de | Same fix as loaded_pin_sees_voltage_sag: buildAndGateNetlist must consume the per-pin _pinLoading boolean so an ideal pin builds DigitalInputPinUnloaded (no conductance stamp) and reads the full 5V. | high |
| loaded_pin_sees_voltage_sag | `src/components/gates/and.ts:196` | Behavioral AND netlist selects input-pin loading from the static model param loaded (and.ts:196) and a single static rIn default of 1e6 (and.ts:175), ignoring the per-pin _pinLoading and _pinElectrical maps that compiler | Have buildAndGateNetlist read the per-pin loaded flag and per-pin ResolvedPinElectrical (the _pinLoading / _pinElectrical maps set on props by compiler.ts) when choosing DigitalInputPinLoaded vs Unloaded and the rIn valu | high |
| loaded_en_sees_voltage_sag_ideal_clk_clr_see_no_sag | `src/components/memory/counter.ts:279` | vEN reads 0 because the Counter compiles as its digital model. Counter.defaultModel is digital (counter.ts:432) and the test at line 544 places the Counter without model:behavioral, so en/C/clr are digital nets with no a | Two parts: (1) the analog loading override path requires the behavioral netlist; either the test must set model:behavioral or the override compile path must force the analog model when digitalPinLoadingOverrides target a | high |
| invariant_persists_across_multiple_accepted_steps | `src/components/passives/capacitor.ts:592` | Same root cause as pool_state0_state1_agree: across repeated accepted DC-steady-state steps the test asserts state0[C1.Q]===state1[C1.Q] strictly each step. The capacitor charge is recomputed from the live NR-solved term | Identical to the companion test: make the steady-state charge bit-stable (reuse rotated charge when the converged voltage is unchanged, INITPRED-style s1->s0 copy) or re-derive the invariant against ngspices actual stead | high |
| pool_state0_state1_agree_slot_for_slot_after_accepted_step | `src/components/passives/capacitor.ts:592` | After an accepted DC-steady-state step the test requires state0[C1.Q]===state1[C1.Q] strictly (Object.is). The capacitor recomputes its charge each NR iteration as s0[Q]=C*vcap where vcap=v0-v1 is the freshly NR-solved t | The invariant cannot be met through the C*vcap path while node voltages carry 1-ULP solver roundoff between identical steps. Either the recompute must reuse the rotated charge when the converged voltage is unchanged (pre | high |
| rl_dc_steady_state_tight_tolerance | `src/solver/analog/__tests__/harness/comparison-session.ts:1480` | lastStep.branches[l1:branch].ours reads 5.0 (the vs:pos node voltage) instead of the 0.05A inductor branch current, so errorPct=9900. capture.ts:260 keys matrixRowLabels for branch rows 0-based as branchIndex-1, but getS | Index the 1-based voltages array with row+1 in the branch loop (ourFinal.voltages[row+1] and ngFinal.voltages[row+1]) at comparison-session.ts:1480-1481, or store branch rows 1-based in capture.ts:260. Same off-by-one ex | high |
| diode_shockley_equation_consistency | `src/solver/analog/__tests__/mna-end-to-end.test.ts:435` | relError 1.4 percent between iOhm=(Vs-Vd)/R and iShockley=Is*(exp(Vd/(n*Vt))-1) at the converged Vd=0.633685V. The engine solves the diode at vte=CONSTKoverQ*REFTEMP with REFTEMP=300.15K (constants.ts:39, diode.ts:363 vt | Set the test Vt to CONSTKoverQ*300.15 (=0.0258646V) rather than 0.02585, matching the engine REFTEMP of 300.15K (27C). No source change; the diode load uses the correct ngspice thermal voltage. | high |
| linear_converges_in_two_iterations | `src/solver/analog/__tests__/newton-raphson.test.ts:130` | Test expects 2 NR iterations for a linear resistor divider but the engine returns 3. The DC-OP runs ngspice CKTop INITF ladder INITJCT->INITFIX->INITFLOAT, verified line-for-line: dispatcher at newton-raphson.ts:896-915  | Change the expectation at newton-raphson.test.ts:130 to toBe(3) to match the INITJCT/INITFIX/INITFLOAT ladder both engines run. No source change; newton-raphson.ts iteration count matches ngspice STATnumIter. | high |
| statePool state1 is updated after accepted transient step | `src/solver/analog/analog-engine.ts:2282` | Test runs analysis dcop only (no transient step) then reads d1 state1 VD, expecting >0.5. state0->state1 copy lives only in _seedFromDcop (analog-engine.ts:2282), which fires on the dcop->transient transition during step | Either the test must take one accepted transient step before reading state1 (its name says after accepted transient step but it builds with analysis dcop and never steps), or dcop convergence must seed state1 from state0 | medium |
| behavioral_preset_preserves_digital_truth_q_qbar_after_rising_edge_d1 | `src/solver/analog/behavioral-drivers/d-flipflop-driver.ts:85` | Behavioral D-FF preset yields ~Q=1 when Q=1 (expected ~Q=0). The edge detector uses a continuous product risingEdge = vClock*(1-prevClock) plus an instance-field _firstSample (line 53) instead of the NaN sentinel in SLOT | Replace the multiplicative edge detector and instance-field _firstSample with the documented NaN-sentinel scheme in SLOT_LAST_CLOCK: detect a true 0->1 edge against the prior accepted-step clock and skip detection only w | medium |
| treats above-threshold input as HIGH (A=MID, B=MID): AND -> HI | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder classifies with a CMOS dead-band (vIH=2.0 vIL=0.8 defaults at lines 61-62 fed from and.ts AND_BEHAVIORAL_PARAM_DEFS); MID=1.5V falls between the thresholds so result=0.5 (indeterminate) and propag | Replace the three-way dead-band classifier with a single midpoint comparison v > 0.5 ? 1.0 : 0.0 (normalized {0,1} contract) and drop the vIH/vIL dead-band defaults; or set gate input thresholds to the normalized midpoin | high |
| treats above-threshold input as HIGH (A=MID, B=MID): NAND -> LO | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder dead-band classifier (vIH=2.0 vIL=0.8) maps MID=1.5V to 0.5 indeterminate; the NAND driver inverts 0.5 to 0.5 so ctrl_out=0.5 instead of LO=0.0. Contract requires single 0.5V midpoint. | Single midpoint comparison v > 0.5 ? 1.0 : 0.0 with no dead-band; or normalize gate input thresholds so 1.5V classifies HIGH. | high |
| treats above-threshold input as HIGH (A=MID, B=MID): NOR -> LO | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder dead-band classifier (vIH=2.0 vIL=0.8) maps MID=1.5V to 0.5 indeterminate; the NOR driver yields ctrl_out=0.5 instead of LO=0.0. Contract requires single 0.5V midpoint. | Single midpoint comparison v > 0.5 ? 1.0 : 0.0 with no dead-band; or normalize gate input thresholds so 1.5V classifies HIGH. | high |
| treats above-threshold input as HIGH (A=MID, B=MID): OR -> HI | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder dead-band classifier (vIH=2.0 vIL=0.8) maps MID=1.5V to indeterminate 0.5 which propagates through the OR driver to ctrl_out=0.5 instead of HI=1.0. Contract requires single 0.5V midpoint. | Use a single midpoint comparison v > 0.5 ? 1.0 : 0.0 and remove the indeterminate band; or normalize gate input thresholds so 1.5V classifies HIGH. | high |
| treats above-threshold input as HIGH (A=MID, B=MID): XNOR -> HI (equal) | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder dead-band classifier (vIH=2.0 vIL=0.8) maps both MID=1.5V inputs to 0.5 indeterminate so the XNOR driver yields ctrl_out=0.5 rather than HI=1.0. Contract requires single 0.5V midpoint. | Single midpoint comparison v > 0.5 ? 1.0 : 0.0 with no dead-band; or normalize gate input thresholds so 1.5V classifies HIGH. | high |
| treats above-threshold input as HIGH (A=MID, B=MID): XOR -> LO (equal) | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder dead-band classifier (vIH=2.0 vIL=0.8) maps both MID=1.5V inputs to 0.5 indeterminate so the XOR driver yields ctrl_out=0.5 instead of LO=0.0. Contract requires single 0.5V midpoint. | Single midpoint comparison v > 0.5 ? 1.0 : 0.0 with no dead-band; or normalize gate input thresholds so 1.5V classifies HIGH. | high |
| treats above-threshold input as HIGH (In_1=MID): BUF -> HI | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder dead-band classifier (vIH=2.0 vIL=0.8) maps MID=1.5V to 0.5 indeterminate which the BUF driver passes to ctrl_out=0.5 instead of HI=1.0. Contract requires single 0.5V midpoint. | Single midpoint comparison v > 0.5 ? 1.0 : 0.0 with no dead-band; or normalize gate input thresholds so 1.5V classifies HIGH. | high |
| treats above-threshold input as HIGH (in=MID): NOT -> LO | `src/solver/analog/behavioral-drivers/digital-input-thresholder.ts:73` | DigitalInputThresholder dead-band classifier (vIH=2.0 vIL=0.8) maps MID=1.5V to 0.5 indeterminate; the NOT driver inverts 0.5 to 0.5 so ctrl_out=0.5 instead of LO=0.0. Contract requires single 0.5V midpoint. | Single midpoint comparison v > 0.5 ? 1.0 : 0.0 with no dead-band; or normalize gate input thresholds so 1.5V classifies HIGH. | high |
| hotload_vIH_shifts_digital_input_threshold | `src/solver/analog/behavioral-drivers/mux-driver.ts:95` | After raising vIH/vIL so sel=2.5V reclassifies LOW, a single transient step() does not fully propagate the new threshold decision through the feed-forward Norton chain thresholder->mux driver(load reads rhsOld)->output V | Re-settle to DCOP after the threshold hotload before reading, or restructure the thresholder/driver chain so a within-step NR fully propagates (chain of rhsOld-fed Norton sources lags one layer per iteration). | medium |
| ac_sweep_at_each_value | `src/solver/analog/ckt-load.ts:127` | vmid entries are NaN. Same cause as linear_sweep: buildRcSweepFactory builds the divider with elementsByFamily=new Map() (monte-carlo.test.ts:171), so runByDeviceFamily(elementsByFamily) at ckt-load.ts:127 loads no eleme | Populate elementsByFamily in buildRcSweepFactory. Empty family buckets are the only defect; the family-dispatch load path is correct. | high |
| gaussian_distribution | `src/solver/analog/ckt-load.ts:127` | All 1000 trials report failedTrials. The test fixtures build ConcreteCompiledAnalogCircuit with elementsByFamily set to an empty Map (monte-carlo.test.ts:93). cktLoad drives every element load() solely through runByDevic | The load path is family-bucket driven (runByDeviceFamily over elementsByFamily); the hand-built fixtures must populate elementsByFamily from the elements array (group by deviceFamily as compiler.ts:1509-1515 does) instea | high |
| linear_sweep | `src/solver/analog/ckt-load.ts:127` | vmid entries are NaN. The sweep fixture buildSweepDividerFactory builds ConcreteCompiledAnalogCircuit with elementsByFamily=new Map() (monte-carlo.test.ts:126). ParameterSweepRunner.runDcSync calls runByDeviceFamily(elem | Populate elementsByFamily from the elements array in buildSweepDividerFactory. Source load path is family-bucket driven and correct; the fixture starves it of buckets. | high |
| log_sweep | `src/solver/analog/ckt-load.ts:127` | Number.isFinite(vmid[i]) is false (NaN). Same cause as the other sweep tests: buildSweepDividerFactory passes elementsByFamily=new Map() (monte-carlo.test.ts:126), so runByDeviceFamily(elementsByFamily) at ckt-load.ts:12 | Populate elementsByFamily from the elements array in the sweep fixtures. The family-driven load path is correct; the fixture supplies empty buckets. | high |
| cktTerrVoltage_gear_order2_matches_ngspice | `src/solver/analog/ckt-terr.ts:53` | Bit-exact mismatch: cktTerrVoltage uses GEAR_LTE_FACTORS[1]=0.2222222222 (ngspice cktterr.c:25) but the test reference (test line 630) uses factorV=2/9. Result 1.3664819071200633e-12 (source) vs 1.366481907074514e-12 (te | Source line 53 matches ngspice cktterr.c:25. Test line 630 factorV=2/9 should be 0.2222222222 to be ngspice-correct. Escalate the test reference value. | high |
| constant charge history produces finite timestep (not Infinity)- abstol-gated | `src/solver/analog/ckt-terr.ts:190` | Same as line 51 test: constant Q gives ddiff===0, line 190 returns Infinity, but ngspice cktterr.c:69 clamps denom to abstol giving finite del=trtol*tol/abstol. The ddiff===0 early return is not present in ngspice. | Delete line 190 early return; rely on Math.max(abstol, factor*ddiff) denom clamp matching cktterr.c:69. | high |
| gear_lte_factor_order_3 | `src/solver/analog/ckt-terr.ts:53` | Test asserts GEAR_LTE_FACTORS[2]===3/22 (0.13636363636363635) but source ships 0.1363636364, the truncated decimal that matches ngspice cktterr.c:26 exactly. Source is ngspice-bit-exact; the test demands the mathematical | Source line 53 value 0.1363636364 matches ngspice cktterr.c:26. The test expectation 3/22 contradicts ngspice parity. Escalate: cannot satisfy both ngspice bit-exactness and the rational assertion. | high |
| gear_lte_factor_order_5 | `src/solver/analog/ckt-terr.ts:53` | Test asserts GEAR_LTE_FACTORS[4]===10/137 (0.072992700729927) but source ships 0.07299270073, the truncated decimal matching ngspice cktterr.c:29. Source is ngspice-bit-exact; test demands the exact rational. | Source line 53 value 0.07299270073 matches ngspice cktterr.c:29. Test expectation 10/137 contradicts ngspice parity. Escalate. | high |
| gear_lte_factor_order_6 | `src/solver/analog/ckt-terr.ts:53` | Test asserts GEAR_LTE_FACTORS[5]===20/343 (0.05830903790087463) but source ships 0.05830903790 matching ngspice cktterr.c:30. Source is ngspice-bit-exact; test demands the exact rational. | Source line 53 value 0.05830903790 matches ngspice cktterr.c:30. Test expectation 20/343 contradicts ngspice parity. Escalate. | high |
| order 1 trapezoidal: returns finite positive timestep for non-trivial charges | `src/solver/analog/ckt-terr.ts:190` | cktTerr returns Infinity early when ddiff===0 (linear charge history), bypassing ngspice cktterr.c:69 MAX(abstol, factor*diff[0]) clamp which keeps del finite (=trtol*tol/abstol) when diff[0]==0. ngspice has no diff==0 s | Delete the line 190 early return; let denom=Math.max(abstol, factor*ddiff) clamp to abstol when ddiff===0, matching cktterr.c:69, then return del per order. | high |
| order 2 gear: applies sqrt root extraction for nonzero 3rd divided difference | `src/solver/analog/ckt-terr.ts:53` | Bit-exact mismatch: cktTerrVoltage uses GEAR_LTE_FACTORS[1]=0.2222222222 (truncated decimal, matches ngspice cktterr.c:25) but the test computes its reference with the exact rational 2/9=0.2222222222222. Source matches n | Source line 53 already matches ngspice cktterr.c:25 truncated literal. The test factor 2/9 at test line 233 is the divergent value; per SPICE-correct rule the test reference should use 0.2222222222 not 2/9. Escalate: tes | high |
| trapezoidal order 2 and gear order 2 both return positive finite timestep for cubic data | `src/solver/analog/ckt-terr.ts:53` | Bit-exact mismatch on the rGear2 assertion: cktTerrVoltage uses GEAR_LTE_FACTORS[1]=0.2222222222 (ngspice cktterr.c:25) while test computes NGSPICE_REF_GEAR2 with factor 2/9 (test line 293). Source is ngspice-correct; te | Source line 53 matches ngspice cktterr.c:25. Test line 293 factor 2/9 diverges from ngspice; escalate that the test reference should use 0.2222222222. | high |
| nodeset_steers_ngspice_to_latch_state_digiTS_does_not | `src/solver/analog/compiler.ts:1571` | digiTS ignores .nodeset on the production compile path so its NR settles at the symmetric metastable point (Q1:C ours=1.078) while ngspice honours the guess and latches (ngspice=4.609). The full enforcement chain exists  | In the analog compiler, resolve parsed .nodeset net/pin names to MNA node ids and pass a nodesets Map into the ConcreteCompiledAnalogCircuit constructor at compiler.ts:1571 (parallel to how ics should flow). Downstream a | high |

## `HARNESS/PARITY` (41)

| Test | Root cause (file:line) | Diagnosis | Fix hint | Conf |
|---|---|---|---|---|
| full_iteration_paired_active | `src/components/active/optocoupler.ts:54` | Optocoupler-cccs active full_iteration sibling: compareAllAttempts (asserts line 70) throws 0 comparable steps for the same reason as transient_step_end_paired_active. The describe block shares one ComparisonSession on o | Fix the upstream structural defect: remove the extra MNA unknown from the optocoupler composite expansion / BJT internal-node allocation so matrixSize equals ngspice (11). Once steps are accepted, compareAllAttempts will | high |
| full_iteration_paired_low | `src/components/active/optocoupler.ts:54` | Optocoupler-cccs low-drive full_iteration sibling: compareAllAttempts (asserts line 70) throws 0 comparable steps. Shares one ComparisonSession on optocoupler-cccs-canon-low.dts; harness_topology_diff confirms matrix-siz | Same upstream fix as the optocoupler _active/_low entries: align our composite MNA equation count to ngspice (11) by removing the extra node/branch in the optocoupler expansion or BJT internal-node allocation. Verify str | high |
| transient_step_end_paired_active | `src/components/active/optocoupler.ts:54` | Optocoupler active fixture (second of duplicate batch entries, same fixture optocoupler-cccs-canon-active.dts): identical root cause to the first active entry. harness_topology_diff: matrix-size divergence 12 vs 11, extr | Same as the other _active entry: fix the composite expansion / BJT internal-node count so our matrixSize equals ngspice (11). Verify with harness_topology_diff on optocoupler-cccs-canon-active.dts after the fix (structur | high |
| transient_step_end_paired_low | `src/components/active/optocoupler.ts:54` | Optocoupler low-drive fixture (second duplicate batch entry, same optocoupler-cccs-canon-low.dts): identical root cause. Matrix-size divergence 12 vs 11 from the composite expansion leaves the phototransistor base/CCCS n | Same as the other _low entry: align our composite MNA equation count to ngspice (11) by fixing the extra node/branch in the optocoupler expansion or BJT internal-node allocation. | high |
| dcop_paired_rail_saturation | `src/components/active/real-opamp.ts:489` | Duplicate of the rail_saturation record. RealOpAmp self-compare transient records zero accepted steps so getStepEnd(0) throws Step out of range 0; railLim at real-opamp.ts:483-503 increments ctx.noncon every iteration in | Same as the sibling record: make the rail clamp reach a fixed point so the limited flag clears next iteration rather than perpetually bumping noncon at real-opamp.ts:489. | medium |
| full_iteration_paired_rail_saturation | `src/components/active/real-opamp.ts:541` | Duplicate of the other full_iteration_paired_rail_saturation record (same RealOpAmp session). RealOpAmp rail-saturation DCOP errors at step 0 (dt=0 iters=101 converged=false per convergence log) so 0 comparable steps. Ro | Same fix as the sibling record: replace the hard RHS-branch switch at real-opamp.ts:541-556 with a continuous-Jacobian rail-saturation linearization so the DCOP converges. | medium |
| transient_step_end_paired_rail_saturation | `src/components/active/real-opamp.ts:485` | RealOpAmp rail-saturation (second duplicate batch entry, same createSelfCompare fixture real-opamp-canon-rail-saturation.dts): identical root cause. Engine accepts 0 transient steps because the RealOpAmp macromodel does  | Same as the other _rail_saturation entry: fix the railLim/outputSaturated convergence limit-cycle at real-opamp.ts:485-542. paired ngspice harness is blocked by pairedSpiceEquivalent:false; use createSelfCompare + conver | medium |
| transient_step_end_paired_ac_sinusoid | `src/components/passives/inductor.ts:891` | tapped-transformer-canon-ac-sinusoid: our engine does not advance past step 0 (harness: ours 1 step / 49 NR iters / lastDt=0; ngspice 1009 steps). compareAllSteps then reports step 0 dt: ours=0 ngspice=5e-7. harness_firs | Investigate why the transient timestep never advances for coupled inductors (MutualInductor + 3 Inductors): the engine accepts only the t=0 step then stops. Check the first-transient-dt handoff in analog-engine.ts step l | medium |
| transient_step_end_paired_ac_step_down | `src/components/passives/inductor.ts:891` | transformer-canon-ac-step-down: identical mode to ac_sinusoid. Our engine accepts only the t=0 step (harness: ours 1 step / 49 NR iters / lastDt=0; ngspice 1009 steps) so compareAllSteps reports step 0 dt: ours=0 ngspice | Same as ac_sinusoid: localize why the transient timestep never advances for a MutualInductor-coupled inductor pair. Check first-transient-dt advance in analog-engine.ts step loop and timestep.getClampedDt, plus inductor  | medium |
| full_iteration_paired_reverse_bias | `src/components/passives/polarized-cap.ts:207` | Same polarizedcap-canon-reverse-bias session as transient_step_end_paired_reverse_bias. 706 per-iteration divergences all trace to a single 1-ULP value-only mismatch at the (nCap,nCap) shared diagonal at step0/iter0 (har | Same fix as transient_step_end_paired_reverse_bias: align the += accumulation order of the three nCap-node leaf conductances (cBody/rEsr/rLeak) with ngspices cktload device-walk order. Leaf order set in polarized-cap.ts: | high |
| transient_step_end_paired_reverse_bias | `src/components/passives/polarized-cap.ts:207` | polarizedcap-canon-reverse-bias: both engines run 107 identical steps; the only divergence is a 1-ULP value-only mismatch (absDelta 1.78e-15) at the shared diagonal cell (node nCap, node nCap) at step0/iter0, confirmed b | Align the leaf stamp accumulation order on the nCap diagonal with ngspice. The leaf order in POLARIZED_CAP_NETLIST_BUILDER elements[] (polarized-cap.ts:207-235) is rEsr,rLeak,cBody; the compiler reverse-sorts within buck | high |
| full_iteration_paired_matched_load | `src/components/passives/transmission-line-element.ts:268` | Same root cause as transient_step_end_paired_matched_load: 2808 per-iteration divergences all 1-ULP (first node_3 ours=0.5 ngspice=0.49999999999999994 absDelta=5.55e-17). Matrix bit-identical step counts identical; the 1 | Same as the step_end finding: reorder the step-0 DC-OP RHS accumulation for the TRA bridge plus resistor to match ngspices stamp emission order, or treat as arithmetic-order divergence and escalate. Zero-tolerance Object | medium |
| transient_step_end_paired_matched_load | `src/components/passives/transmission-line-element.ts:268` | Pure 1-ULP arithmetic-order divergence not a model bug. Step counts (1004) convergence and iter-0 Jacobian are bit-identical (harness_matrix_diff classification=match zero mismatches). At step1/iter0 the only difference  | This is a zero-tolerance Object.is comparison failing on a 1-ULP delta where ours is more accurate than ngspice. To reach bit-exact match the step-0 DC-OP RHS accumulation order for the TRA MODEDC bridge plus the load re | medium |
| full_iteration_paired_breakover | `src/components/semiconductors/diode.ts:942` | diac-canon-breakover step-0 DCOP fails to converge (ours 2400 NR iters failed; ngspice 5 converged) so compareAllAttempts sees 0 comparable steps. harness_first_divergence: DIAC1_D_rev.VD ours=66.38 vs ngspice=-37.65, wi | Same as full_iteration_paired_blocking: use harness_get_attempt on DIAC1_D_rev across the failing DCOP iters to confirm the breakdown pnjlim operands (diode.ts:942-948 vs dioload.c:219-243). The +66V VD shows vd crossing | medium |
| transient_step_end_paired_blocking | `src/components/semiconductors/diode.ts:913` | Diac blocking fixture: our engine accepts 0 steps because the diac sub-diode does not converge at step 0. harness_first_divergence shows DIAC1_D_rev.VD ours=66.38 (=tVcrit, inflated by N=40) vs ngspice -20, and harness_m | Diac topology matrixSize matches ngspice (3=3) so this is numerical not structural. The diode reaches a non-finite conductance/RHS at vd=tVcrit~66V where ngspice stays finite. Drill the tranNR attempt with harness_get_at | medium |
| transient_step_end_paired_breakover | `src/components/semiconductors/diode.ts:913` | Diac breakover fixture: same non-convergence as the blocking variant. The diac is two anti-parallel diodes sharing nodes; at MODEINITJCT both sub-diodes seed vd=tVcrit (~66V, inflated by N=40) and our diode load produces | Same root as diac blocking. Diode load at the inflated tVcrit junction guess goes non-finite vs ngspice; fix the breakover/limiting arm so vd tracks ngspice. Use harness_start on diac-canon-breakover.dts then harness_fir | medium |
| temp_350K_compareAllAttempts | `src/components/semiconductors/mosfet.ts:420` | At default 300.15K this fixture is bit-exact (harness 110/110 converged, firstDivergence null). The harness injects .options TEMP into the ngspice deck from engine.circuitTemp (comparison-session.ts:779-780) so both side | Drill the temp pass tTransconductance (KP/ratio4) tVto and tPhi lines 420 423-430 against mos1temp.c at instanceTemp=350K; the absDelta 3.98 on a source-clamped gate node indicates the temp-shifted operating point sends  | medium |
| temp_400K_compareAllAttempts | `src/components/semiconductors/mosfet.ts:420` | Same fixture and code path as temp_350K but at 400K. ours=-370.9V on the source-clamped M1 gate at iter 0 is a full NR blowup not a small parity drift. Default-temp parity is exact (harness 110/110); only the temperature | Same drill as 350K but the blowup magnitude points at the temp pass producing an operating point digiTS NR cannot solve; check tTransconductance line 420 and tVto line 430 plus the limiting using temp-shifted drainVcrit/ | medium |
| full_iteration_paired_isolation | `src/components/switching/trans-gate.ts:337` | Identical root cause to full_iteration_paired_pass_through on the isolation fixture (trans-gate-canon-isolation.dts). TransGateDefinition lacks pairedSpiceEquivalent:false; its behavioral subcircuit (BehavioralFETDriver  | Same fix: declare TransGateDefinition pairedSpiceEquivalent:false and move the full_iteration_paired_* and dcop/transient paired tests to ComparisonSession.createSelfCompare (T2). FetSW.load stamping at fet-sw.ts:127 and | high |
| full_iteration_paired_pass_through | `src/components/switching/trans-gate.ts:337` | TransGateDefinition lacks pairedSpiceEquivalent:false. Its behavioral model (BehavioralFETDriver Norton ctrl rail + FetSW conductance switch) has no ngspice primitive equivalent, so the harness auto-deck drops the four F | Mark TransGateDefinition pairedSpiceEquivalent:false (like fuse/opamp/ota) and switch these tests from ComparisonSession.create (T3 paired) to createSelfCompare (T2). The numeric stamp at fet-sw.ts:127 is correct; the on | high |
| transient_step_end_paired_isolation | `src/solver/analog/behavioral-drivers/bridge-output-driver.ts:154` | Same mechanism as paired_pass_through. TransGate boundary pins compile to BridgeOutputDriverElement instances that stamp a parasitic 1/rHiZ=1e-7 (Hi-Z, bridge-output-driver.ts:154) or 1/rOut (:162) diagonal conductance o | Same fix as pass_through: do not stamp the bridge boundary parasitic conductance for ideal-source boundary pins (or generate a matching parasitic on the ngspice side). Wrong cells are the 1e-7 stamps at bridge-output-dri | high |
| transient_step_end_paired_pass_through | `src/solver/analog/behavioral-drivers/bridge-output-driver.ts:154` | TransGate compiles in the digital domain so its boundary source pins (vs/vp1/vp2/load) each get a BridgeOutputDriverElement. In Hi-Z the bridge stamps 1/rHiZ=1e-7 on its boundary node diagonal (bridge-output-driver.ts:15 | The bridge boundary drivers inject a parasitic 1/rHiZ (and 1/rOut) conductance that the ngspice deck has no counterpart for. Either suppress the bridge diagonal conductance stamp when the boundary pin is an ideal source  | high |
| full_iteration_paired_blocking | `src/solver/analog/compiler.ts:517` | TRIAC blocking (triac.test.ts:395 fixture triac-canon-blocking.dts) is a four-BJT composite (QTR1_Q1..Q4). harness_topology_diff: matrix-size divergence of 4 equations (ours 11 vs ngspice 7) one Kull QS epi-collector bra | At compiler.ts:515-518 apply leaf-entry default model params WITHOUT marking givenness (new PropertyBag setter writing _mparams but not _mparamsGiven) so only subEl.params-listed params are given; QS branch then not allo | high |
| full_iteration_paired_gated_on | `src/solver/analog/compiler.ts:517` | TRIAC gated-on (triac.test.ts:425 fixture triac-canon-gated-on.dts) same four-BJT composite as triac blocking; first divergence at RG/VG gate node iter 0. Same structural matrix-size divergence: one Kull QS epi-collector | Same fix as triac blocking: set leaf-entry default model params at compiler.ts:515-518 without marking _mparamsGiven so isModelParamGiven(RCO) is false and the QS branch is not allocated. | high |
| full_iteration_paired_reverse_blocking | `src/solver/analog/compiler.ts:517` | TRIAC reverse-blocking (triac.test.ts:457 fixture triac-canon-reverse-blocking.dts) same four-BJT composite; first divergence QTR1_Q4/Q3 internal node iter 0 with tiny absDelta 2.49e-26 reflecting the structural extra-eq | Same fix: apply leaf-entry default model params without marking givenness at compiler.ts:515-518 so the QS branch is not allocated for composite sub-BJTs, matching ngspice BJTintCollResistGiven gating. | high |
| full_iteration_paired_triggered | `src/solver/analog/compiler.ts:517` | SCR triggered (scr.test.ts:279 fixture scr-canon-triggered.dts) same two-BJT composite. harness_topology_diff: matrix-size divergence of 2 equations (ours 11 vs ngspice 9), scr element ours-only, VG/VA branches at offset | Same fix as scr blocking: at compiler.ts:515-518 set leaf-entry default model params WITHOUT marking givenness (new PropertyBag setModelParamDefault that writes _mparams but not _mparamsGiven) so isModelParamGiven(RCO) r | high |
| transient_step_end_paired_blocking | `src/solver/analog/compiler.ts:517` | Third paired_blocking record, same root cause class. Composite expansion (triac/scr/diac) leaks registry-default givenness: compiler.ts:515-518 uses setModelParam for leafEntry.params defaults which marks them given (pro | Single fix at compiler.ts:517: write composite leaf registry defaults without givenness; only explicit subEl.params should be marked given. Resolves all paired_blocking/gated_on/reverse_blocking/triggered equation-count  | high |
| transient_step_end_paired_gated_on | `src/solver/analog/compiler.ts:517` | Triac gated-on regime; same A1 equation-count divergence. Triac composite NpnBJT/PnpBJT spice leaves get RCO=0.01 marked given by the compiler.ts:515-518 leaf-default loop (setModelParam marks given at properties.ts:164) | Same single fix at compiler.ts:517: do not mark composite leaf registry defaults given. | high |
| transient_step_end_paired_reverse_blocking | `src/solver/analog/compiler.ts:517` | Triac reverse-polarity blocking regime; same A1 equation-count divergence. Triac composite BJT spice leaves inherit RCO=0.01 as given via compiler.ts:515-518 setModelParam (properties.ts:164 marks given), so bjt.ts:1549  | Single fix at compiler.ts:517: composite leaf registry defaults must be written without givenness so rcoGiven stays false and the prime node collapses, matching ngspice. | high |
| transient_step_end_paired_triggered | `src/solver/analog/compiler.ts:517` | SCR triggered regime; same A1 equation-count divergence as the blocking cases. SCR composite expands into NpnBJT/PnpBJT spice leaves; compiler.ts:515-518 marks the BJT registry-default RCO=0.01 as given via setModelParam | Fix compiler.ts:517 to not mark composite leaf registry defaults given; rcoGiven will then be false and the BJT prime node collapses, matching ngspice equation count. | high |
| full_iteration_paired_ac_sinusoid | `src/solver/analog/dc-operating-point.ts:496` | Tapped transformer = three coupled ideal inductors (LTX1_L1/L2/L3) with three mutual couplings driven by V1 with R_S1/R_S2 loads. Same DC-OP branch-current-singularity stall as the two-winding transformer: ours 1 step (4 | Enable/repair the OPtran pseudo-transient fallback (dc-operating-point.ts 496-521 gated by params.optran and ctx.opTranFallback) so the coupled-inductor DC singularity resolves like ngspice cktop.c OPtran. Same fix as th | medium |
| full_iteration_paired_ac_step_down | `src/solver/analog/dc-operating-point.ts:496` | Transformer (two coupled inductors LTX1_L1/LTX1_L2 plus mutual) driven by V1 with R_LOAD on the secondary. Our engine stalls at step 0 (1 step 49 iters then dt=0) while ngspice runs 1009 steps. harness_first_divergence e | Enable/repair the OPtran pseudo-transient fallback (dc-operating-point.ts lines 496-521 gated by params.optran and ctx.opTranFallback) so the transformer DC-OP branch-current singularity is resolved the way ngspice cktop | medium |
| temp_300_15K_compareAllAttempts | `src/solver/analog/dc-operating-point.ts:403` | Transient at step 0 our engine stalls (1 step then dt=0) while ngspice advances 107 steps. harness_first_divergence earliest is shape attemptCount ours=23 ngspice=238 at step0/iter0; the dcopGminDynamic sub-solve is bit- | Compare our dynamicGmin-to-newGmin fallthrough and gillespieSrc backtracking attempt sequence against ngspice cktop.c dynamic_gmin/gillespie_src so our step-0 transient OP runs the same number of sub-attempts and converg | medium |
| temp_350K_compareAllAttempts | `src/solver/analog/dc-operating-point.ts:403` | Same step-0 transient-OP stall as the 300.15K case: the dcopGminDynamic sub-solve matches ngspice bit-for-bit but our convergence ladder gives up after ~23 attempts vs ngspice ~238 and accepts a wrong OP then cannot adva | Fix the gmin/source-step ladder progression so step-0 transient OP converges to ngspices point (see 300.15K finding). The temperature mismatch is by test design (ngspice has no matching .options TEMP) so it is a separate | medium |
| temp_400K_compareAllAttempts | `src/solver/analog/dc-operating-point.ts:403` | Same step-0 transient-OP convergence-ladder stall as 300.15K/350K (dcopGminDynamic bit-identical to ngspice but ladder gives up after far fewer attempts and accepts a wrong OP then cannot advance). At 400K the test sets  | Fix the gmin/source-step ladder progression so step-0 transient OP converges (see 300.15K finding). Temperature mismatch is by test design. | medium |
| transient_temp_sweep_300_15K_paired | `src/solver/analog/dc-operating-point.ts:496` | Same transformer DC-OP stall as full_iteration_paired_ac_step_down. harness_run on transformer-temp-sweep.dts reproduces it without any temp set: ours 1 step (49 iters) then dt=0 error while ngspice runs 1008 steps. The  | Enable/repair the OPtran pseudo-transient fallback (dc-operating-point.ts 496-521) so the transformer branch-current singularity resolves like ngspice OPtran. Same fix as full_iteration_paired_ac_step_down. | medium |
| transient_temp_sweep_350K_paired | `src/solver/analog/dc-operating-point.ts:496` | Same transformer DC-OP branch-current-singularity stall as the 300.15K case (identical first divergence 0.003141587485876422 confirming the temp set is irrelevant because the engine never advances past step 0). Two coupl | Enable/repair the OPtran pseudo-transient fallback (dc-operating-point.ts 496-521). Same fix as the 300.15K transformer case. | medium |
| transient_temp_sweep_400K_paired | `src/solver/analog/dc-operating-point.ts:496` | Same transformer DC-OP branch-current-singularity stall as 300.15K/350K (identical first divergence 0.003141587485876422). Two coupled inductors plus mutual create a DC singularity gmin cannot resolve; engine stuck in DC | Enable/repair the OPtran pseudo-transient fallback (dc-operating-point.ts 496-521). Same fix as the other transformer temp-sweep cases. | medium |
| full_iteration_paired_dt_energised | `src/solver/analog/ni-integrate.ts:64` | RelayDT energised same session/fixture as transient_step_end_paired_dt_energised (relay.test.ts:568 reads the recorded run). Same root cause: inductor-coil companion conductance is exactly 2x off at step 0 iter 0 (ours g | Same as transient_step_end_paired_dt_energised: fix the first-transient-step ag[0] feeding the inductor companion conductance so geq matches ngspice 200; verify nicomcof/ag seeding at MODEINITTRAN against ngspice and the | medium |
| transient_step_end_paired_dt_energised | `src/solver/analog/ni-integrate.ts:64` | RelayDT energised (relay.test.ts:556 fixture relay-dt-canon-energised.dts). harness_matrix_diff classification value-only: the four inductor-coil cells (Lrelay_coilL branch diagonal and node couplings) are exactly 2x off | Drill the first-transient-step ag fed to the inductor load: at MODEINITTRAN the engine ag[0] driving geq (ni-integrate.ts:64) is half of ngspice. Compare our nicomcof/ag0 seeding for the boot step against ngspice nicomco | medium |
| full_iteration_paired_triangle_rc | `src/solver/analog/timestep.ts:435` | AC triangle-RC (ac-voltage-source.test.ts:567 fixture acvsource-canon-triangle-rc.dts). Steps 0-28 match bit-exact; harness_first_divergence earliest is shape attemptCount (ours 6 vs ngspice 4) and integration delta at s | Replay from the first non-matching delta: compare our computeNewDt (timestep.ts:412-456, 2x growth cap line 440) and getClampedDt post-breakpoint saveDelta clamp (line 529) against ngspice CKTtrunc/dctran.c around the 0. | medium |

## `OTHER` (11)

| Test | Root cause (file:line) | Diagnosis | Fix hint | Conf |
|---|---|---|---|---|
| gaussian_distribution_mean_near_zero_stddev_near_amplitude | `src/components/sources/ac-voltage-source.ts:513` | Test calls computeWaveformValue noise directly expecting a Box-Muller gaussian return, but the noise arm of computeWaveformValue was removed and replaced with a throw (lines 507-516) because noise is now the deterministi | Retarget the statistical audit at the seeded TRNOISE generator via evaluateNgspiceWaveform with a TRNOISE coefficient vector (randnumb recon) instead of computeWaveformValue noise, or delete this test as it asserts the r | high |
| hotload_noiseSampleTime_changes_noise_breakpoint_schedule | `src/components/sources/ac-voltage-source.ts:427` | enumWaveformCoeffs returns null for noise (default arm), so building a noise fixture leaves _functionType null and any waveform evaluation during compile/step hits the computeWaveformValue noise throw (line 513). The get | Add a TRNOISE arm to enumWaveformCoeffs so the noise enum sets _functionType=TRNOISE and _evaluate uses evaluateNgspiceWaveform instead of the computeWaveformValue throw, allowing the noise fixture to build and the break | high |
| lag1_autocorrelation_below_threshold | `src/components/sources/ac-voltage-source.ts:513` | Test calls computeWaveformValue noise directly to measure lag-1 autocorrelation of Box-Muller draws, but the computeWaveformValue noise arm is removed and now throws (lines 507-516); noise is the deterministic ngspice TR | Retarget the autocorrelation audit at the seeded TRNOISE generator through evaluateNgspiceWaveform with a TRNOISE coefficient vector, or remove this test since it asserts the deleted live-Math.random Box-Muller contract. | high |
| noise_mode_produces_finite_node_voltage_distinct_from_sine | `src/components/sources/ac-voltage-source.ts:427` | enumWaveformCoeffs default arm returns null for waveform noise instead of building a TRNOISE coefficient vector, so the native noise enum is never routed onto _functionType=TRNOISE and _evaluate falls through to computeW | Add a case noise arm to enumWaveformCoeffs returning functionType TRNOISE with the seeded coeffs so the noise enum routes onto evaluateNgspiceWaveform; then update the test to assert the deterministic TRNOISE output rath | high |
| every junction wires carry positive current and KCL is satisfied | `src/editor/wire-current-resolver.ts:32` | Same root cause as the parallel-split body-path test: ComponentCurrentPath has no element identity and resolve() omits paths for 1-pin elements (Ground), so getComponentPaths() is shorter and misaligned versus elementToC | Attach an element identity (elementIndex or label) to ComponentCurrentPath at the resolve() push sites so the body-path-to-element mapping is by identity, not positional index that breaks when 1-pin elements are skipped. | high |
| parallel split at junction: branch wires carry their branch current | `src/editor/wire-current-resolver.ts:32` | ComponentCurrentPath carries no element identity (eIdx or label) and resolve() builds _componentPaths skipping elements with fewer than 2 pins (Ground continues at line 302). getComponentPaths() therefore returns fewer p | Add an element identity field (elementIndex or label) to ComponentCurrentPath and populate it at the push sites in resolve() (lines 298 and 304) so consumers map a body path to its element by identity instead of position | high |
| RC step response: exponential charging matches V(1-e^-t/tau) | `src/solver/analog/analog-engine.ts:317` | The warm-start transient DC operating point (_transientDcop -> solveDcOperatingPoint -> _seedFromDcop) is deferred to the first step() via the if (!this._firstStep) gate instead of running at init/compile time. The test  | Run the initial transient operating-point solve at init()/compile() time (capturing the source value present then) so a later hot setSignal followed by step() continues the transient from the established reactive-element | high |
| RL step response: V_R matches 1-e^-t/tau (R=10 L=1mH tau=100us) | `src/solver/analog/analog-engine.ts:317` | Same deferred-warm-start-DCOP root cause as the RC step test. The transient operating point seeded by _seedFromDcop runs only on the first step() (gated by if (!this._firstStep)), which happens after the test hot-changes | Run the initial transient operating-point solve at init()/compile() time so the inductor state is established before the hot setSignal; the subsequent step() must continue the transient from that state rather than re-run | high |
| restores the closest snapshot to time 0 when jumped to start | `src/test-utils/non-engine-coordinator.ts:126` | jumpToTime restores the first-recorded snapshot whose stub ID is 1 instead of 0; non-engine-coordinator saveSnapshot starts nextSnapshotId at 1 (line 126) while real DigitalEngine.saveSnapshot is 0-based (digital-engine. | Change non-engine-coordinator.ts line 126 from let nextSnapshotId = 1 to let nextSnapshotId = 0 to match DigitalEngine 0-based snapshot IDs. | high |
| restores the closest snapshot when jumpToTime is called | `src/test-utils/non-engine-coordinator.ts:126` | jumpToTime(3.4) picks the time=3 snapshot (3rd recorded) and restores its ID; expected 2 (0-based) but got 3 because non-engine-coordinator saveSnapshot starts nextSnapshotId at 1 (line 126) whereas real DigitalEngine.sa | Change non-engine-coordinator.ts line 126 from let nextSnapshotId = 1 to let nextSnapshotId = 0 to match DigitalEngine 0-based snapshot IDs. | high |
| snapshot IDs match what the coordinator returned | `src/test-utils/non-engine-coordinator.ts:126` | The non-engine coordinator stub initializes nextSnapshotId to 1 and saveSnapshot returns 1-based IDs (1,2,3), but real production DigitalEngine.saveSnapshot (digital-engine.ts:235,529) starts at 0 and returns 0-based IDs | Change non-engine-coordinator.ts line 126 from let nextSnapshotId = 1 to let nextSnapshotId = 0 to match DigitalEngine 0-based snapshot IDs. | high |

## `NAN` (5)

| Test | Root cause (file:line) | Diagnosis | Fix hint | Conf |
|---|---|---|---|---|
| limiting_paired_forward | `src/solver/analog/__tests__/harness/comparison-session.ts:3066` | Harness evidence: at step0 tranNR iter0 BOTH sides record an identical AK pnjlim event (vBefore=vAfter=0.4992749262783062 wasLimited=false divergenceNorm=0). But ngspice labels the diode DVD (SPICE D-type prefix) while d | In getLimitingComparison normalize the SPICE type prefix when matching ng events to the requested label (strip leading device-type letter D/Q/M/J for diode/bjt/mos/jfet) or match on the bridge deviceName mapped back to o | high |
| output_statistics | `src/solver/analog/ckt-load.ts:127` | stats.mean is NaN. Same root as gaussian_distribution: the fixture passes elementsByFamily=new Map() (monte-carlo.test.ts:126) so runByDeviceFamily at ckt-load.ts:127 loads nothing, every trial fails to converge, the con | Populate elementsByFamily in the fixture (group elements by deviceFamily). With buckets populated the divider converges and mean lands near 2.5V. NaN is a downstream consequence of the empty-family load. | high |
| hotload_ctr_lowers_collector_voltage | `src/solver/analog/compiler.ts:517` | after is NaN at the collector node. The base build never converges because compiler.ts:517 copies the BJT spice default RCO=0.01 as given, enabling rcoGiven (bjt.ts:1549) and the Kull colPrime node split (bjt.ts:1756). T | Fix the composite default-given leak so the optocoupler converges; the CTR hot-load direction check then exercises a finite collector voltage. | high |
| init_active_anode_below_rail_collector_below_rail | `src/solver/analog/compiler.ts:517` | vCollector is NaN. compiler.ts:517 setModelParam copies the BJT spice default RCO=0.01 and marks it given, so rcoGiven (bjt.ts:1549) is true and the BJT splits off the Kull colPrime internal node (bjt.ts:1756), creating  | Do not mark leaf-default model params as given during composite expansion so the BJT does not allocate the unconnected quasi-sat node. | high |
| init_blocking_node_voltage_near_zero | `src/solver/analog/compiler.ts:517` | Composite sub-element construction writes leafEntry model-registry DEFAULT params via setModelParam which MARKS THEM GIVEN. The DIAC diode sub-elements thus get ISR=1e-14 flagged as given so diode setup _recSatCurGiven b | Write leafEntry.params (model-registry defaults) with non-given semantics (replaceModelParams without markGiven or a setModelParam variant that does not add to _mparamsGiven); only subEl.params explicit overrides should  | high |

## `DIGITAL` (1)

| Test | Root cause (file:line) | Diagnosis | Fix hint | Conf |
|---|---|---|---|---|
| masked_addresses_are_independent_across_distinct_register_slots | `src/solver/digital/bus-resolution.ts:75` | The fixture wires two non-tri-state digital drivers onto rf:Rw (the Splitter output spl:0,1 and the In in_rw:out), so the compiler (compiler.ts:486-531) registers Rw as a multi-driver bus net with both as hard drivers. r | Mask each driver value and non-highZ mask to the net declared bit width before the conflict XOR at bus-resolution.ts:75 (carry the net width into resolveBusDrivers and AND driverValue/driverNonHighZ with the width mask), | medium |


Now I have all the data. Let me produce the complete unified diff.

---

```diff
--- a/spec/state-pool-schema.md
+++ b/spec/state-pool-schema.md
@@ -1,7 +1,7 @@
 # Spec: Declarative State-Pool Contract for Reactive Analog Elements
 
 **Status:** Final, ready for executor hand-off.
-**Scope:** `src/solver/analog/element.ts`, `src/solver/analog/compiler.ts` (single-line use), one new file `src/solver/analog/state-schema.ts`, and ~10 element files listed in the audit.
-**Out of scope:** model switching, `analog-engine.ts`, engine-interface files.
+**Scope:** `src/solver/analog/element.ts`, `src/solver/analog/compiler.ts` (single-line use), `src/solver/analog/analog-engine.ts` (two targeted fixes — see Wave A steps 4–5), one new file `src/solver/analog/state-schema.ts`, and ~10 element files listed in the audit.
+**Out of scope:** model switching, engine-interface files.
 
 ---
 
```

```diff
# Amendment L: Explicit-defaults clarification — add note to Section 1 intro
@@ -22,6 +22,10 @@ This is deliberately a thin layer on top of the existing `const SLOT_X = N; sta
 4. An **init hook per slot** for the rare cases where the initial value depends on `params` (e.g. `RB_EFF = params.RB`, `GD_JUNCTION = GMIN`).
 
 **What is NOT added:** no map lookups, no string keys at runtime, no per-call dispatch, no base class for reactive elements (optional mixin instead), no schema registry. Slot reads remain `this.s0[this.base + SLOT_GEQ]` — byte-identical to today's `capacitor.ts`.
+
+**Explicit-defaults rule:** Every slot MUST declare an explicit `init` kind. Zero-initialised slots use `{ kind: 'zero' }` — there is no implicit default. The `SlotDescriptor.init` field is non-optional precisely to enforce this.
```

```diff
# Amendment D: Add ReactiveAnalogElement sub-interface to Section 1.3
@@ -173,6 +177,22 @@ Replace the `stateSize` / `initState` JSDoc at `element.ts:235-248` with:
 
 ### 1.3 Amendment to `AnalogElement` in `src/solver/analog/element.ts`
 
+Also add the following sub-interface to `element.ts` (alongside `AnalogElementCore`):
+
+```typescript
+/**
+ * Sub-interface for reactive elements (isReactive: true) that participate in
+ * the declarative state-pool contract. All reactive elements MUST implement
+ * this interface so the dev-time probe can enforce pool exclusivity.
+ *
+ * stateSchema is non-optional here — every reactive element must declare its
+ * full slot layout. Stateless non-reactive elements (resistors, voltage
+ * sources, etc.) may omit stateSchema via the base AnalogElementCore interface.
+ */
+export interface ReactiveAnalogElement extends AnalogElementCore {
+  readonly isReactive: true;
+  readonly stateSchema: import("./state-schema.js").StateSchema; // non-optional
+}
+```
+
 Replace the `stateSize` / `initState` JSDoc at `element.ts:235-248` with:
```

Wait — the original text starts with "### 1.3" and doesn't have a duplicate "Replace" line. Let me produce the diff more carefully against actual line positions. I'll use the content I read to produce precise hunks.

```diff
--- a/spec/state-pool-schema.md
+++ b/spec/state-pool-schema.md
```

```diff
# Amendment A (part 1): Fix initState call-site claim in Section 2 prose
@@ -280,11 +280,14 @@ This is deliberately a thin layer on top of the existing `const SLOT_X = N; sta
 **Engine guarantees on entry:**
 - `pool.state0` contents for this element's slots are **guaranteed zero**. `StatePool`'s constructor (`state-pool.ts:10-15`) uses `new Float64Array(totalSlots)` which zero-fills. `initState` is called exactly once per compile at `compiler.ts:1310-1312` on a freshly-allocated pool. No stale data.
 - `pool.state1` and `pool.state2` are also zero, but `initState` MUST NOT write to them — history vectors are managed exclusively by `StatePool.acceptTimestep()` (`state-pool.ts:17-21`).
 - `this.stateBaseOffset` is guaranteed valid (≥ 0) when `initState` is called.
 
-**Call sites:** exactly one — `compiler.ts:1310-1312`. Not called from `analog-engine.ts:init` (that only rebinds pool refs for re-use, see `analog-engine.ts:175-187`), not called from DC-op, not called from `reset()` (`analog-engine.ts:195-211` — reset uses `statePool.reset()` which zero-fills, meaning **on reset, initial values are lost**). Flagged in section 6 as an open issue, but the fix is out of scope for this spec: a follow-up must either (a) have `reset()` re-invoke `initState` for all elements, or (b) for now, rely on the observation that every current reset is followed by a fresh transient that re-converges naturally.
+**Call sites:** two in the current codebase, but only one is intended:
+1. `compiler.ts:1310-1312` — the authoritative call site, on a freshly-allocated pool.
+2. `analog-engine.ts:186` — `el.initState?.(cac.statePool)` inside the engine `init()` loop (`analog-engine.ts:173-189`). This is a **redundant duplicate** that fires every time the engine is re-initialised on an already-compiled circuit. Wave A step 4 removes it so the compiler is the sole call site.
+
+Not called from DC-op. The `reset()` path (`analog-engine.ts:195-211`) zero-fills the pool via `statePool.reset()` but does not re-run `initState`, meaning non-zero initial values (FET `GM/GDS`, BJT `RB_EFF`, first-call flags) regress to 0 after reset. Wave A step 5 fixes this by re-running `initState` on all pool-backed elements after `cac.statePool.reset()`.
```

Now let me compose the full diff in one clean block with all amendments, using exact text matches from what I read:

---

```
--- a/spec/state-pool-schema.md
+++ b/spec/state-pool-schema.md

# Amendment L: Explicit-defaults rule — add note after "What is NOT added" paragraph

@@ -22,6 +22,9 @@
 **What is NOT added:** no map lookups, no string keys at runtime, no per-call dispatch, no base class for reactive elements (optional mixin instead), no schema registry. Slot reads remain `this.s0[this.base + SLOT_GEQ]` — byte-identical to today's `capacitor.ts`.
 
+**Explicit-defaults rule:** Every slot MUST declare an explicit `init` kind. Zero-initialised slots use `{ kind: 'zero' }` — there is no implicit default. The `SlotDescriptor.init` field is non-optional precisely to enforce this at the type level.
+
 ### 1.2 The interface — `src/solver/analog/state-schema.ts` (new file)

# Amendment C (part 1): Fix applyInitialValues call-site comment in Section 1.2 code block

@@ -99,8 +102,9 @@
 /**
  * Apply `SlotInit` entries to the backing Float64Array starting at `base`.
  * Called exactly once per element from its initState(). Runs at compile time
- * (via compiler.ts:1310-1312), never per step.
+ * (via compiler.ts:1310-1312 — the sole authoritative call site after Wave A
+ * removes the redundant analog-engine.ts:186 call), never per step.
  *
  * `params` is the element's resolved param record, used by `fromParams` slots.
  * Pass an empty object if the element has no param-dependent initial values.

# Amendment D: Add ReactiveAnalogElement sub-interface — insert before section 1.3 heading

@@ -173,6 +176,23 @@
 
 ### 1.3 Amendment to `AnalogElement` in `src/solver/analog/element.ts`
 
+Also add the following sub-interface to `element.ts` (immediately after `AnalogElementCore`):
+
+```typescript
+/**
+ * Sub-interface for reactive elements (isReactive: true) that participate in
+ * the declarative state-pool schema contract. All reactive elements MUST
+ * implement this interface. stateSchema is non-optional here — every reactive
+ * element must declare its full slot layout.
+ *
+ * Stateless non-reactive elements (resistors, voltage sources, etc.) satisfy
+ * the base AnalogElementCore interface and may omit stateSchema.
+ */
+export interface ReactiveAnalogElement extends AnalogElementCore {
+  readonly isReactive: true;
+  readonly stateSchema: import("./state-schema.js").StateSchema; // non-optional
+}
+```
+
+Section 2 mandate: all elements with `isReactive: true` MUST implement `ReactiveAnalogElement`. The compiler MAY enforce this with a type assertion in the Wave A commit.
+
 Replace the `stateSize` / `initState` JSDoc at `element.ts:235-248` with:

# Amendment C (part 2): Fix element.ts JSDoc call-site reference in Section 1.3 code block

@@ -197,7 +216,7 @@
   /**
    * Bind to state pool after allocation. Called once by the compiler at
-   * compiler.ts:1310-1312. Contract:
+   * compiler.ts:1310-1312 (sole call site — analog-engine.ts:186 removed in Wave A). Contract:
    *   - Pool contents are guaranteed zero on entry (fresh StatePool).
    *   - Must cache pool reference + base offset for hot-path access.
    *   - Must call applyInitialValues(schema, pool, base, params) if a schema

# Amendment A: Fix call-site claim in Section 2 + promote reset() gap

@@ -283,7 +302,14 @@
 - `this.stateBaseOffset` is guaranteed valid (≥ 0) when `initState` is called.
 
-**Call sites:** exactly one — `compiler.ts:1310-1312`. Not called from `analog-engine.ts:init` (that only rebinds pool refs for re-use, see `analog-engine.ts:175-187`), not called from DC-op, not called from `reset()` (`analog-engine.ts:195-211` — reset uses `statePool.reset()` which zero-fills, meaning **on reset, initial values are lost**). Flagged in section 6 as an open issue, but the fix is out of scope for this spec: a follow-up must either (a) have `reset()` re-invoke `initState` for all elements, or (b) for now, rely on the observation that every current reset is followed by a fresh transient that re-converges naturally.
+**Call sites:** two currently exist, but Wave A reduces this to one:
+1. `compiler.ts:1310-1312` — the authoritative call site, runs on a freshly-allocated pool.
+2. `analog-engine.ts:186` — `el.initState?.(cac.statePool)` inside the engine `init()` loop (lines 173–189). **Wave A step 4 removes this line.** It fires every time `MNAEngine.init()` is called on an already-compiled circuit, re-seeding the pool with initial values on top of whatever state the element had accumulated during a prior run. Removing it makes the compiler the sole call site.
+
+Not called from DC-op. The `reset()` path at `analog-engine.ts:195-211` calls `cac.statePool.reset()` (which zero-fills `state0`) but does not re-run `initState`. **Wave A step 5 fixes this:** after `cac.statePool.reset()` at `analog-engine.ts:~202`, iterate all pool-backed elements and call `el.initState?.(cac.statePool)` to re-seed non-zero initial values (FET `GM/GDS = 1e-12`, BJT `RB_EFF = params.RB`, first-call flags = 1.0, JFET `GD_JUNCTION = GMIN`). Without this fix, every reset regresses these sentinels to 0, causing wrong behaviour on the first post-reset timestep.

# Amendment C (part 3): Rewrite Section 3 probe specification

@@ -299,22 +318,40 @@
 **Probe specification:**
 
 Location: `src/solver/analog/state-schema.ts` (already included in section 1.2 as `assertPoolIsSoleMutableState`).
 
-When it runs: once per element, immediately after `compiler.ts:1311` calls `element.initState(statePool)`. Gated by `import.meta.env?.DEV` (Vite convention, already used elsewhere in the repo — verify in `compiler.ts`; if absent, use `process.env.NODE_ENV !== "production"`).
-
-What it does:
-1. Snapshot every enumerable own numeric field on the element instance into a `Map<string, number>`.
-2. Invoke `element.stamp(mockSolver)` and, if reactive, `element.stampCompanion(1e-9, "TRAPEZOIDAL", zeroVoltages)`, and if nonlinear, `element.updateOperatingPoint(zeroVoltages)`. Use a minimal mock `SparseSolver` that no-ops on `stampMatrix`/`stampRHS` (the existing `sparse-solver.ts` can be instantiated with `matrixSize = nodeCount`; pass the real solver the compiler already built).
-3. Re-snapshot. Any numeric field that changed but is not `stateBaseOffset` (which the compiler mutates legitimately) is a violation.
-4. Emit a compiler diagnostic with code `reactive-state-outside-pool`, severity `error`, referencing the field name and element owner.
+When it runs: **once per element, on the first engine `step()` call**, via a one-shot flag in the engine. NOT at `compiler.ts:1312`. Rationale: at compile time the real solver is not yet built; invoking `stamp`/`stampCompanion` at `compiler.ts:1312` would require a fake solver stub and would run against zero voltages, causing many nonlinear elements to throw or produce degenerate NaN results. By deferring to the first step, the probe runs with the real solver the engine already owns and with voltages that have been through at least one DC operating-point solve. Gated by `import.meta.env?.DEV`.
+
+Invocation site: in `MNAEngine.step()`, guarded by a one-shot `_probeRan: boolean` flag initialised to `false` in `init()`. On the first call to `step()`, the engine iterates all elements with `stateSchema` defined, calls `assertPoolIsSoleMutableState(owner, element, () => { element.stamp(this._solver); if (element.isReactive) element.stampCompanion(dt, method, this._voltages); if (element.isNonlinear) element.updateOperatingPoint(this._voltages); })`, collects violations, and emits them as engine diagnostics. Sets `_probeRan = true` so subsequent steps skip it.
+
+The `run` callback uses the **real solver and real voltages** the engine already holds — no mock stub required.
+
+**Known limitation:** `assertPoolIsSoleMutableState` uses `Object.keys(element)` which sees own enumerable properties only. Closure-based factory elements (such as the `diode.ts` factory which holds `capFirstCall` as a closure variable) have closure state that is invisible to `Object.keys`. The probe catches class-based instance field leaks; it does NOT catch closure-variable leaks. This is a documented limitation. Amendment E2 (diode `CAP_FIRST_CALL` slot) migrates the one known closure leak into the pool where the probe can then see it.
+
+What it does:
+1. Snapshot every enumerable own numeric field on the element instance into a `Map<string, number>`.
+2. Invoke the `run` callback (which calls `stamp`, and conditionally `stampCompanion` / `updateOperatingPoint` using the engine's real solver and voltages).
+3. Re-snapshot. Any numeric field that changed but is not `stateBaseOffset` (which the compiler mutates legitimately) is a violation.
+4. Emit an engine diagnostic with code `reactive-state-outside-pool`, severity `error`, referencing the field name and element owner.

# Amendment B: Promote reset() re-init from open issue to Wave A task

@@ -493,6 +530,8 @@
 **Wave A — Infrastructure (one commit, no functional change):**
 1. Create `src/solver/analog/state-schema.ts` (file from section 1.2 verbatim).
 2. Update `element.ts:235-248` JSDoc and add the optional `stateSchema` field (section 1.3). Also add the `ReactiveAnalogElement` sub-interface (section 1.3 addition).
 3. Add the dev-probe invocation at the end of `compiler.ts:1312`, inside `if (import.meta.env?.DEV)`. It iterates every `analogElements` entry, runs `assertPoolIsSoleMutableState`, and pushes any returned violations as diagnostics onto the existing `diagnostics` array.
-4. Run full test suite. No element has yet adopted the schema, so no violations fire. Expected: green.
+4. **Remove the redundant `initState` call from `analog-engine.ts:186`:** delete the line `el.initState?.(cac.statePool);` inside the `if (el.stateSize > 0)` block at lines 173–189 of `analog-engine.ts`. The compiler is now the sole call site for `initState`.
+5. **Fix `reset()` re-init gap in `analog-engine.ts:195-211`:** after the `cac.statePool.reset()` call at `analog-engine.ts:~202`, add an iteration over pool-backed elements that re-calls `el.initState?.(cac.statePool)` for every element with `stateSize > 0`. This re-seeds non-zero initial values (NaN sentinels, first-call flags, `fromParams` values) that `statePool.reset()` zero-fills. Without this, a reset followed by a new transient starts with wrong seeds. Example insertion point (immediately after `if (cac?.statePool) cac.statePool.reset();`):
+   ```typescript
+   if (cac?.statePool) {
+     cac.statePool.reset();
+     for (const el of elements) {
+       if (el.stateSize > 0) el.initState?.(cac.statePool);
+     }
+   }
+   ```
+   Note: `elements` must be in scope; bind it at `init()` time as `this._elements` alongside the existing `this._compiled`.
+6. Add the dev-probe one-shot flag `_probeRan` to `MNAEngine` and wire the first-step probe call as described in section 3.
+7. Run full test suite. No element has yet adopted the schema, so no violations fire. Expected: green.

# Amendment F: Correct transformer slot count to 13

@@ -474,7 +520,13 @@
-| `transformer.ts` | 10 | `G11`, `G22`, `G12`, `HIST1`, `HIST2`, `PREV_I1`, `PREV_I2`, `PREV_PREV_I1`, `PREV_PREV_I2`, `FIRST_CALL` |
+| `transformer.ts` | 13 | `G11`, `G22`, `G12`, `HIST1`, `HIST2`, `PREV_I1`, `PREV_I2`, `PREV_V1`, `PREV_V2`, `PREV_PREV_I1`, `PREV_PREV_I2`, `PREV_PREV_V1`, `PREV_PREV_V2` |

# Amendment G: Correct tapped-transformer slot count to 12

@@ -475,7 +521,7 @@
-| `tapped-transformer.ts` | 15 | `G11`, `G22`, `G33`, `G12`, `G13`, `G23`, `HIST1`, `HIST2`, `HIST3`, `PREV_I1`, `PREV_I2`, `PREV_I3`, `PREV_PREV_I1`, `PREV_PREV_I2`, `PREV_PREV_I3` (14 if no first-call flag is needed; verify against `transformer.ts` pattern at read-time and add `FIRST_CALL` as slot 15 if required) |
+| `tapped-transformer.ts` | 12 | `G11`, `G22`, `G33`, `G12`, `G13`, `G23`, `HIST1`, `HIST2`, `HIST3`, `PREV_I1`, `PREV_I2`, `PREV_I3` |

# Amendment E2: Correct diode slot count to 4/8 and document CAP_FIRST_CALL

@@ -478,7 +524,11 @@
-| `diode.ts` | current stateSize 4 or 7 (`diode.ts:161`). Add `CAP_FIRST_CALL` as a new slot at the tail (value 1.0 on init via `constant: 1.0`), making stateSize 5 or 8. `schottky.ts` inherits automatically. |
+| `diode.ts` | 4 (resistive) or **8** (capacitive). Slots 0–3 always present: `VD`, `GEQ`, `IEQ`, `ID`. Slots 4–7 capacitive only: `CAP_GEQ`, `CAP_IEQ`, `VD_PREV`, `CAP_FIRST_CALL`. The `CAP_FIRST_CALL` slot (index 7) replaces the closure variable `capFirstCall` at `diode.ts:155`. Seed: `{ kind: "constant", value: 1.0 }` — matching `bjt.ts:773` `L1_SLOT_CAP_FIRST_CALL` convention (1.0 = first call pending, flipped to 0 after first `stampCompanion`). `initState` for capacitive diode writes: `s0[base + SLOT_CAP_FIRST_CALL] = 1.0` (the schema's `applyInitialValues` handles this; the existing `s0[base + SLOT_GEQ] = GMIN` at `diode.ts:168` must also be expressed as `{ kind: "constant", value: GMIN }` in the schema). Closure variable `capFirstCall` is deleted. `schottky.ts` inherits automatically. **Note:** the probe's `Object.keys` snapshot cannot see `capFirstCall` as a closure variable — this migration is required to make the probe effective for diode. |

# Amendment F (continued): Add migration guidance note for transformer

@@ -484,6 +534,13 @@
 For `transformer.ts` and `tapped-transformer.ts`, the companion-matrix entries (`_g11` etc.) and history entries (`_hist1` etc.) are re-stamped every `stampCompanion` call from the `CoupledInductorPair` state, so they're classic pool candidates. The `_pair` and `_state` objects themselves are instance-owned infrastructure — if they contain mutable numeric state, they too must migrate. **Executor: before migrating transformer, read `coupled-inductor.ts` to determine whether `CoupledInductorState` is a Float64Array view into a pool (good) or plain numeric fields (also a violation that must be migrated together).** This is the one spot where the executor needs to read one more file before committing to final slot names.
+
+**Pre-read finding (no executor read needed):** `CoupledInductorState` (`coupled-inductor.ts:29-37`) is a plain object interface with scalar fields `prevI1`, `prevI2`, `prevV1`, `prevV2`, `prevPrevI1?`, `prevPrevI2?`, `prevPrevV1?`, `prevPrevV2?`. It is NOT a pool view. All 8 fields plus the 5 instance companion fields (`_g11`, `_g22`, `_g12`, `_hist1`, `_hist2`) are violators — total 13 slots. Migration deletes `this._state: CoupledInductorState` and `this._pair.updateState(...)` at `transformer.ts:349`. `CoupledInductorPair` itself is not deleted — it holds only immutable physical parameters (`l1`, `l2`, `m`) and is safe to keep. The transformer inlines all history tracking via its 13 pool slots.

# Amendment H: Reframe polarized-cap and transmission-line as add-pool-from-scratch

@@ -507,7 +557,14 @@
 10. `transmission-line.ts` (four sub-element classes).
+
+**Important reframe for steps 7 and 10 (Amendment H):** `AnalogPolarizedCapElement` and all four `transmission-line.ts` sub-element classes (`SegmentInductorElement`, `SegmentCapacitorElement`, `CombinedRLElement`, plus the outer `TransmissionLineElement`) have **no existing pool infrastructure** — no `stateSize`, no `stateBaseOffset`, no `initState`. The compiler's `element.stateSize ?? 0` fallback at `compiler.ts:1301` silently gives them zero pool allocation today. Migration is NOT a rename — it requires **adding the full pool interface from scratch**: declare `stateSize`, `stateBaseOffset = -1`, `initState`, and the schema. Violator fields for each class (all confirmed in current source):
+- `AnalogPolarizedCapElement` (`polarized-cap.ts:221-223`): `geq`, `ieq`, `vPrev` — 3 slots.
+- `SegmentInductorElement` (`transmission-line.ts:~294-296`): `geq`, `ieq`, `iPrev` — 3 slots.
+- `SegmentCapacitorElement` (`transmission-line.ts:~357-359`): `geq`, `ieq`, `vPrev` — 3 slots.
+- `CombinedRLElement` (`transmission-line.ts:~413-415`): `geqL`, `ieq`, `iPrev` — 3 slots.
+
+For reviewability, split the transmission-line migration (step 10) into **one commit per sub-element class** rather than one large commit.

# Amendment I: Replace FET JFET flatten with base-plus-extension composition

@@ -479,9 +543,19 @@
-| `njfet.ts` (+ `pjfet.ts` inherits) | add 3 slots to `AbstractFetElement` schema (currently 25 → 28): `JFET_VGS_JUNCTION`, `JFET_GD_JUNCTION` (init `fromParams` = `GMIN`), `JFET_ID_JUNCTION`. **Important:** only JFETs use these; MOSFETs leave them zero. This is acceptable — they are slots 25-27 at the tail. Alternatively, keep a separate JFET-only schema that extends the FET base. Executor decision: **extend the existing `AbstractFetElement` schema to 28 slots and let MOSFET leave the JFET junction slots zero-initialised**, because a single schema per base class is simpler than a schema-union type. |
+| `njfet.ts` / `pjfet.ts` (via `AnalogJfetElement` base) | **Base-plus-extension composition.** `AbstractFetElement` keeps its 25-slot base schema unchanged. MOSFET instances stay at 25 slots. `AnalogJfetElement` (the shared base for `njfet`/`pjfet`) declares a 3-slot **extension schema** for `VGS_JUNCTION`, `GD_JUNCTION` (`init: { kind: "constant", value: GMIN }` — current default at `njfet.ts:119`), `ID_JUNCTION`. Total for JFET instances: 28 slots (25 base + 3 extension). No unused slots on MOSFET. Mechanism: `stateSchema` for JFET classes is `defineStateSchema("AnalogJfetElement", [...FET_BASE_SCHEMA.slots, ...JFET_EXTENSION_SLOTS])` — standard tuple spreading at the declaration site. No change to `defineStateSchema` signature. `stateSize` for JFET = 28. Fields `_vgs_junction`, `_gd_junction`, `_id_junction` at `njfet.ts:118-120` are migrated to pool slots 25–27. |

@@ -480,6 +558,6 @@
-| `mosfet.ts` / `fet-base.ts` | keep the existing 25-slot layout from `fet-base.ts:46-74`, add `VSB` and `GMBS` formally into the schema (already slots 23-24 on disk per the in-flight migration, see section 5), extend to 28 for JFET as above → **final FET base stateSize: 28**. |
+| `mosfet.ts` / `fet-base.ts` | keep the existing 25-slot layout from `fet-base.ts:46-74`, add `VSB` and `GMBS` formally into the schema (already slots 23-24 on disk per the in-flight migration, see section 5). **Final MOSFET stateSize: 25.** JFET extension is declared separately (see row above) — the base schema is NOT extended to 28. |

# Amendment I (section 5.3 follow-up paragraph): Replace JFET flatten with extension

@@ -529,6 +607,8 @@
-> Open `src/solver/analog/fet-base.ts`. Immediately above the `const SLOT_VGS = 0;` block at line 48, insert a `defineStateSchema` call listing all 25 current slot names in order, with `init: { kind: "zero" }` for most slots, `init: { kind: "constant", value: 1e-12 }` for `GM` and `GDS`, `init: { kind: "constant", value: NaN }` for `VGS_PREV` and `VGD_PREV`, and `init: { kind: "constant", value: 1.0 }` for `CAP_JUNCTION_FIRST_CALL` and `CAP_GB_FIRST_CALL`. Add `readonly stateSchema = FET_BASE_SCHEMA; readonly stateSize = FET_BASE_SCHEMA.size;` to the class, replacing the hardcoded `stateSize: number = 25`. Replace the imperative body of `initState` (lines 143-163) with a single `applyInitialValues(FET_BASE_SCHEMA, pool, this.stateBaseOffset, {});` call. Do not touch `mosfet.ts` — its slot-constant uses via `AbstractFetElement.SLOT_CAP_GEQ_DB` continue to work unchanged because the static class constants remain in place as a backward-compatible aliases. Extend the schema to 28 slots (`JFET_VGS_JUNCTION`, `JFET_GD_JUNCTION` init `{ kind: "constant", value: GMIN }`, `JFET_ID_JUNCTION`) in the same commit so `njfet.ts` can migrate off its `protected _vgs_junction` fields in Wave C step 13.
+>
+> **JFET extension is NOT added to the base schema.** After migrating `AbstractFetElement` to a 25-slot schema, create `AnalogJfetElement` (new abstract base for `njfet`/`pjfet`) in `fet-base.ts` or `njfet.ts`. Declare `JFET_EXTENSION_SCHEMA = defineStateSchema("AnalogJfetElement.extension", [...])` with the 3 JFET-specific slots. `AnalogJfetElement.stateSchema = defineStateSchema("AnalogJfetElement", [...FET_BASE_SCHEMA.slots, ...JFET_EXTENSION_SCHEMA.slots])` and `stateSize = 28`. MOSFET-derived classes (`AnalogNMosfetElement`, `AnalogPMosfetElement`) keep `stateSize = 25` and do NOT use the extension. Fields `_vgs_junction`, `_gd_junction`, `_id_junction` at `njfet.ts:118-120` migrate to pool slots 25, 26, 27.

# Amendment E (retrofit Wave E): Add new Wave E section

@@ -511,6 +593,42 @@
 **Wave D — MOSFET finalisation:**
 14. See section 5.3 below.
 
+**Wave E — Retrofit stateSchema declarations on all already-pool-compliant reactive elements (one commit per element):**
+
+All elements migrated in Waves 1–4 (before this spec) are already pool-compliant at the implementation level but lack a `stateSchema` declaration. This wave adds `stateSchema` to each, making them `ReactiveAnalogElement`-conformant and enabling the dev probe. Each element also adopts `{ kind: "zero" }` for every zero-initialised slot (per the explicit-defaults rule). Elements to retrofit:
+
+15. `capacitor.ts` — 6 slots, all zero-init. (Covered by Wave B step 5 above — Wave E confirms it as the reference.)
+16. `inductor.ts` — 4 slots, all zero-init.
+17. `bjt.ts` simple factory — 10 slots, all zero-init.
+18. `bjt.ts` L1 factory — 24 slots. Reference layout: `bjt.ts:713-774`. `L1_SLOT_RB_EFF = 10` → `{ kind: "fromParams", compute: p => p.RB ?? 0 }`; `L1_SLOT_CAP_FIRST_CALL = 23` → `{ kind: "constant", value: 1.0 }`. All other slots zero-init. This is the canonical pattern for mixed `fromParams` + `constant` + `zero` slots.
+19. `fet-base.ts` — 25 slots (post Wave D). `VGS_PREV`, `VGD_PREV` → `{ kind: "constant", value: NaN }`; `GM`, `GDS` → `{ kind: "constant", value: 1e-12 }`; `CAP_JUNCTION_FIRST_CALL`, `CAP_GB_FIRST_CALL` → `{ kind: "constant", value: 1.0 }`; all others zero-init.
+20. `diode.ts` — 4 or 8 slots (post Amendment E2). `CAP_FIRST_CALL` slot → `{ kind: "constant", value: 1.0 }`.
+21. `zener.ts`, `tunnel-diode.ts`, `varactor.ts`, `scr.ts`, `triac.ts` — executor must verify slot counts by reading each file. Apply `{ kind: "zero" }` for all zero-init slots.
+
+**BJT reference note (Amendment K):** `bjt.ts` is fully committed (Wave 3.2), pool-compliant, and the diff against HEAD is empty. `bjt.ts:713-774` is the canonical reference for the 24-slot L1 pattern. Confirmed: `L1_SLOT_RB_EFF = 10`, `L1_SLOT_CAP_FIRST_CALL = 23`.

# Amendment J: Add non-violations section

@@ -549,6 +637,57 @@
 ---
 
 ## 6. Open questions / explicit non-goals
 
+### 6.1 Scope decisions: confirmed non-violations
+
+The following classes were investigated and are **correctly carved out** from the pool migration. They are NOT violations.
+
+**`digital-pin-model.ts` — `_prevVoltage`, `_prevCurrent`:**
+`DigitalOutputPinModel` (lines 62–63) and `DigitalInputPinModel` (lines 274–275) hold `_prevVoltage` / `_prevCurrent` as instance fields. These are written only in `updateCompanion`, which runs on the accepted-timestep path — never inside NR-iteration methods (`stamp`, `stampCompanion`, `stampNonlinear`, `updateOperatingPoint`). Because NR rollback only matters for state written during the NR retry loop, and these fields are updated only after a timestep is accepted, they are exempt from the pool requirement. **Action:** add inline comments at the field declarations in `digital-pin-model.ts` documenting this exemption so future maintainers do not silently break the invariant.
+
+**Behavioral flipflop classes (`behavioral-flipflop.ts` and variants):**
+Fields `_prevClockVoltage`, `_latchedQ`, `_count`, `_storedValue` in `behavioral-flipflop.ts`, `behavioral-flipflop/d-async.ts`, `jk.ts`, `jk-async.ts`, `rs.ts`, `rs-async.ts`, `t.ts`, and `behavioral-sequential.ts` are event-detection state. They are written only in `updateCompanion` (never in `stampCompanion` / `stampNonlinear` / `updateOperatingPoint`). Correctly carved out. **Action:** the existing file header comment at `behavioral-flipflop.ts:4-12` already documents this design; for variant files that lack an equivalent header, add a one-liner comment: `// State fields written only in updateCompanion (accepted-timestep path) — pool exemption valid.`
+
+> **See also (separate defect, not in scope):** investigation surfaced that `updateCompanion` on behavioral flipflop elements may not be called by the engine's dispatch path (`updateState` is the post-accept hook; the flipflop `updateState` implementations are intentional no-ops). Whether `updateCompanion` is wired via a separate path (coordinator bridge, test harness, or another hook) is unverified. This is a separate functional concern unrelated to the state-pool schema and MUST NOT be addressed in this spec. Track as a separate follow-up.

# Amendment J (continued): Update the open-questions bullet about reset() to reference the Wave A fix

@@ -546,8 +700,7 @@
 **Open questions the executor must NOT resolve (punt to a follow-up):**
 
-- `reset()` in `analog-engine.ts:195-211` zero-fills the pool but does not re-run `initState`. Any element with a non-zero initial value (FET `GM`/`GDS = 1e-12`, BJT `RB_EFF = params.RB`, MOSFET first-call flags, JFET `GD_JUNCTION = GMIN`) will start the next transient with wrong seed values. This is a pre-existing bug (not introduced by the spec) and is out of scope. Flag it in the Wave A commit message as a known follow-up.
+- ~~`reset()` re-init gap~~ — **Promoted to Wave A step 5.** See section 5.1 for the concrete fix at `analog-engine.ts:~202`.
 - The non-rollback-sensitive `_reverseBiasDiagEmitted` latch in `polarized-cap.ts:226` is intentionally kept on the instance.

# Amendment A/F/G: Fix the "Out of scope" line for analog-engine.ts in the header

@@ -557,6 +711,7 @@
 - `src/solver/analog/analog-engine.ts:175-187` — init-time `initState` binding loop.
+- `src/solver/analog/analog-engine.ts:186` — redundant `initState` call removed in Wave A step 4.
 - `src/solver/analog/analog-engine.ts:248-254` — step-top checkpoint (`_stateCheckpoint.set(statePool.state0)`).
 - `src/solver/analog/analog-engine.ts:297-302, 369-371` — rollback paths the spec exists to feed.
-- `src/solver/analog/analog-engine.ts:195-211` — `reset()` which zero-fills but does not re-init (flagged open question).
+- `src/solver/analog/analog-engine.ts:195-211` — `reset()` fixed in Wave A step 5 to re-run `initState` after zero-fill.
 - `src/solver/analog/compiler.ts:1298-1312` — state-pool allocation loop; the dev probe attaches at line 1312.
+- `src/solver/analog/coupled-inductor.ts:29-37` — `CoupledInductorState` plain-object interface (all 8 fields migrate to transformer pool slots in Wave C step 11).
```

---

Now let me write the clean, consolidated unified diff:

```
--- a/spec/state-pool-schema.md
+++ b/spec/state-pool-schema.md

# ═══════════════════════════════════════════════════════════════════
# Amendment L: Explicit-defaults clarification
# ═══════════════════════════════════════════════════════════════════

@@ -22,6 +22,9 @@
 **What is NOT added:** no map lookups, no string keys at runtime, no per-call dispatch, no base class for reactive elements (optional mixin instead), no schema registry. Slot reads remain `this.s0[this.base + SLOT_GEQ]` — byte-identical to today's `capacitor.ts`.
 
+**Explicit-defaults rule:** Every slot MUST declare an explicit `init` kind. Zero-initialised slots use `{ kind: 'zero' }` — there is no implicit default. The `SlotDescriptor.init` field is non-optional precisely to enforce this at the type level.
+
 ### 1.2 The interface — `src/solver/analog/state-schema.ts` (new file)

# ═══════════════════════════════════════════════════════════════════
# Amendment C (part 1): Fix applyInitialValues comment in Section 1.2
# ═══════════════════════════════════════════════════════════════════

@@ -99,7 +102,7 @@
  * Apply `SlotInit` entries to the backing Float64Array starting at `base`.
  * Called exactly once per element from its initState(). Runs at compile time
- * (via compiler.ts:1310-1312), never per step.
+ * (via compiler.ts:1310-1312 — the sole authoritative call site after Wave A removes the redundant analog-engine.ts:186 call), never per step.
  *
  * `params` is the element's resolved param record, used by `fromParams` slots.
  * Pass an empty object if the element has no param-dependent initial values.

# ═══════════════════════════════════════════════════════════════════
# Amendment D: Add ReactiveAnalogElement sub-interface (Section 1.3)
# ═══════════════════════════════════════════════════════════════════

@@ -173,6 +176,24 @@
 
 ### 1.3 Amendment to `AnalogElement` in `src/solver/analog/element.ts`
 
+Also add the following sub-interface to `element.ts` (immediately after `AnalogElementCore`):
+
+```typescript
+/**
+ * Sub-interface for reactive elements (isReactive: true) that participate in
+ * the declarative state-pool schema contract. All reactive elements MUST
+ * implement this interface. stateSchema is non-optional here — every reactive
+ * element must declare its full slot layout.
+ *
+ * Stateless non-reactive elements (resistors, voltage sources, etc.) satisfy
+ * the base AnalogElementCore interface and may omit stateSchema.
+ */
+export interface ReactiveAnalogElement extends AnalogElementCore {
+  readonly isReactive: true;
+  readonly stateSchema: import("./state-schema.js").StateSchema; // non-optional
+}
+```
+
+Section 2 mandate: all elements with `isReactive: true` MUST implement `ReactiveAnalogElement`. The compiler MAY enforce this with a type assertion in the Wave A commit.
+
 Replace the `stateSize` / `initState` JSDoc at `element.ts:235-248` with:

# ═══════════════════════════════════════════════════════════════════
# Amendment C (part 2): Fix initState JSDoc call-site ref in Section 1.3
# ═══════════════════════════════════════════════════════════════════

@@ -197,7 +216,7 @@
   /**
    * Bind to state pool after allocation. Called once by the compiler at
-   * compiler.ts:1310-1312. Contract:
+   * compiler.ts:1310-1312 (sole call site — analog-engine.ts:186 removed in Wave A step 4). Contract:
    *   - Pool contents are guaranteed zero on entry (fresh StatePool).
    *   - Must cache pool reference + base offset for hot-path access.
    *   - Must call applyInitialValues(schema, pool, base, params) if a schema

# ═══════════════════════════════════════════════════════════════════
# Amendment A: Fix call-site count + promote reset() fix — Section 2
# ═══════════════════════════════════════════════════════════════════

@@ -283,7 +302,16 @@
 - `this.stateBaseOffset` is guaranteed valid (≥ 0) when `initState` is called.
 
-**Call sites:** exactly one — `compiler.ts:1310-1312`. Not called from `analog-engine.ts:init` (that only rebinds pool refs for re-use, see `analog-engine.ts:175-187`), not called from DC-op, not called from `reset()` (`analog-engine.ts:195-211` — reset uses `statePool.reset()` which zero-fills, meaning **on reset, initial values are lost**). Flagged in section 6 as an open issue, but the fix is out of scope for this spec: a follow-up must either (a) have `reset()` re-invoke `initState` for all elements, or (b) for now, rely on the observation that every current reset is followed by a fresh transient that re-converges naturally.
+**Call sites:** two currently exist, reduced to one by Wave A:
+1. `compiler.ts:1310-1312` — the authoritative call site, on a freshly-allocated pool.
+2. `analog-engine.ts:186` — `el.initState?.(cac.statePool)` inside the engine `init()` loop (lines 173–189). This is a **redundant duplicate** that fires every time `MNAEngine.init()` is called on an already-compiled circuit, re-seeding the pool on top of accumulated state. **Wave A step 4 removes this line**, making the compiler the sole call site.
+
+Not called from DC-op.
+
+**`reset()` re-init gap (now a Wave A fix, not an open issue):** `analog-engine.ts:195-211` calls `cac.statePool.reset()` which zero-fills `state0`, but does not re-run `initState`. Elements with non-zero initial values — FET `GM/GDS = 1e-12` (`fet-base.ts:148-149`), BJT `RB_EFF = params.RB` (`bjt.ts:770`), first-call flags = 1.0 (`bjt.ts:773`), JFET `GD_JUNCTION = GMIN` — regress to 0 after reset, causing wrong behaviour on the first post-reset timestep. **Wave A step 5 fixes this** by iterating pool-backed elements after `cac.statePool.reset()` and calling `el.initState?.(cac.statePool)` to re-seed. See section 5.1 for the code.

# ═══════════════════════════════════════════════════════════════════
# Amendment C (part 3): Rewrite Section 3 probe specification
# ═══════════════════════════════════════════════════════════════════

@@ -301,19 +320,36 @@
 **Probe specification:**
 
 Location: `src/solver/analog/state-schema.ts` (already included in section 1.2 as `assertPoolIsSoleMutableState`).
 
-When it runs: once per element, immediately after `compiler.ts:1311` calls `element.initState(statePool)`. Gated by `import.meta.env?.DEV` (Vite convention, already used elsewhere in the repo — verify in `compiler.ts`; if absent, use `process.env.NODE_ENV !== "production"`).
-
-What it does:
-1. Snapshot every enumerable own numeric field on the element instance into a `Map<string, number>`.
-2. Invoke `element.stamp(mockSolver)` and, if reactive, `element.stampCompanion(1e-9, "TRAPEZOIDAL", zeroVoltages)`, and if nonlinear, `element.updateOperatingPoint(zeroVoltages)`. Use a minimal mock `SparseSolver` that no-ops on `stampMatrix`/`stampRHS` (the existing `sparse-solver.ts` can be instantiated with `matrixSize = nodeCount`; pass the real solver the compiler already built).
-3. Re-snapshot. Any numeric field that changed but is not `stateBaseOffset` (which the compiler mutates legitimately) is a violation.
-4. Emit a compiler diagnostic with code `reactive-state-outside-pool`, severity `error`, referencing the field name and element owner.
+When it runs: **once per element, on the first engine `step()` call**, via a one-shot `_probeRan` flag initialised to `false` in `MNAEngine.init()`. NOT at `compiler.ts:1312`.
+
+Rationale for deferred invocation: the real `SparseSolver` is not constructed until `MNAEngine.init()`. Invoking `stamp`/`stampCompanion` at compile time would require a fake solver stub and would run against zero voltages, causing nonlinear elements (diode, BJT, FET) to produce degenerate or NaN results or throw. By deferring to the first step, the probe runs with the real solver the engine already owns and with voltages that have passed through at least one DC operating-point solve. Gated by `import.meta.env?.DEV`.
+
+Invocation in `MNAEngine.step()`: on the first call (`!this._probeRan`), iterate all elements that have `stateSchema` defined and call:
+```typescript
+assertPoolIsSoleMutableState(el.stateSchema.owner, el, () => {
+  el.stamp(this._solver);
+  if (el.isReactive) el.stampCompanion(dt, method, this._voltages);
+  if (el.isNonlinear) el.updateOperatingPoint(this._voltages);
+});
+```
+Collect all returned violations and emit them as engine diagnostics (severity `error`, code `reactive-state-outside-pool`). Set `this._probeRan = true`.
+
+**Known limitation — closure variables:** `assertPoolIsSoleMutableState` snapshots via `Object.keys(element)`, which sees only own enumerable properties. Closure-based factory elements (such as `diode.ts`, where `capFirstCall` is a closure variable at line 155) have closure state invisible to this probe. The probe reliably catches class-based instance field leaks; it does NOT catch closure-variable leaks. This is a documented limitation. Amendment E2 migrates the one known closure leak (`diode.ts capFirstCall`) into a pool slot (slot 7, `CAP_FIRST_CALL`), after which the probe is effective for diode.
+
+What it does (unchanged shape):
+1. Snapshot every enumerable own numeric field on the element instance into a `Map<string, number>`.
+2. Invoke the `run` callback (stamp chain using real solver and real voltages).
+3. Re-snapshot. Any numeric field that changed but is not `stateBaseOffset` is a violation.
+4. Emit an engine diagnostic with code `reactive-state-outside-pool`, severity `error`, referencing the field name and element owner.

# ═══════════════════════════════════════════════════════════════════
# Amendment F: Correct transformer slot count to 13 in Section 4.2
# ═══════════════════════════════════════════════════════════════════

@@ -474,7 +510,14 @@
-| `transformer.ts` | 10 | `G11`, `G22`, `G12`, `HIST1`, `HIST2`, `PREV_I1`, `PREV_I2`, `PREV_PREV_I1`, `PREV_PREV_I2`, `FIRST_CALL` |
+| `transformer.ts` | 13 | `G11`, `G22`, `G12`, `HIST1`, `HIST2`, `PREV_I1`, `PREV_I2`, `PREV_V1`, `PREV_V2`, `PREV_PREV_I1`, `PREV_PREV_I2`, `PREV_PREV_V1`, `PREV_PREV_V2` |

# ═══════════════════════════════════════════════════════════════════
# Amendment G: Correct tapped-transformer slot count to 12 in Section 4.2
# ═══════════════════════════════════════════════════════════════════

@@ -475,7 +511,7 @@
-| `tapped-transformer.ts` | 15 | `G11`, `G22`, `G33`, `G12`, `G13`, `G23`, `HIST1`, `HIST2`, `HIST3`, `PREV_I1`, `PREV_I2`, `PREV_I3`, `PREV_PREV_I1`, `PREV_PREV_I2`, `PREV_PREV_I3` (14 if no first-call flag is needed; verify against `transformer.ts` pattern at read-time and add `FIRST_CALL` as slot 15 if required) |
+| `tapped-transformer.ts` | 12 | `G11`, `G22`, `G33`, `G12`, `G13`, `G23`, `HIST1`, `HIST2`, `HIST3`, `PREV_I1`, `PREV_I2`, `PREV_I3` |

# ═══════════════════════════════════════════════════════════════════
# Amendment E2: Correct diode slot count (4/7 → 4/8), add CAP_FIRST_CALL
# ═══════════════════════════════════════════════════════════════════

@@ -478,7 +514,11 @@
-| `diode.ts` | current stateSize 4 or 7 (`diode.ts:161`). Add `CAP_FIRST_CALL` as a new slot at the tail (value 1.0 on init via `constant: 1.0`), making stateSize 5 or 8. `schottky.ts` inherits automatically. |
+| `diode.ts` | 4 (resistive) or **8** (capacitive). Slots always present (0–3): `VD`, `GEQ`, `IEQ`, `ID`. Slots capacitive only (4–7): `CAP_GEQ`, `CAP_IEQ`, `VD_PREV`, `CAP_FIRST_CALL`. The `CAP_FIRST_CALL` slot at index 7 replaces the closure variable `capFirstCall` at `diode.ts:155`. Init: `{ kind: "constant", value: 1.0 }` — matching `bjt.ts:773` convention (1.0 = first call pending, flipped to 0 after first `stampCompanion`). The existing `s0[base + SLOT_GEQ] = GMIN` write at `diode.ts:168` must also be expressed as `{ kind: "constant", value: GMIN }` in the schema. The closure variable `capFirstCall` is deleted and replaced by `s0[base + SLOT_CAP_FIRST_CALL]` reads. `schottky.ts` inherits automatically. **Note:** the `capFirstCall` closure variable is invisible to `Object.keys` — this migration is required to make the probe effective for the capacitive diode path. |

# ═══════════════════════════════════════════════════════════════════
# Amendment I: Replace FET JFET flatten with base-plus-extension in Section 4.2
# ═══════════════════════════════════════════════════════════════════

@@ -479,7 +519,7 @@
-| `njfet.ts` (+ `pjfet.ts` inherits) | add 3 slots to `AbstractFetElement` schema (currently 25 → 28): `JFET_VGS_JUNCTION`, `JFET_GD_JUNCTION` (init `fromParams` = `GMIN`), `JFET_ID_JUNCTION`. **Important:** only JFETs use these; MOSFETs leave them zero. This is acceptable — they are slots 25-27 at the tail. Alternatively, keep a separate JFET-only schema that extends the FET base. Executor decision: **extend the existing `AbstractFetElement` schema to 28 slots and let MOSFET leave the JFET junction slots zero-initialised**, because a single schema per base class is simpler than a schema-union type. |
+| `njfet.ts` / `pjfet.ts` (via `AnalogJfetElement` base) | **Base-plus-extension composition.** `AbstractFetElement` keeps its 25-slot base schema unchanged; MOSFET stays at 25 slots. `AnalogJfetElement` (new abstract base for `njfet`/`pjfet`) declares a 3-slot extension: `VGS_JUNCTION`, `GD_JUNCTION` (`init: { kind: "constant", value: GMIN }` — matching current `njfet.ts:119`), `ID_JUNCTION`. Its `stateSchema` is `defineStateSchema("AnalogJfetElement", [...FET_BASE_SCHEMA.slots, ...JFET_EXTENSION_SLOTS])` — standard tuple spreading, no change to `defineStateSchema` signature. JFET `stateSize = 28`, MOSFET `stateSize = 25`. Fields `_vgs_junction`, `_gd_junction`, `_id_junction` at `njfet.ts:118-120` migrate to pool slots 25–27. |

@@ -480,7 +520,7 @@
-| `mosfet.ts` / `fet-base.ts` | keep the existing 25-slot layout from `fet-base.ts:46-74`, add `VSB` and `GMBS` formally into the schema (already slots 23-24 on disk per the in-flight migration, see section 5), extend to 28 for JFET as above → **final FET base stateSize: 28**. |
+| `mosfet.ts` / `fet-base.ts` | keep the existing 25-slot layout from `fet-base.ts:46-74`, add `VSB` and `GMBS` formally into the schema (already slots 23-24 on disk per the in-flight migration, see section 5). **Final MOSFET stateSize: 25.** JFET extension is declared separately on `AnalogJfetElement` — the FET base schema is NOT extended to 28. |

# ═══════════════════════════════════════════════════════════════════
# Amendment F (continued): Add transformer pre-read note to Section 4.2
# ═══════════════════════════════════════════════════════════════════

@@ -484,6 +524,10 @@
 For `transformer.ts` and `tapped-transformer.ts`, the companion-matrix entries (`_g11` etc.) and history entries (`_hist1` etc.) are re-stamped every `stampCompanion` call from the `CoupledInductorPair` state, so they're classic pool candidates. The `_pair` and `_state` objects themselves are instance-owned infrastructure — if they contain mutable numeric state, they too must migrate. **Executor: before migrating transformer, read `coupled-inductor.ts` to determine whether `CoupledInductorState` is a Float64Array view into a pool (good) or plain numeric fields (also a violation that must be migrated together).** This is the one spot where the executor needs to read one more file before committing to final slot names.
+
+**Pre-read finding (no executor file read required):** `CoupledInductorState` (`coupled-inductor.ts:29-37`) is a plain-object interface with scalar fields `prevI1`, `prevI2`, `prevV1`, `prevV2`, and optional `prevPrevI1`, `prevPrevI2`, `prevPrevV1`, `prevPrevV2`. It is NOT a pool view — all 8 fields are violators. Combined with the 5 instance companion fields (`_g11`, `_g22`, `_g12`, `_hist1`, `_hist2`), the transformer has 13 pool slots total. Migration: delete `this._state: CoupledInductorState` and the `this._pair.updateState(...)` call at `transformer.ts:349`. `CoupledInductorPair` itself is NOT deleted (it holds only immutable physical parameters `l1`, `l2`, `m`). The transformer inlines all history tracking via its 13 pool slots, matching what `AnalogTappedTransformerElement` already does.

# ═══════════════════════════════════════════════════════════════════
# Amendment B: Promote reset() fix to Wave A — Section 5.1 step numbering
# ═══════════════════════════════════════════════════════════════════

@@ -493,8 +533,21 @@
 **Wave A — Infrastructure (one commit, no functional change):**
 1. Create `src/solver/analog/state-schema.ts` (file from section 1.2 verbatim).
 2. Update `element.ts:235-248` JSDoc and add the optional `stateSchema` field (section 1.3). Also add the `ReactiveAnalogElement` sub-interface (section 1.3 addition).
 3. Add the dev-probe invocation at the end of `compiler.ts:1312`, inside `if (import.meta.env?.DEV)`. It iterates every `analogElements` entry, runs `assertPoolIsSoleMutableState`, and pushes any returned violations as diagnostics onto the existing `diagnostics` array.
-4. Run full test suite. No element has yet adopted the schema, so no violations fire. Expected: green.
+4. **Remove the redundant `initState` call from `analog-engine.ts:186`.** Delete the line `el.initState?.(cac.statePool);` inside the `if (el.stateSize > 0)` block at `analog-engine.ts:173-189`. The compiler is now the sole call site for `initState`.
+5. **Fix `reset()` re-init gap at `analog-engine.ts:195-211`.** Replace:
+   ```typescript
+   if (cac?.statePool) cac.statePool.reset();
+   ```
+   with:
+   ```typescript
+   if (cac?.statePool) {
+     cac.statePool.reset();
+     const elements = this._elements; // bound in init() as this._elements = elements
+     for (const el of elements) {
+       if (el.stateSize > 0) el.initState?.(cac.statePool);
+     }
+   }
+   ```
+   This re-seeds NaN sentinels, first-call flags, and `fromParams` values (e.g. `GM/GDS = 1e-12`, `RB_EFF = params.RB`) that `statePool.reset()` would otherwise zero out. Bind `this._elements` in `MNAEngine.init()` alongside `this._compiled`.
+6. Add the dev-probe one-shot flag `_probeRan = false` to `MNAEngine` fields and wire the first-step probe call in `step()` as described in section 3.
+7. Run full test suite. No element has yet adopted the schema, so no violations fire. Expected: green.

# ═══════════════════════════════════════════════════════════════════
# Amendment H: Reframe polarized-cap and transmission-line as add-from-scratch
# ═══════════════════════════════════════════════════════════════════

@@ -505,7 +558,17 @@
 10. `transmission-line.ts` (four sub-element classes).
+
+**Reframe for steps 7 and 10 (Amendment H):** `AnalogPolarizedCapElement` and all sub-element classes in `transmission-line.ts` have **no existing pool infrastructure** — no `stateSize`, no `stateBaseOffset`, no `initState` today. The compiler's `element.stateSize ?? 0` fallback at `compiler.ts:1301` silently gives them zero pool allocation. Migration is an **add-from-scratch**, not a rename. Violator fields:
+- `AnalogPolarizedCapElement` (`polarized-cap.ts:221-223`): `geq`, `ieq`, `vPrev` — 3 slots. Add `stateSize = 3`, `stateBaseOffset = -1`, `initState`, and schema.
+- `SegmentInductorElement` (`transmission-line.ts:~294-296`): `geq`, `ieq`, `iPrev` — 3 slots.
+- `SegmentCapacitorElement` (`transmission-line.ts:~357-359`): `geq`, `ieq`, `vPrev` — 3 slots.
+- `CombinedRLElement` (`transmission-line.ts:~413-415`): `geqL`, `ieq`, `iPrev` — 3 slots.
+
+Split the transmission-line migration (step 10) into **one commit per sub-element class** for reviewability.

# ═══════════════════════════════════════════════════════════════════
# Amendment I (Section 5.3): Replace JFET flatten instruction with extension approach
# ═══════════════════════════════════════════════════════════════════

@@ -529,8 +586,10 @@
-> Open `src/solver/analog/fet-base.ts`. Immediately above the `const SLOT_VGS = 0;` block at line 48, insert a `defineStateSchema` call listing all 25 current slot names in order, with `init: { kind: "zero" }` for most slots, `init: { kind: "constant", value: 1e-12 }` for `GM` and `GDS`, `init: { kind: "constant", value: NaN }` for `VGS_PREV` and `VGD_PREV`, and `init: { kind: "constant", value: 1.0 }` for `CAP_JUNCTION_FIRST_CALL` and `CAP_GB_FIRST_CALL`. Add `readonly stateSchema = FET_BASE_SCHEMA; readonly stateSize = FET_BASE_SCHEMA.size;` to the class, replacing the hardcoded `stateSize: number = 25`. Replace the imperative body of `initState` (lines 143-163) with a single `applyInitialValues(FET_BASE_SCHEMA, pool, this.stateBaseOffset, {});` call. Do not touch `mosfet.ts` — its slot-constant uses via `AbstractFetElement.SLOT_CAP_GEQ_DB` continue to work unchanged because the static class constants remain in place as a backward-compatible aliases. Extend the schema to 28 slots (`JFET_VGS_JUNCTION`, `JFET_GD_JUNCTION` init `{ kind: "constant", value: GMIN }`, `JFET_ID_JUNCTION`) in the same commit so `njfet.ts` can migrate off its `protected _vgs_junction` fields in Wave C step 13.
+> Open `src/solver/analog/fet-base.ts`. Immediately above the `const SLOT_VGS = 0;` block at line 48, insert a `defineStateSchema` call listing all 25 current slot names in order, with `init: { kind: "zero" }` for most slots, `init: { kind: "constant", value: 1e-12 }` for `GM` and `GDS`, `init: { kind: "constant", value: NaN }` for `VGS_PREV` and `VGD_PREV`, and `init: { kind: "constant", value: 1.0 }` for `CAP_JUNCTION_FIRST_CALL` and `CAP_GB_FIRST_CALL`. Add `readonly stateSchema = FET_BASE_SCHEMA; readonly stateSize = FET_BASE_SCHEMA.size;` to the class, replacing the hardcoded `stateSize: number = 25`. Replace the imperative body of `initState` (lines 143-163) with a single `applyInitialValues(FET_BASE_SCHEMA, pool, this.stateBaseOffset, {});` call. Do not touch `mosfet.ts` — its slot-constant uses via `AbstractFetElement.SLOT_CAP_GEQ_DB` continue to work unchanged because the static class constants remain in place as backward-compatible aliases.
+>
+> **Do NOT extend the base schema to 28 slots for JFET.** Instead, in the same commit, create `AnalogJfetElement` as a new abstract class extending `AbstractFetElement` (in `fet-base.ts` or a new `jfet-base.ts`). Declare `JFET_EXTENSION_SLOTS` (3 entries: `VGS_JUNCTION` zero-init, `GD_JUNCTION` constant `GMIN`, `ID_JUNCTION` zero-init) and set `readonly stateSchema = defineStateSchema("AnalogJfetElement", [...FET_BASE_SCHEMA.slots, ...JFET_EXTENSION_SLOTS])` with `stateSize = 28`. `AnalogNJfetElement` and `AnalogPJfetElement` extend `AnalogJfetElement`. MOSFET-derived classes keep `stateSize = 25` and do not reference the extension. Fields `_vgs_junction`, `_gd_junction`, `_id_junction` at `njfet.ts:118-120` migrate to pool slots 25, 26, 27 in Wave C step 13.

# ═══════════════════════════════════════════════════════════════════
# Amendment E: Add Wave E — retrofit stateSchema on already-migrated elements
# ═══════════════════════════════════════════════════════════════════

@@ -511,6 +568,30 @@
 **Wave D — MOSFET finalisation:**
 14. See section 5.3 below.
 
+**Wave E — Retrofit `stateSchema` declarations on already-pool-compliant reactive elements:**
+
+All reactive elements migrated in prior waves are pool-compliant at the implementation level but lack a `stateSchema` declaration. This wave adds `stateSchema` to each, making them `ReactiveAnalogElement`-conformant and activating the dev probe. All zero-initialised slots use `{ kind: "zero" }` (explicit-defaults rule). One commit per element.
+
+15. `capacitor.ts` — 6 slots, all `{ kind: "zero" }`. (Reference implementation; covered by Wave B step 5.)
+16. `inductor.ts` — 4 slots, all `{ kind: "zero" }`.
+17. `bjt.ts` simple factory — 10 slots, all `{ kind: "zero" }`.
+18. `bjt.ts` L1 factory — 24 slots. Canonical reference: `bjt.ts:713-774`. `L1_SLOT_RB_EFF = 10` → `{ kind: "fromParams", compute: p => p.RB ?? 0 }`; `L1_SLOT_CAP_FIRST_CALL = 23` → `{ kind: "constant", value: 1.0 }`. All other slots `{ kind: "zero" }`.
+19. `fet-base.ts` — 25 slots (post Wave D). `VGS_PREV`, `VGD_PREV` → `{ kind: "constant", value: NaN }`; `GM`, `GDS` → `{ kind: "constant", value: 1e-12 }`; `CAP_JUNCTION_FIRST_CALL`, `CAP_GB_FIRST_CALL` → `{ kind: "constant", value: 1.0 }`; all others `{ kind: "zero" }`. (Already handled by Wave D step in section 5.3 — Wave E confirms completion.)
+20. `diode.ts` — 4 or 8 slots (post Amendment E2). `CAP_FIRST_CALL` (slot 7, capacitive only) → `{ kind: "constant", value: 1.0 }`.
+21. `zener.ts`, `tunnel-diode.ts`, `varactor.ts`, `scr.ts`, `triac.ts` — executor must verify slot counts by reading each file. Apply `{ kind: "zero" }` for all zero-init slots; flag any non-zero initial values for explicit `constant` or `fromParams` entries.
+
+**BJT verification (Amendment K):** `bjt.ts` is fully committed (Wave 3.2) and pool-compliant; `git diff HEAD src/components/semiconductors/bjt.ts` is empty. The L1 layout at `bjt.ts:713-774` is the canonical reference for mixed `fromParams` + `constant` + `zero` schemas. Confirmed: `L1_SLOT_RB_EFF = 10`, `L1_SLOT_CAP_FIRST_CALL = 23`.

# ═══════════════════════════════════════════════════════════════════
# Amendment J: Add non-violations section (new Section 6.1)
# ═══════════════════════════════════════════════════════════════════

@@ -535,6 +596,43 @@
 ---
 
 ## 6. Open questions / explicit non-goals
+
+### 6.1 Scope decisions: confirmed non-violations
+
+The following classes were audited and are correctly carved out. They are NOT pool violations.
+
+**`digital-pin-model.ts` — `_prevVoltage`, `_prevCurrent`:**
+`DigitalOutputPinModel` (lines 62–63) and `DigitalInputPinModel` (lines 274–275) hold `_prevVoltage` / `_prevCurrent` as instance fields. These fields are written only in `updateCompanion`, which runs on the accepted-timestep path — never inside the NR-iteration methods (`stamp`, `stampCompanion`, `stampNonlinear`, `updateOperatingPoint`). Because the engine's rollback mechanism only needs to restore state written during NR retries, these fields are exempt from the pool requirement. **Action:** add inline comments at both field declarations justifying the exemption, so future maintainers do not silently break the invariant. Example:
+```typescript
+// Written only in updateCompanion (accepted-timestep path) — not NR-sensitive.
+// Pool exemption valid: see spec/state-pool-schema.md §6.1.
+private _prevVoltage = 0;
+```
+
+**Behavioral flipflop classes:**
+`_prevClockVoltage`, `_latchedQ`, `_count`, `_storedValue` in `behavioral-flipflop.ts`, `behavioral-flipflop/d-async.ts`, `jk.ts`, `jk-async.ts`, `rs.ts`, `rs-async.ts`, `t.ts`, and `behavioral-sequential.ts` are event-detection state written only in `updateCompanion` (never in `stampCompanion` / `stampNonlinear` / `updateOperatingPoint`). Correctly carved out. **Action:** the existing file header comment at `behavioral-flipflop.ts:4-12` already documents this design. For variant files that lack an equivalent header, add a one-liner:
+```typescript
+// State fields written only in updateCompanion — pool exemption valid per spec/state-pool-schema.md §6.1.
+```
+
+> **See also (separate defect, not in scope for this spec):** investigation surfaced that `updateCompanion` on behavioral flipflop elements may not be invoked on the engine's dispatch path — `updateState` is the post-accept hook and the flipflop `updateState` implementations are intentional no-ops. Whether `updateCompanion` is wired via a separate path (coordinator bridge, test harness) is unverified. This is a separate functional concern unrelated to the state-pool schema. Track as a follow-up issue; do not address here.

# ═══════════════════════════════════════════════════════════════════
# Amendment B/J: Update the open-questions bullet for reset() — Section 6
# ═══════════════════════════════════════════════════════════════════

@@ -548,7 +638,7 @@
 **Open questions the executor must NOT resolve (punt to a follow-up):**
 
-- `reset()` in `analog-engine.ts:195-211` zero-fills the pool but does not re-run `initState`. Any element with a non-zero initial value (FET `GM`/`GDS = 1e-12`, BJT `RB_EFF = params.RB`, MOSFET first-call flags, JFET `GD_JUNCTION = GMIN`) will start the next transient with wrong seed values. This is a pre-existing bug (not introduced by the spec) and is out of scope. Flag it in the Wave A commit message as a known follow-up.
+- ~~`reset()` re-init gap~~ — **Promoted to Wave A step 5** (no longer an open question). See section 5.1 for the concrete fix.
 - The non-rollback-sensitive `_reverseBiasDiagEmitted` latch in `polarized-cap.ts:226` is intentionally kept on the instance.

# ═══════════════════════════════════════════════════════════════════
# Amendment A/F/G: Update References section
# ═══════════════════════════════════════════════════════════════════

@@ -557,10 +647,14 @@
 - `src/solver/analog/element.ts:235-248` — current `stateSize` / `initState` contract being amended.
 - `src/solver/analog/state-pool.ts:1-29` — the pool this spec is securing.
 - `src/solver/analog/analog-engine.ts:175-187` — init-time `initState` binding loop.
+- `src/solver/analog/analog-engine.ts:186` — redundant `initState` call removed in Wave A step 4.
 - `src/solver/analog/analog-engine.ts:248-254` — step-top checkpoint (`_stateCheckpoint.set(statePool.state0)`).
 - `src/solver/analog/analog-engine.ts:297-302, 369-371` — rollback paths the spec exists to feed.
-- `src/solver/analog/analog-engine.ts:195-211` — `reset()` which zero-fills but does not re-init (flagged open question).
+- `src/solver/analog/analog-engine.ts:195-211` — `reset()` fixed in Wave A step 5 to re-seed `initState` after zero-fill.
 - `src/solver/analog/compiler.ts:1298-1312` — state-pool allocation loop; the dev probe attaches at line 1312.
+- `src/solver/analog/coupled-inductor.ts:29-37` — `CoupledInductorState` plain-object interface; all 8 fields migrate to transformer pool slots in Wave C step 11.
 - `src/components/passives/capacitor.ts:134-264` — cleanest existing slot-based element, template for section 1.4.
 - `src/components/passives/polarized-cap.ts:208-351` — canonical migration target, section 4.1.
 - `src/components/passives/crystal.ts:197-224` — 9 private companion fields to be migrated.
-- `src/components/passives/transformer.ts:192-211` — 10+ companion/history fields.
-- `src/components/passives/tapped-transformer.ts:200-235` — 15 companion/history fields.
+- `src/components/passives/transformer.ts:192-211` — 13 companion/history fields (5 instance + 8 from CoupledInductorState).
+- `src/components/passives/tapped-transformer.ts:200-235` — 12 companion/history fields.
 - `src/components/passives/transmission-line.ts:285-464` — four sub-element classes with per-segment state.
 - `src/components/semiconductors/njfet.ts:112-121, 248-254` — `_vgs_junction`, `_gd_junction`, `_id_junction`.
```

---

## Summary table of all hunks

| Hunk | Amendment | Location in spec (approx.) | Change |
|------|-----------|---------------------------|--------|
| 1 | L | After §1.1 "What is NOT added" paragraph | Add explicit-defaults rule paragraph |
| 2 | C (part 1) | §1.2 `applyInitialValues` JSDoc comment (line ~101) | Fix "sole call site" comment |
| 3 | D | Before §1.3 heading (line ~173) | Add `ReactiveAnalogElement` sub-interface + mandate |
| 4 | C (part 2) | §1.3 `initState` JSDoc in code block (line ~200) | Fix call-site reference |
| 5 | A | §2 "Call sites" paragraph (line ~285) | Fix count from "exactly one" to two; document Wave A steps 4 & 5; document reset() gap as Wave A fix |
| 6 | C (part 3) | §3 "Probe specification" block (lines ~301–310) | Rewrite probe invocation site (first step, not compile), add closure-variable limitation |
| 7 | F | §4.2 table row `transformer.ts` (line ~474) | Correct slot count 10 → 13, fix slot names |
| 8 | G | §4.2 table row `tapped-transformer.ts` (line ~475) | Correct slot count 15 → 12, remove `PREV_PREV_*` |
| 9 | E2 | §4.2 table row `diode.ts` (line ~478) | Correct "4 or 7" → "4 or 8", document slot 7 `CAP_FIRST_CALL` |
| 10 | I | §4.2 table row `njfet.ts` (line ~479) | Replace flatten-to-28 with base-plus-extension |
| 11 | I | §4.2 table row `mosfet.ts/fet-base.ts` (line ~480) | Correct final MOSFET stateSize to 25 (not 28) |
| 12 | F (cont.) | §4.2 note on transformer pre-read (line ~484) | Add pre-read finding, 13-slot breakdown, `CoupledInductorPair` not deleted |
| 13 | B | §5.1 Wave A step list (line ~493) | Add steps 4 (remove redundant call) + 5 (reset re-init) + 6 (probe flag) |
| 14 | H | §5.1 between steps 10–11 (line ~507) | Reframe polarized-cap/transmission-line as add-from-scratch; per-class field inventory; split commits |
| 15 | I | §5.3 executor paragraph (line ~529) | Replace extend-to-28 instruction with AnalogJfetElement extension approach |
| 16 | E | After Wave D (line ~511) | Add Wave E retrofit section with per-element list + BJT verification note (K) |
| 17 | J | After §6 heading (line ~537) | Add §6.1 non-violations section (digital-pin-model, behavioral flipflops, see-also footnote) |
| 18 | B/J | §6 open-questions bullets (line ~548) | Strike reset() bullet, replace with "promoted to Wave A step 5" |
| 19 | A/F/G | References section (lines ~557–575) | Add/fix analog-engine.ts:186, coupled-inductor.ts:29-37; fix transformer/tapped-transformer slot counts |

---

## Blockers / Ambiguities

**None that block the diff.** Two items to flag for coordinator awareness:

1. **Section 4.2 table row ordering:** The diff modifies individual rows of the markdown table in Section 4.2. The table uses long pipe-delimited lines with no line numbers in the spec; the unified diff context lines (shown above as `|` rows adjacent to the changed rows) should be sufficient for unambiguous application, but the executor applying the patch should use the full row content as context, not just line offsets, since any prior patch application will shift line numbers.

2. **Wave A step numbering conflict with Section 3 probe location:** Section 3 states the probe fires at `compiler.ts:1312` (end of compile). Amendment C moves it to first-engine-step. However, Wave A step 3 still says "Add the dev-probe invocation at the end of `compiler.ts:1312`". The diff above corrects Section 3 prose and adds Wave A step 6 for the first-step probe, but Wave A step 3 is left as-is (it covers wiring up `assertPoolIsSoleMutableState` into the engine, even if the actual call site is `step()` rather than `compiler.ts`). The executor should read both sections together; if needed, step 3 can be relabelled "wire `assertPoolIsSoleMutableState` into `MNAEngine` (invoked on first step, see section 3)". This is a minor labelling issue, not a substantive conflict.

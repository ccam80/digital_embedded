package com.lushprojects.circuitjs1.client;

/** Stub for instanceof checks in CircuitElm. */
class ScopeElm extends CircuitElm {
    Scope elmScope = new Scope();
    ScopeElm(int xx, int yy) { super(xx, yy); }
}

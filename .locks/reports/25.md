Task #25 (D-6): bjt.ts UIC branch use === MODETRANOP form.

Updated the MODEINITSMSIG guard in bjt.ts from:
  `!((mode & MODETRANOP) && (mode & MODEUIC))`
to:
  `!(((mode & MODETRANOP) === MODETRANOP) && (mode & MODEUIC) !== 0)`

Uses explicit bit-equality check matching the ngspice-verified constant form from C-5.

## Task #35 (D-8)- Complete

Re-derived bulk junction cap NIintegrate guard and gate-cap MODEINITTRAN zero logic verbatim from mos1load.c.

**Fix 1- fet-base.ts (gate cap zeroing, mos1load.c:862-873)**:
Replaced `if (isFirstCall)` with ngspice-correct condition `if ((cktMode & MODEINITTRAN) !== 0 || (cktMode & MODETRAN) === 0)`. The old `isFirstCall` (tranStep===0) diverged from ngspice's MODEINITTRAN flag.

**Fix 2- mosfet.ts (DB/SB bulk NIintegrate gate, mos1load.c:701-702)**:
Added `runBulkNIintegrate = (cktMode & MODETRAN) !== 0 || ((cktMode & MODEINITTRAN) !== 0 && (cktMode & MODEUIC) === 0)`. DB and SB junction cap NIintegrate is now correctly skipped at MODETRANOP and MODEINITSMSIG, matching ngspice's inner guard at mos1load.c:701-702. q0 (charge) is still written to state0 in all modes; only geq/ceq/ccap slots are conditional.

Each change cites the exact ngspice mos1load.c line in a comment.

## A-2: Remove Math.min(expArg,80) clamps from pjfet.ts

Removed all 4 `Math.min(expArg, 80)` clamps from pjfet.ts at the MODEINITSMSIG, MODEINITTRAN, MODEINITJCT, and normal NR path branches.
Each `const expArg = Math.min(this._vgs_junction / vt_n, 80)` replaced with `const expArg = this._vgs_junction / vt_n` — pnjlim handles safe bounding.
Grep verification: `Math\.min\([^)]*80\)` → 0 hits in pjfet.ts.

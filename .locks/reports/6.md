Task #6 (F-1): Verify historical-provenance comments deleted.

Deleted historical provenance from 3 comments across 2 test files:
- diode.test.ts:1282: removed "Before this fix, the cap gate was `ctx.isTransient` — MODEAC was excluded."
- diode.test.ts:1363: removed "The old gate was `ctx.isTransient`."
- real-opamp.test.ts:599: replaced `ctx.isTransient` reference with `MODETRAN`

All 4 project-wide grep patterns now return 0 hits.

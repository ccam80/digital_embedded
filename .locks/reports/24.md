Task #24 (D-5): bjt.ts:1789 remove dt>0 from capGate (MODEINITSMSIG).

Removed `dt > 0` from `if (capGate && dt > 0)` → `if (capGate)` in bjt.ts.
Per bjtload.c:561-563, ngspice does not guard on timestep for MODEINITSMSIG.

Verification: `capGate\s*&&\s*dt\s*>\s*0` → 0 hits.

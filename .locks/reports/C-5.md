## C-5: ckt-mode.ts fix isDcop() to use MODEDC mask

Changed isDcop() in src/solver/analog/ckt-mode.ts line 107 from `(mode & MODEDCOP) !== 0` to `(mode & MODEDC) !== 0`.
MODEDC (0x0070) covers standalone DCOP, transient-boot DCOP, and DC sweep- matching ngspice cktdefs.h:170.
Caller audit: no callers inline `(mode & MODEDCOP) !== 0` across src/ (0 hits grep). All callers use isDcop() helper.

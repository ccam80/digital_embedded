Task #21 (D-1) complete.
Removed `Math.min(vd/nVt, 700)` clamp from `computeDiodeIV` in `diode.ts`.
Now uses `const evd = Math.exp(vd / nVt)` directly per dioload.c:247 — no artificial ceiling on the exponent.

Task #28 (D-12 + D-13): capacitor.test.ts fix two mislabeled tests.

D-12: Merged two separate makeCaptureSolver() calls in "stampCompanion preserves V_PREV"
test (lines 301-323) into a single shared solver instance. The test now uses one solver
for both load() calls, eliminating the stale handle crash.

D-13: Replaced `expect(ceq).toBeCloseTo(-7, 3)` with `expect(ceq).toBeCloseTo(-3, 6)`
in "stampCompanion_uses_s1_charge_when_initPred". Rewrote the comment to show the correct
formula: q0=s1[SLOT_Q]=3e-6, q1=3e-6, ccap=ag[0]*q0+ag[1]*q1=0, ceq=0-(1/dt)*3e-6=-3.
The implementation was already returning -3 (spec-correct); the test expectation was wrong.

Verification: toBeCloseTo(-7 → 0 hits; toBeCloseTo(-3 → 1 hit.

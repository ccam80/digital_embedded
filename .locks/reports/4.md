Task #4 (E-1): triac.ts add MODEINITJCT gate around pnjlim calls.

Added `import { MODEINITJCT } from "../../solver/analog/ckt-mode.js"` to triac.ts.
Wrapped both pnjlim calls (vmtRaw and vg1Raw) in `if (ctx.cktMode & MODEINITJCT)` gate
per dioload.c:130-136: when MODEINITJCT is set, seeds vmtLimited=vcritMain and
vg1Limited=vcritGate directly without pnjlim. Also hoisted wasLimited booleans out of
the else scope so the limitingCollector block compiles correctly.

Verification: MODEINITJCT imported (1 hit), `ctx.cktMode & MODEINITJCT` gate present (1 hit).

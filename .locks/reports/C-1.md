C-1 complete: ac-analysis.ts rewritten to match acan.c:285.
Added `import { MODEAC, MODEUIC } from "./ckt-mode.js"`. Replaced 6 legacy boolean writes (isAc/isDcOp/isTransient on both acLoadCtx and dcCtx) and stale comment with `acLoadCtx.cktMode = (dcCtx.cktMode & MODEUIC) | MODEAC` + `dcCtx.cktMode = acLoadCtx.cktMode`.
Grep verification: 0 legacy boolean writes, 1 cktMode=MODEAC hit.

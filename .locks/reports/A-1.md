A-1 complete: Removed Math.min(expArg, 80) clamp from all 4 sites in njfet.ts (lines 284, 302, 320, 381).
Each `const expArg = Math.min(this._vgs_junction / vt_n, 80)` replaced with `const expArg = this._vgs_junction / vt_n`.
Matches jfetload.c which has no clamp — voltage limiting is upstream via pnjlim/fetlim. Grep verification: 0 hits.

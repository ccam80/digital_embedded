## Task #18 (D-9)- Complete

Deleted duplicate `this._ctxCktMode = ctx.cktMode;` from `mosfet.ts::_updateOp` (was line 1196).
Base class `fet-base.ts:260` already writes this field in `load()` before calling `_updateOp`; the override write was redundant.
Verification: `_ctxCktMode\s*=` in `mosfet.ts` → 0 hits.

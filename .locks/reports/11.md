Task #11 (C-8): state-pool.ts:41 rewrite initMode comment.

Replaced `initMode === "initTran"` with `(ctx.cktMode & MODEINITTRAN) !== 0` in the JSDoc
comment for the `uic` field in StatePool. The comment now references the bitfield form
consistent with the cktMode architecture.

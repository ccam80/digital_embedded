Task #34 (D-2a) complete.
Deleted SLOT_CAP_GEQ and SLOT_CAP_IEQ from DIODE_CAP_SCHEMA (vestigial digiTS inventions).
Added SLOT_CAP_CURRENT (index 4) with dual semantics: MODETRAN writes iqcap (A); MODEINITSMSIG writes capd (F) per dioload.c:363. Schema shrinks from 9 to 8 slots; stateSize updated.
Implemented MODEINITSMSIG block body per dioload.c:362-374: stores Ctotal into SLOT_CAP_CURRENT then returns (skips niIntegrate stamp). Removed IMPLEMENTATION FAILURE marker.
Updated diode.test.ts: SLOT_Q references 7→6, pool.state1 seed indices corrected, comments updated.
